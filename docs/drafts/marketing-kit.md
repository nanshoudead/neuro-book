# NeuroBook 宣传材料弹药库（Marketing Kit）

> 用途：制作 PPT、发帖、演讲时直接取用的定位语、痛点叙事、功能卖点、对比表和演示脚本。
> 状态：草案，随产品版本更新。数字类素材（规则数、版本号）引用前先核对最新值。
> 诚实性红线：不说「开源」（License 是 PolyForm Noncommercial，说「源码可获取 / source-available」）；不宣传 RP 模式为当前可用主功能（写作模式 v1 已隐藏 RP 入口，表述为「重新设计中」）；不编造用户数 / 星数。

## 1. 一句话定位（Tagline）

主推版（PPT 封面 / GitHub About）：

> 本地优先的长篇小说 AI 创作 IDE —— 世界引擎防吃书，多 Agent 流水线写章节，llmlint 去 AI 味。

备选：

- 短版：**让 AI 写得了长篇的 IDE。**
- 对比版：**AI 聊天框写得好一段话，NeuroBook 写得完一部书。**
- 痛点版：**设定不漂移、时间线不混乱、文字没有 AI 味。**
- 英文版：**A local-first IDE for long-form fiction — a world engine that never forgets, an agent pipeline that writes chapters, and a linter that catches AI-sounding prose.**

## 2. 电梯陈述

30 秒版：

> AI 能写好一段文字，但写不好一部长篇：写到几十万字，设定漂移、时间线混乱、开始吃书，而且整章丢给模型，回来的文字一股 AI 味。NeuroBook 把长篇创作当成 IDE 问题来解决：世界状态由专门的 World Engine 用时间线管理，不靠模型记忆；写作由 leader、writer、检索等多个 Agent 分工完成；成稿再过一遍 340 条规则的 llmlint，把 AI 味筛出来。整部作品是你本地的 Markdown 文件和 SQLite，随时带走。

90 秒版（在 30 秒版基础上追加）：

> 具体来说，World Engine 用「时间线 + 切面」做事件溯源：每个重要时间点记录一次状态变更，任意时刻的世界状态都能推算出来——角色三个月前受的伤、王国十年前的国库，随时可查。世界结构用 Zod schema 自己定义，历法可以完全架空。写作侧，默认主链是灵感探索 → 世界书初始化 → World Engine 建档 → 剧情规划 → 章节写作 → 写后回补，每个 Agent 有独立的工具权限：leader 可以改世界状态，writer 只能读。部署上 Windows 解压即用、服务器一行命令拉 Docker 镜像，模型 API Key 自己配，数据不出自己机器。

## 2.5 分渠道标题库（发帖 / 视频标题）

同一产品，不同渠道换钩子。**标题只承诺痛点解决，不堆术语。**

- **LINUX DO / V2EX（技术 + 自部署人群）**：`我做了一个本地优先的长篇小说创作 IDE：世界状态用事件溯源引擎管，多 Agent 写章节，数据全在自己手里`
- **NGA / 网文创作区（网文作者）**：`写到几十万字总吃书？我给小说做了个"天道"，谁在哪一刻是什么样它全记着`
- **知乎（深度长文）**：`长篇 AI 写作的真问题不是"写不出"，是"写到后面世界散架"——我是怎么用一个世界引擎解决它的`
- **Reddit r/WritingWithAI（英文）**：`I built a local-first novel-writing IDE with a world-state engine that never forgets your lore`
- **Reddit r/SillyTavernAI（英文 RP 人群）**：`Import your character cards into a workspace where the world actually evolves over time`
- **B 站 / 视频标题**：`让 AI 写完一整部小说而不写崩，我做了这个工具`

## 3. 痛点叙事（「问题」页素材）

每条 = 一页 slide：痛点标题 + 说明 + NeuroBook 的回答。

| # | 痛点标题 | 说明（讲稿） | NeuroBook 的回答（punchline） |
| --- | --- | --- | --- |
| 1 | 写长了就吃书 | 模型上下文装不下整部作品，设定靠对话记忆，越写越漂移：上一卷断了的手臂这一卷自己长回来了。 | 世界状态不靠模型记，靠引擎算。任意时刻的世界状态都可以从时间线推算出来。 |
| 2 | AI 写的一股 AI 味 | 填充词、机械过渡、公式化设问、「不是……而是……」，读者一眼识破。 | 像 eslint 检查代码一样检查稿件：340 条规则，静态规则秒扫全稿，LLM 规则做语境判断。 |
| 3 | 聊天框不是生产工具 | 设定贴在对话里、正文散落在会话记录里，换个工具全部重来。 | 设定、正文、剧情、世界状态全是本地文件，人和 Agent 改的是同一份 workspace，随时打包带走。 |
| 4 | 一次调用包打天下 | 让一个模型同时查设定、定剧情、写正文、管记忆，哪件都做不精。 | 领域化多 Agent：leader 规划、writer 写正文、retrieval 查设定，各自有工具白名单和上下文边界。 |

具体场景（讲「问题页」时用来带画面，任选其一）：

- **吃书**：「主角第 12 章左臂被斩断，写到第 88 章你早忘了，让 AI 续写，它给主角安排了一套双手剑法——你的读者比你记得清，评论区当场翻车。」
- **AI 味**：「『这不仅仅是一场战斗，更是一次灵魂的洗礼。』——这种句子你一眼就知道是 AI 写的，你的读者也是。」
- **散落**：「设定在这个对话、大纲在那个文档、正文在第三个地方，换个 AI 工具，前面攒的全带不走。」

## 4. 三大支柱（核心功能页素材）

### 支柱一：World Engine —— 不吃书的世界状态引擎

副标题：任意时刻的世界状态，随时可查、永不漂移。

要点（slide bullets）：

- 「时间线 + 切面」事件溯源：每个重要时间点记录状态变更，任意时刻状态由之前切面推算得出。
- 补设定 = 在合适的时间点插一个切面；倒叙、回忆天然支持。
- 世界结构自己定义（Zod schema）：人物、门派、王国、大陆都可以是有状态的 subject。
- 历法自定义：现实公历、简化纪年、完全架空的历法都支持，公元前也能算。
- Agent 沙箱读写分权：leader 可写、writer 只读，写作时不会误改世界。
- 剧情 Scene 锚定世界时间轴、地点和出场角色，剧情规划与世界状态互相咬合。
- 每一次状态变更都是一条可审计的记录，引擎还会主动指出矛盾（如引用了不存在的主体），带解释、分级别。

讲稿金句：

> 长篇最大的敌人不是写不出来，是写着写着世界散架了。

> World Engine 就是你世界的「天道」：谁在什么时候变成了什么样，天道都记录在案；写出矛盾，天道会指出来。（网文受众场合用）

> 设定卡片记录「他是谁」，World Engine 记录「他在每一刻是谁」。（面对"和设定库有什么区别"时用）

演示建议：Workbench 里选一个 slice 点「查询此时状态」，展示同一个角色在两个时间点的不同状态。

### 支柱二：多 Agent 写作流水线 —— 不是聊天框，是工作室

副标题：检索、规划、写作、审校，各归其位。

要点：

- 默认写作主链：灵感探索 → 项目与世界书初始化 → World Engine 建档 → 剧情规划与状态推进 → 章节写作 → 写后回补。
- leader 负责剧情规划与调度，writer 专职正文，retrieval / researcher 查设定查资料。
- 每个 Agent 有独立的工具白名单、上下文模板和压缩策略。
- 全程人工审批（HITL）、会话树回溯、上下文压缩，长任务不失控。
- 编辑器内 Inline AI：选中即改、流式预览 diff、不打断主编辑流。

讲稿金句：

> 你不会让一个员工同时当编辑、资料员和写手，为什么让一个模型这么干？

> 你面对的不是一个冰冷的陌生工具，而是一个自带「器灵」的工作室：配好 API Key，剩下的路器灵带你走。（上手体验场合用）

演示建议：让 leader 规划下一章 → 展示它调用 World Engine 推进状态 → 调用 writer 产出正文。

### 支柱三：llmlint —— 给文字做 lint

副标题：写完的最后一步，去掉 AI 味。

要点：

- 340 条规则（311 条默认启用），覆盖填充词、机械过渡、公式化设问、二元对比、空泛总结、节奏单调等典型 AI 痕迹。
- 双层规则：静态正则秒级扫全稿，LLM 规则做语境判断。
- 类 eslint 体验：多文件 / glob、Markdown 遮罩、JSON / stylish 输出、机械问题自动修复。
- 既是 NeuroBook 内的润色 Skill，也是独立 CLI 工具：github.com/notnotype/llmlint。

讲稿金句：

> 代码有 linter 三十年了，文字现在也有了。

演示建议：现场对一段典型 AI 生成文字跑 `llmlint check`，展示命中报告和修复前后对比。

## 5. 次级卖点（一页多条的「还有更多」slide）

- 📂 数据自持有：全 Markdown + 项目级 SQLite，无云端锁定，任何编辑器都能打开。
- 🔑 模型自选：多 Provider，API Key 自己配，成本自己控。
- 🎭 SillyTavern 角色卡迁移：inspect → unpack → import 三段式，原卡和 worldbook 完整归档，稳定设定迁入世界书。
- 📦 Windows 解压即用：内置 Bun runtime 和预构建产物，双击启动，一键升级且保留数据。
- 🧩 提示词三层透明（技术受众）：上下文可预览（不做黑箱）→ 常用调整走低代码选项 → 深度定制写 TSX Profile，而且有「用户资产助手」Agent 协助你改——让 Agent 帮你改 Agent。
- 🐳 服务器一行部署：`bunx --bun --package github:notnotype/neuro-book neuro-book-deploy`，低内存服务器拉预构建镜像。
- 🛠️ 可编程 Agent 底座（技术受众）：Profile 声明行为边界，TSX 类型安全地写上下文模板，Sidecar 旁路任务不污染主上下文。

## 6. 数字弹药（引用前核对）

| 数字 | 表述 | 来源 |
| --- | --- | --- |
| 340 / 311 | llmlint 规则总数 / 默认启用数 | docs/tasks/51、PROJECT-STATUS |
| 4 | 部署方式（Portable / ghcr / Product Bun / Source） | README、docs/deployment.md |
| 4 | World Engine 写入操作原语（replace / increment / remove / append） | docs/tasks/56 |
| 3 | SillyTavern 迁移阶段（inspect / unpack / import） | README |
| 4 | token 计量维度（输入 / 输出 / 缓存创建 / 缓存命中），支持货币换算 | ✅已核实代码：model-cost-draft.ts（inputTokens/outputTokens/cacheWrite/cacheRead）+ NovelIdeCostSettingsPanel.vue（USD/CNY + 汇率刷新） |
| v0.5.x | 当前版本线（canary 快速迭代中） | GitHub Releases |

注意：没有可公开的用户数 / 星数 / 留存数据，不要在 PPT 里放增长曲线占位图。

## 7. 竞争格局与应对口径（不点名版）

对外文案与 PPT **不点名任何具体竞品**，只按「工具类型」划分。每类给一句区隔口径，可直接当讲稿：

| 竞品类型 | 他们解决什么 | 一句话区隔口径 |
| --- | --- | --- |
| AI 聊天框 | 生成一段文字 | 模型负责生成，NeuroBook 负责让几十万字的工程不散架。 |
| 角色扮演工具（酒馆类） | 角色对话体验 | worldbook 是静态词条，World Engine 是随时间演化、可推算的世界。 |
| 静态设定库型工具（codex / 设定卡片 / 设定树） | 结构化管理设定 | 卡片记录「他是谁」，World Engine 记录「他在每一刻是谁」——设定卡是死档案，世界是活的。 |
| 自动量产型引擎（挂机连续生成） | 无人值守产出章节 | NeuroBook 不做挂机量产：当前模型无人值守的质量守不住。人类主导是产品立场，不是能力缺口。 |
| 通用编码 Agent 上的写作插件 | 借编码工具写小说 | 写作值得一个专用的底座：Web 界面而非终端、多 Provider 自由、上下文从第一行就为叙事设计，而不是在编码工具里借道写小说。 |

能力对比表（PPT 对比页用，列名用类型不用项目名）：

| 能力 | 聊天框 | 酒馆类 | 静态设定库型 | 量产引擎型 | NeuroBook |
| --- | --- | --- | --- | --- | --- |
| 长篇设定管理 | 靠对话记忆 | 静态词条 | 静态卡片 | 摘要链 | ✅ 随时间演化的世界状态 |
| 任意时刻状态推算 | ❌ | ❌ | ❌ | ❌ | ✅ 切面 reduce，可审计 |
| 一致性矛盾检测 | ❌ | ❌ | 有限 | 部分 | ✅ 引擎主动报 issue（如悬空引用） |
| 创作主导权 | 人 | 人 | 人 | 机器 | ✅ 人类主导 + Agent 执行 |
| AI 味检查 | ❌ | ❌ | ❌ | ❌ | ✅ llmlint 340 规则 |
| 数据归属 | 云端 | 本地 | 视产品 | 本地 | ✅ 本地文件 + SQLite |

内部观察（不进文案）：

- 「防写崩」叙事已被市场验证（同类最高 5.4k★），讲这个故事不需要教育用户，只需要讲清差异。
- 「schema 自定义 + 可编程工作流」已非独家卖点；差异必须落在「随时间演化 + 可审计 + 主动报矛盾」，不能停留在「灵活」。
- 短剧 / 漫剧影视化是当前开源创作工具的流量方向；结构化世界状态（人物、地点、时间线机器可读）是天然的影视化上游资产。作为远期叙事储备，不进当前文案。

## 7.5 产品立场页（价值观 slide，放在三支柱之后）

标题：**人类主导，Agent 执行。**

- 我们不做「睡一觉起来多了五章」：当前模型无人值守的产出质量守不住，挂机量产出来的是要重写的字数，不是作品。
- NeuroBook 的分工是：作者掌握每一个创作决定，Agent 负责检索、推演、执行和记账。
- 检索是主动的：RAG 不是在背后无脑注入相似片段，而是作为工具交给 Agent，让它带着意图自己判断何时该查、查什么（World Engine 经 CodeAct 已带 RAG 工具；lorebook 文件当前用 rg 等文件工具精确检索）。带意图的检索比被动相似度召回更准。

讲稿金句：

> 器灵替你跑流程，但每一个创作决定都是你按的。

> AI 是器灵，不是代笔。

## 7.6 定位页：我们做基础设施，功能由社区长出来（价值观 slide）

标题：**NeuroBook 是底座，不是全家桶。**

- 别的工具比谁的功能清单更长：关系图、AI 封面、拆书、伏笔表……做成一个个焊死的按钮。
- NeuroBook 反过来：把 World Engine、多 Agent、文件化 workspace、Skill 机制做成**可编程的地基**，具体功能作为 Skill 在上面生长。
- 好处对作者：你要的功能不必等官方排期——它可以是一个 Skill，你自己写、或从社区拿。
- 好处对生态：关系图、封面生成、txt 拆书迁移这类需求，都能由社区 Skill「长出来」，而不是挤在一个团队的路线图里。

讲稿金句：

> 与其给你一百个焊死的按钮，不如给你长出一百个按钮的土壤。

对外纪律（内部备注，不上 slide）：存量迁移、关系图 / 封面、伏笔追踪这三类，统一归到「Skill 生态会长出来」的框里，**不做功能承诺、不进主推清单**；被追问才用本页口径接住，绝不主动演示未完成能力。

## 8. 目标用户画像（「为谁而做」页）

1. **长篇网文作者**：几十万字以上、多卷本，苦于设定表和时间线维护。核心钩子：World Engine。
2. **AI 辅助写作重度用户**：已经在用 AI 写作但被 AI 味和上下文限制折磨。核心钩子：llmlint + 多 Agent。
3. **SillyTavern / RP 玩家**：手里有大量角色卡和 worldbook 资产，想升级到有状态的世界。核心钩子：角色卡迁移 +（未来）RP 模式。口径注意：RP 模式表述为「重新设计中」。
4. **自部署爱好者**：要求数据本地、模型自选。核心钩子：本地优先 + 一行部署。

## 9. 演示脚本（Live Demo 流程，约 8 分钟）

1. **开场（30s）**：打开 Bookshelf，展示一个已有项目的 workspace 文件树——「这就是一部作品的全部，都是文件」。
2. **World Engine（3min）**：打开 Workbench → 看 timeline → 选一个历史 slice 查询「此时状态」→ 现场写一个切面（如「主角受伤」）→ 再查询状态变化。强调：这些不是笔记，是可推算的状态。
3. **写作主链（3min）**：对 leader 说「规划下一章」→ 展示它读世界状态、更新剧情 Scene → 调用 writer 产出正文到 manuscript → 展示 HITL 审批点。
4. **llmlint（1.5min）**：对刚生成的正文跑润色 Skill / CLI，展示命中规则和修复。
5. **收尾（30s）**：回到文件树——「刚才所有产出都落在这些本地文件里，打包就能带走」。

Demo 风险预案：现场模型调用可能慢或失败，提前录一段备用视频；World Engine 查询是纯本地计算，网络差时把演示重心放在支柱一。

## 10. 技术架构页（技术受众场合选用）

- 技术栈：Nuxt 4（Vue 3，实测 4.3.1）+ Bun runtime + SQLite（App 级 + Project 级双库）+ Prisma 7。
- Agent 底座：NeuroAgentHarness，基于 Pi 框架扩展（multi-provider、tool calling、append-only session tree），支持 Multi-Agent、HITL、运行时 Profile / Tool Catalog、上下文压缩、Runtime Hooks。
- Profile 系统：TSX 作为上下文模板语言，类型安全、可预览、内容寻址编译产物 + 原子发布。
- World Engine：事件溯源三表（WorldSubject / WorldSlice / WorldPatch），patches + 4-op 写入协议，CodeAct 沙箱暴露分组 API（world.time / subject / search / slice）。
- 分发：Windows Portable（内置 Bun + 预构建 payload）、GHCR 预构建镜像，服务器零构建。

## 11. 反对意见应对（Q&A 备稿）

- **「为什么不开源？」**——源码完全公开可自由使用、修改、分享（PolyForm Noncommercial），只限制把软件本身拿去商用；**作者用它写的作品商业出版完全不受限**。
- **「我的稿子会不会被拿去训练？」**——数据全在本地文件和本地数据库，模型调用走你自己配置的 Provider，项目方不经手内容。
- **「模型费用谁出？」**——自带 API Key，多 Provider 可选。token 统计比市面绝大多数 Agent 工具透明：输入、输出、缓存创建、缓存命中分项计量，并直接换算成美元 / 人民币——你能确切知道「写这一章花了多少钱」。
- **「和直接用 Claude/GPT 写有什么区别？」**——回到痛点页：模型负责生成，NeuroBook 负责让几十万字的工程不散架。
- **「为什么不能挂机自动写几十章？」**——立场问题，不是能力问题：当前模型无人值守的产出质量守不住，挂出来的是要重写的字数。NeuroBook 的分工是人类主导创作决定，Agent 负责检索、推演和执行。
- **「装个编码 Agent 的写作插件不就行了？」**——三点：①那是终端工具，普通作者要先学会用命令行；NeuroBook 是 Web 界面，打开就是写作台。②那类插件锁定单一厂商订阅；NeuroBook 多 Provider 自由。③编码 Agent 的系统提示词和工具面是为写代码设计的，等于在编码工具里借道写小说；NeuroBook 的上下文工程从第一行就为叙事而生。
- **「和设定卡片 / 设定树类工具有什么区别？」**——卡片记录「他是谁」，World Engine 记录「他在每一刻是谁」。设定卡是死档案，改了就把旧状态抹掉；World Engine 的每次变更都是带时间戳的可审计记录，任意时刻状态可回放，写出矛盾（比如引用了不存在的人物）引擎会主动报出来。
- **「伏笔追踪有吗？」**——分层答：World Engine 管世界状态，Plot Workbench 管叙事结构；伏笔登记 / 推进 / 回收属于剧情工坊路线图上的下一步，底层的时间轴和 Scene 结构已经为它备好了地基。（不在主文案宣传，仅被问时答。）
- **「几百万字的存量作品怎么迁进来？」**——当前通过 Agent + 拆书类 Skill 处理；一键式 txt 导入在 Skill 生态路线图上。（诚实口径：还不是一键，不要承诺。）
- **「有没有人物关系图 / AI 封面这些？」**——不内置全家桶，走 Skill 生态扩展；当前主战场是「让长篇不写崩」这条主链。
- **「RP 功能呢？」**——RP / 世界模拟能力在底层保留，入口正按写作模式的体验标准重新设计；当前版本主打写作模式。
- **「成熟度如何？」**——v0.5 快速迭代阶段，写作模式主链已跑通端到端验证；适合尝鲜共建，不承诺 API 稳定。

## 12. PPT 大纲

已升级为 §15 逐页设计稿（14 页完整版式 + 页面文案 + 口播讲稿，末尾附 5 分钟闪电版 / 技术分享版 / 网文路演版三种裁剪）。制作 PPT 直接从 §15 取料，本节不再单独维护页面清单，避免两处口径漂移。

## 13. 视觉素材清单

已有（docs/images/）：

- 主页.png、TSX可视化编辑器.png
- 剧本工作台.png（⚠️ 需核对是否为 Task 78 Scene-only 改版后的界面，过时则重拍）

待补（按优先级）：

1. World Engine Workbench：timeline + 状态查询结果（支柱一主图，最高优先级）。
2. llmlint 检查报告（CLI stylish 输出或编辑器内呈现）。
3. 写作主链示意图（灵感 → 世界书 → World Engine → 剧情 → 章节 → 回补），需要手绘 / 制图。
4. Inline AI 改稿的流式 diff 预览动图。
5. Multi-Agent 会话树 / HITL 审批界面截图。

## 14. 术语一致性表（对外统一口径，防止一稿多名）

| 统一用法 | 禁止 / 避免 | 说明 |
| --- | --- | --- |
| World Engine / 世界引擎 | 世界模拟、simulation、剧情系统 | 对外只用「世界引擎」；simulation 是 legacy 内部名 |
| 切面（slice） | mutation、entry、快照 | 一个时间点的状态变更记录 |
| 主体（subject） | 实体、entity、对象 | 人物 / 门派 / 王国等有状态的存在 |
| 器灵 | 助手、bot、AI 管家 | 拟人化叙事专用词，网文受众语境 |
| 天道 | 监工、裁判、校验器 | World Engine 的拟人比喻，仅网文场合 |
| 世界书（lorebook） | 设定集、wiki、资料库 | 稳定设定的文件目录 |
| Skill | 插件、脚本、扩展 | Agent 可调用的能力单元 |
| 本地优先 / source-available | 开源、免费开源 | License 是 PolyForm Noncommercial，不能称开源 |
| 章节写作主链 | 工作流、pipeline（对作者） | 面向作者说「主链」，面向工程师才说 pipeline |

## 15. PPT 逐页设计稿（照此填充幻灯片）

### 视觉基调

- **风格**：深色科技感 + 暖色点缀。深色背景呼应「本地 / IDE / 引擎」的工程气质，暖色（琥珀 / 橙）点缀呼应「创作 / 器灵 / 温度」。
- **主色建议**：背景近黑（#0E0E12），主文字近白，强调色紫（#8957E5，呼应 GHCR 徽章）+ 琥珀（#F5A623）。
- **字体**：中文思源黑体 / HarmonyOS Sans，标题加粗；代码 / 命令用等宽（JetBrains Mono）。
- **一页一个观点**：每页只讲一件事，标题就是结论，正文只做支撑。
- **少字多图**：正文 bullet 每条不超过一行；能上截图 / 示意图就不用文字。
- **口播讲稿**：以下每页的「口播」是演讲时说的话，不是要写在幻灯片上的字。
- **数字与术语**：页面上出现的所有数字以 §6 已核实值为准；用词遵守 §14 术语表。

---

**页 1 · 封面**

- 版式：居中大标题，底部留 logo 和一行链接。
- 大标题：**NeuroBook**
- 副标题：本地优先的长篇小说 AI 创作 IDE
- 底部小字：世界引擎防吃书 · 多 Agent 写章节 · llmlint 去 AI 味
- 配图：主页.png 作为背景弱化处理，或纯色背景 + logo。
- 口播：「大家好，今天介绍 NeuroBook——一个专门为写长篇小说做的 AI 创作 IDE。它要解决的不是怎么生成一段文字，而是怎么写完一整部书还不写崩。」

---

**页 2 · 问题（吃书）**

- 版式：左文右图，或上标题下对比图。
- 大标题：**写着写着，世界就散架了**
- 正文：模型上下文装不下整部作品，设定靠对话记忆 → 越写越漂移。
- 配图：「第 12 章：左臂被斩断」↔「第 88 章：一套双手剑法」的矛盾对比示意。
- 口播：用 §3 的「吃书」具体场景讲——「主角第 12 章断了左臂，写到 88 章 AI 给他安排双手剑法，你读者比你记得清，评论区当场翻车。这是所有长篇 AI 写作的第一个坑。」

---

**页 3 · 问题（AI 味 + 散落）**

- 版式：上下两块，各一个痛点。
- 大标题：**另外两个坑：AI 味，和带不走**
- 正文块 1（AI 味）：「这不仅仅是一场战斗，更是一次灵魂的洗礼」——一眼假。
- 正文块 2（散落）：设定、大纲、正文散在三个地方，换工具全带不走。
- 口播：「第二个坑是 AI 味，那种翻译腔的排比句读者一眼识破。第三个坑是，你的设定、大纲、正文散落在不同对话和文档里，换个工具前面全白攒。」

---

**页 4 · 定位（解法总览）**

- 版式：中间一句话，下方三个并排图标块（三支柱）。
- 大标题：**把长篇创作，当成一个 IDE 问题来解决**
- 三支柱标签：① World Engine 管世界 · ② 多 Agent 写章节 · ③ llmlint 去 AI 味
- 底部一行：而且，作品全是你本地的文件。
- 口播：「NeuroBook 的思路是把这三个坑当工程问题解决。世界状态交给一个引擎、写作交给一组分工的 Agent、成稿交给一个文字 linter。接下来分别讲这三块。」

---

**页 5 · 支柱一 World Engine（主图页）**

- 版式：右侧大截图（Workbench），左侧要点。
- 大标题：**World Engine：不吃书的世界状态引擎**
- 副标题：任意时刻的世界状态，随时可查、永不漂移
- 要点（3 条即可）：时间线 + 切面事件溯源；世界结构自定义（Zod schema）；历法可完全架空。
- 配图：World Engine Workbench 截图（timeline + 状态查询）。**（待补，最高优先级）**
- 配图退路：截图未就绪时可先用页 6 的切面示意图占位，正式版必须换真截图。
- 口播：「世界状态不放在模型记忆里，放在一个引擎里。它用时间线加切面记录：每个关键时刻发生了什么变更，任意时刻的世界状态都能算出来。角色三个月前受的伤、王国十年前的国库，随时可查。」

---

**页 6 · 支柱一续（原理 + 天道）**

- 版式：中间一张原理示意图，底部一句金句。
- 大标题：**谁在哪一刻是什么样，它全记着**
- 示意图：切面①「第 12 章 · 断臂」→ 切面②「第 30 章 · 拜师」→ 切面③「第 52 章 · 突破」…… ——reduce——▶ 「第 88 章此刻的完整状态」。切面直接用剧情事件命名，观众秒懂。
- 金句（大字）：**World Engine 就是你世界的「天道」——写出矛盾，它会指出来。**
- 口播：「更关键的是它是活的、可审计的。每次变更都是带时间戳的记录，你写出矛盾——比如引用了一个不存在的人物——引擎会主动报出来。我们内部叫它『天道』：谁在哪一刻是什么样，它全看着。这是设定卡片给不了的：卡片记录『他是谁』，World Engine 记录『他在每一刻是谁』。」

---

**页 7 · 支柱二 多 Agent 流水线**

- 版式：中间写作主链流程图，下方分工说明。
- 大标题：**不是聊天框，是一间工作室**
- 流程图：灵感 → 世界书 → World Engine 建档 → 剧情规划 → 章节写作 → 写后回补。
- 分工：leader 规划调度 · writer 专职正文 · retrieval 查设定；各有工具权限边界。
- 金句：**你不会让一个员工同时当编辑、资料员和写手，为什么让一个模型这么干？**
- 口播：「写作不是一句 prompt 的事。NeuroBook 把它拆给不同职责的 Agent：leader 规划、writer 写正文、检索去查设定。每个 Agent 有独立的工具权限，比如 leader 能改世界状态，writer 只能读，不会写着写着把世界改乱。全程你可以审批、可以回退。」

---

**页 8 · 支柱三 llmlint**

- 版式：右侧检查报告截图，左侧要点。
- 大标题：**llmlint：给文字做 lint，去掉 AI 味**
- 要点：340 条规则；静态规则秒扫全稿 + LLM 规则语境判断；机械问题自动修复；也是独立 CLI。
- 配图：llmlint 检查报告截图。**（待补）**
- 金句：**代码有 linter 三十年了，文字现在也有了。**
- 口播：「最后一步，去 AI 味。llmlint 像 eslint 检查代码一样检查你的稿子，340 条规则命中填充词、机械过渡、公式化设问这些痕迹，机械问题还能自动修。它既在 NeuroBook 里，也是一个独立开源 CLI。」

---

**页 9 · 立场（人类主导）**

- 版式：居中大字立场 + 三行支撑。
- 大标题：**人类主导，Agent 执行**
- 支撑：不做挂机量产（当前模型守不住质量）；你掌握每个创作决定；Agent 负责检索、推演、执行、记账。
- 金句：**AI 是器灵，不是代笔。**
- 口播：「有人问能不能挂机自动写几十章。我们刻意不做——当前模型无人值守的质量守不住，挂出来的是要重写的字数，不是作品。NeuroBook 的分工是：你做所有创作决定，Agent 是替你跑腿的器灵。」

---

**页 10 · 立场（底座而非全家桶）· 可选**

- 版式：左「别人：一堆焊死的按钮」右「我们：长按钮的土壤」对比。
- 大标题：**NeuroBook 是底座，不是全家桶**
- 正文：把 World Engine、多 Agent、文件化 workspace、Skill 做成可编程地基，功能作为 Skill 生长。
- 金句：**与其给你一百个焊死的按钮，不如给你长出一百个按钮的土壤。**
- 口播：「很多工具比谁功能清单长。我们反过来：做一个可编程的底座，具体功能——关系图、封面、拆书——作为 Skill 在上面长出来，你自己写或者从社区拿，不用等我们排期。」
- 备注：时间紧可与页 9 合并或跳过。

---

**页 11 · 还有更多（次级卖点）**

- 版式：2×3 图标网格。
- 大标题：**这些也都在**
- 六格：数据自持有（Markdown+SQLite）· 多 Provider 自选 · 透明 token 计费（可换算人民币）· SillyTavern 角色卡迁移 · Windows 解压即用 · 提示词三层透明。
- 口播：「快速带过一些别的：作品全是本地文件；模型自己选、API Key 自己配，而且 token 花费能精确到分项、换算成人民币；支持导入酒馆角色卡；Windows 解压即用。」

---

**页 12 · 对比页**

- 版式：能力对比表（§7 的类型化表格）。
- 大标题：**我们解决的，是另一层问题**
- 表格：行=能力，列=聊天框 / 酒馆类 / 静态设定库 / 量产引擎 / NeuroBook。
- 口播：「横向看一下。聊天框解决生成一段，酒馆解决角色对话，设定库解决静态管理，量产引擎追求无人值守。NeuroBook 的独特位置是：随时间演化、可审计、能主动报矛盾的世界，加上人类主导。」
- 备注：**列名用工具类型，绝不点名具体项目**；口径保持「解决的问题不同」，不贬低任何一类工具。

---

**页 13 · 上手**

- 版式：三段命令 + 在线试用二维码 / 链接。
- 大标题：**三行命令，或者解压即用**
- 内容：Windows 下载解压运行 `Start Neuro Book.cmd`；服务器 `bunx ... neuro-book-deploy`；在线试用地址 / 二维码。
- 口播：「上手很轻。Windows 用户下载解压双击就能跑，内置了 runtime 不用装环境；服务器一行命令拉预构建镜像。也可以扫码直接在线试。」
- 备注：在线试用若仍是裸 IP，考虑演讲时口头给，或先上域名。

---

**页 14 · 结尾**

- 版式：居中，仓库地址 + 文档 + 一句邀请。
- 大标题：**一起把长篇写完，而不是写崩**
- 内容：GitHub github.com/notnotype/neuro-book · 文档入口 · 「v0.5 快速迭代中，欢迎尝鲜共建」。
- 口播：「NeuroBook 还在快速迭代，写作主链已经跑通。如果你在写长篇、被吃书和 AI 味折磨过，欢迎来试，也欢迎一起共建。谢谢大家。」

---

### 场景裁剪建议

- **5 分钟闪电版（7 页）**：1 封面 → 2 问题合并（吃书+AI味） → 4 定位 → 5 World Engine → 7 多 Agent → 8 llmlint → 14 结尾。
- **技术分享版**：保留全 14 页，页 5/7 后各插一页 §10 技术架构细节；强化「事件溯源 / TSX Profile / CodeAct」。
- **网文作者路演版**：弱化技术架构，强化页 6「天道」和页 9「器灵」，多放 Workbench 和成稿对比实拍。
