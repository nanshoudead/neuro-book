# Project Status

## Summary

neuro-book 当前处于快速开发阶段。项目主线正在从数据库中心逐步转向文件化 Workspace Root / Project Workspace，并围绕 Markdown Studio、引用系统、剧情结构、多 Agent 协作、统一配置系统、全站鉴权、管理员后台和主界面账户入口补齐核心体验。

部署侧已硬切 SQLite-only：App SQLite 默认位于 `workspace/.nbook/neuro-book.sqlite`，只保存 `User`、auth、global config 相关应用级数据和全局锁；Project SQLite 位于每个 Project Workspace 内的 `.nbook/project.sqlite`，保存 Story / StoryPhase / StoryThread / StoryScene / StoryPlot / StorySceneRef 等项目结构化数据。PostgreSQL schema、adapter、pool、部署选项和 compose override 已移除，旧 PostgreSQL 数据不提供内置迁移。部署主线已调整为 Product-first：Windows Product Portable 是普通用户默认 release，zip 内包含 `app/` Product Payload、`data/` 运行状态、`runtime/bun/` 内置 Bun 和 `launcher/` Windows Launcher；启动不再 clone 源码、安装 Git/ripgrep、安装依赖或执行 Nuxt build。Product Payload 由 `product/` 本地成品验证根生成：`bun run product:stage` 复制预构建 `.output`、运行资产、脚本、SQLite migrations、`assets/workspace` 与 TSX Profile Workbench 编译所需 runtime source 子集，生成 Product Root `.env`，并在 Product Root 内重新编译系统 profiles，使 `.compiled` artifact 绑定产品内 vendor。Product Root 启动时 `process.cwd()` 必须指向 Product Root，产品根不要求 `node_modules`，依赖由 `.output/server/node_modules` 作为 Nitro 内置 vendor 承载；Windows Launcher 以 `app/` 为服务 cwd，通过 `app/workspace` 目录联接把真实用户数据落到 `data/workspace`，并用内置 Bun 运行管理员创建和用户检测脚本。Profile Workbench 后端 worker 通过 `.output/server/index.mjs` 创建 runtime require 解析 `tsx/esm/api` 与 `tsx` loader，不从 Product Root 裸 import 或回退仓库根依赖；profile artifact compiler v5 会从 runtime 上下文解析并 bundle 普通第三方包，Product artifact 的 require shim 会优先使用当前 Product Root cwd 的 `.output/server/index.mjs`，再从当前 `.compiled` 文件向上定位 Product Root 入口，不再写入构建机绝对 file URL，用户 profile 在 product 内编译后运行时也不依赖根 `node_modules`。`product:stage` 与 `package:windows-portable` 会断言系统 profile artifact 不包含 CI/本机绝对 Product require；单文件 profile 编译也通过 staging 目录提交，避免真实 `.compiled` manifest 引用 `*.building.mjs` 临时文件。产品脚本统一从 `.output/server/scripts/**` 运行，`product:start` 自动加载 Product Root `.env` 并用 Bun 启动 Nitro 入口；`assets/workspace/.nbook/agent/scripts/workspace.ts` 和 `agent/bin/workspace/profile/variable(.cmd)` 在 product 内作为 launcher 指向 `.output/server/scripts/**`，并使用内置/系统 Bun 直接运行；`nbook/*` 通过 `.output/server/node_modules/nbook` 产品内 runtime source 包解析，不依赖开发机源码或根 `node_modules`；Product / Windows Portable / GHCR 的版本真相源统一为 `package.json.version`，版本 API 从 Product Root `package.json` 或 `.output/server/package.json` 读取并展示 `v${version}`，Windows bridge zip 暂保留 deprecated `app/release-meta.json` 只用于旧 Launcher 更新校验。`ghcr` / Product Docker 保留为服务器预构建部署，Product Bun 面向已有 Bun 的本机或服务器；`local-git` 和 `source` 降级为源码/过渡路径。Project Workspace 根目录 `project.yaml` 是项目唯一真相源，保存 `kind`、`title`、`summary`；App SQLite 不记录 Project index、Project 状态或 `Novel`/`novelId` mapping。模型 Provider、API Key、Agent 默认 profile、UI/editor 偏好已迁入 Global Config `workspace/.nbook/config.json`，Project 覆盖写入 `workspace/{project}/.nbook/config.json`。这些运行时文件都不进 Git，真实文件不再作为仓库文件维护。

`execute_sql` 已收敛为 Project SQLite-only 工具：不接受 SQLite 文件路径参数，不访问 App SQLite，只能在当前 Project Workspace 上下文中操作 `.nbook/project.sqlite`，继续允许受限 `SELECT` / `WITH` / `INSERT` / `UPDATE` / `DELETE`，并禁止 DDL、多语句、事务控制和危险 PRAGMA。World Engine 第一版当前契约已收敛为 Project SQLite `WorldSubject` / `WorldSlice` / `WorldMutation` 三表、mutation 不存旧值字段，后端不自动改写后续切面；`server/world-engine` 提供 subject 创建、slice 写入 / 编辑 / 删除、reduce 查询、schema/calendar 加载、E/A issues 与核心测试。Agent 内置工具已覆盖 `get_world_state`、`list_world_slices`、`write_world_slice`、`edit_world_slice`、`delete_world_slice`、`create_world_subject`、`get_world_schema`、`list_world_subjects`，系统 profile `world.engine` 要求使用项目日历字符串、返回 issues、区分 E 必须修与 A 需确认语义；HTTP / Agent 公开时间入参会拒绝 `instant:<number>` raw instant 调试格式或首尾空白，HTTP path 非法 percent encoding 返回稳定 400，slice mutation `value` 会在 Agent 工具和 service 最终边界拒绝 `NaN` / `Infinity` / `Date` / 嵌套 `undefined` 等非严格 JSON 值。`/api/projects/world-engine/**` 覆盖 schema、subjects、slices、slice delete、state/query，`POST /slices/:id/delete` 与 `DELETE /slices/:id` 均可删除切面并返回删后 issues；`queryState` / `getWorldState` 返回 `{subjects, issues}`，`listSlices` 会给有问题的 slice 投影读时 E issues。默认 Project Workspace 模板已包含 `world-engine/schema.yaml` 与 `world-engine/calendar.yaml`，新建 Project 可直接使用 World Engine；schema loader、calendar loader、mutation value / attr path / object fields / enum/default/desc / int safe integer / int add safe result / add finite result / finite number / list/collection set 整组替换 / listLimit / listSlices limit safe integer / 配置根结构等校验已由后端和测试覆盖。独立 `/world-engine.preview` 与主 IDE Workbench 都已移除旧后续处理交互，支持真实 API 新建 Project、创建 subject、写入 / 编辑 / 删除 slice、查询 state、展示 action/query/slice issues，并保留 schema-aware Mutation Builder、一键示例世界、timeline 搜索过滤、State Query 摘要、Selected Slice 检查器和独立 Preview 调试入口；主 IDE Workbench 会前置校验内置示例世界所需 schema，schema 不适配时禁用示例入口并把空状态降级到创建 subject / 新建 Slice。主 IDE World Engine Workbench 的主体系统来源已纠偏为真实 `simulation/subjects` discovery：复用 `/api/projects/rag/overview` 读取主体目录、`subject.md` frontmatter、六文件存在性、events / memory 计数和 RAG source 状态，左栏能显示尚未注册到 World Engine 的主体为“待接入”，并提供 `同步主体系统` 显式动作把 pending 主体注册成 World Engine subject 身份；同步动作不复制主体六文件正文，主体系统事实源仍是 `simulation/subjects`。首页 Project 深链接初始化也已修复，会用 `includeProjectPath` 把 URL 指定项目补回项目列表；浏览器已验证 `ming-ding-zhi-shi-2` 能从 `?project=workspace%2Fming-ding-zhi-shi-2` 打开真实 Workbench，并显示 6 个真实主体、排除 `sample-npc`。

World Engine 本轮补充（Preview 写入按钮 time 禁用）：独立 `/world-engine.preview` 的写入 / 编辑 slice 按钮现在绑定 `canWriteSlice`，要求 Project ready、非 busy 且 `sliceForm.time` 非空才可点击；`writeSlice()` 函数入口仍保留 `time 不能为空` 校验，避免事件绕过按钮后直接请求后端。这样 Preview 连续推演时，time 被清空会直接表现为按钮不可用，而不是点下后才报错。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（Preview schema 快捷填充上下文）：独立 `/world-engine.preview` 的 Schema attr 快捷填充现在会优先使用当前 Mutation Builder subject，其次使用 Query 的第一个 subject，再看创建表单 id，最后才回退到同类型第一个已存在 subject。此前在 `ming-ding-zhi-shi-2` 这类多个 `character` 并存的项目里，作者当前选中 / 刚创建 `player` 后点击 `character.hp` 之类快捷项，可能写到同类型第一个角色；现在快捷填充会尊重当前 subject 上下文，减少推演 slice 时的串线。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（Preview subject 重复创建保护）：独立 `/world-engine.preview` 点击已有 subject 后会把该 subject 载入查询 / 写 slice 上下文；现在 Actions 面板会识别当前 `subjectForm.id` 已存在并禁用 `创建 Subject`，同时显示“该 subject 已存在。点击左侧 subject 会载入查询上下文；新建 subject 请填写新的 id。”；`createSubject()` 函数入口也会在同 id 已存在时直接提示 `subject <id> 已存在，请填写新的 id`，不再请求后端。这样同一张表单同时承担“创建草稿”和“当前 subject 上下文”时，不会把作者的查询动作误导成重复创建。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（Preview 创建 subject 保护）：独立 `/world-engine.preview` 的手动创建 subject 入口现在和主 Workbench 一样前置校验 `id/type/time`。Actions 面板会在必填项为空时禁用 `创建 Subject`，`createSubject()` 函数入口也会分别提示 `subject id/type/time 不能为空`，避免把明显缺字段请求打到后端；创建成功后清空 `subjectForm.id/name`、保留 `type/time`，方便连续录入同类型 subject，同时 `queryForm.subjectIds` 与 Mutation Builder 仍聚焦刚创建的 subject，写下一条 slice / 查询状态上下文不丢。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（openPath 消费顺序）：主 IDE 的 `openPath` 深链现在会在 URL 规范化前消费。此前 `normalizeNovelRouteQuery()` 可能先把 URL 规范成只带 `project`，导致 Preview Schema / Calendar chip 带来的 `openPath=world-engine/schema.yaml|world-engine/calendar.yaml` 在文件打开前被清掉；现在首次初始化、workspace route 已匹配和 Project 切换完成后都会先打开目标 Project Workspace 文件，再规范 query。静态契约已锁定 `consumeWorkspaceOpenPathFromRoute()` 早于 `normalizeNovelRouteQuery()` 的关键顺序。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（Preview schema 深链）：独立 `/world-engine.preview` 的 Schema / Calendar 路径 chip 现在可直接打开主 IDE 深链：`/?project=<projectPath>&openPath=world-engine/schema.yaml|world-engine/calendar.yaml`。主 IDE 会在首次初始化、Project route 同步完成或同一 Project 下 `openPath` query 变化时消费该深链，打开 Project Workspace 内对应配置文件，并在打开后清理 `openPath` query；Project 切换仍走既有 World Engine 草稿保护与普通 workspace 未保存文件确认。这样作者在 Preview 新建 Project 后，可以直接进入 `world-engine/schema.yaml` / `world-engine/calendar.yaml` 设置 subject schema / calendar，而不需要手动回主 IDE 文件树里找文件。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（Preview 反馈互斥）：独立 `/world-engine.preview` 现在通过 `setPreviewError()` / `setPreviewNotice()` 统一更新局部错误和成功提示；Project、示例世界、subject、slice 写入 / 编辑 / 删除、state query 与 Mutation Builder 校验入口都已接入，非空错误会清掉旧成功提示，非空成功 / 状态提示会清掉旧错误，避免连续操作后作者同时看到旧绿条和新红条。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（Preview slice time 必填）：独立 `/world-engine.preview` 的写入 / 编辑 slice 入口现在和主 Workbench Slice Composer 一样前置校验 `time` 必填；`sliceForm.time` 为空时直接显示 `time 不能为空` 并清掉旧成功提示，不再把空时间请求打到后端后才展示底层错误。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（Composer 编辑保存清理草稿）：主 IDE Workbench 的 Slice Composer 整块编辑并保存已有 slice 成功后，现在会清理同 slice 的 metadata/value 会话草稿；删除 slice 的完整清理逻辑也复用同一草稿清理函数。这样作者先在 Inspector / 底部审查区留下草稿，再用 Composer 整块保存同一 slice 后，Drafts 队列不会继续指向旧草稿，也不会让旧草稿覆盖刚保存的新结果。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（Review Focus 生命周期）：主 IDE Workbench 的 Review Queue 焦点现在会跟随当前 issue key 集合刷新；如果作者编辑 / 刷新后修掉了当前 issue，`highlightedMutationFocus.issueKey` 不再存在于队列中时会自动清空，底部审查区不会把已解决 issue 继续显示成 `manual-focus`。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（Composer 顶栏新建请求）：主 IDE Workbench 顶栏 `新建 Slice` 在 Slice Composer 已经打开时，现在会向子编辑器发送切回新建模式请求；如果作者正处于编辑旧 slice 且没有未保存草稿，会直接回到新建草稿，如果有草稿则沿用现有确认，避免“想写下一条 slice，实际仍在整块替换旧 slice”。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（value 草稿 mutation 身份）：主 IDE Workbench 底部 Mutation Editor 的 value 草稿现在不再只按 `sliceId:index` 认领，而会记录对应 mutation 的 `subjectId / attr / op / value` 身份；同一 slice 经 Slice Composer 或其它整块保存入口重排、替换、缩短 mutations 后，旧草稿会被清理并回到当前 mutation value，避免作者把旧行草稿误应用到同 index 的新行。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（Subject Creator 连续录入）：主 IDE Workbench 的手动创建 subject 入口现在会在创建成功后清空 `id/name`、保留 `type/time`，方便作者连续录入多个 subject 而不是停在刚创建的 id 上；创建请求飞行中整组表单会禁用，并用提交前的请求体快照回传父层 subject 信息，避免慢请求期间表单变化污染真实创建结果。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（Subject Creator Project 默认值）：主 IDE Workbench 的手动创建 subject 表单现在会在 Project 切换后重置为 `world / 世界 / 当前 schema 默认时间`；schema 刷新时只会替换空 time 或仍是旧默认值的 time，不覆盖作者手动改过的初始化时间，避免跨 Project 沿用旧 id/name/time。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（空状态创建 Subject 入口）：主 IDE Workbench 在没有 slice 且没有待接入主体的 Project 中，主画布空状态现在会同时显示 `创建 Subject` 与 `一键示例世界`；点击 `创建 Subject` 会展开左侧创建面板并取消侧栏折叠，让“可以先创建 subject”的文案有直接入口，不再要求作者去找折叠 details。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（编辑 Slice Builder 对齐）：主 IDE Workbench 的 Slice Composer 通过 `编辑 Slice` 或 `载入所选 Slice` 进入编辑模式时，会在 textarea 载入当前 slice mutations 后，把第一条 mutation 静默同步进 Builder；mutation 选择器和 Builder 默认不再停在旧新建草稿，降低误点“替换所选”覆盖当前 slice 的风险。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（Slice time 必填校验）：主 IDE Workbench 的 Slice Composer 现在用 `sliceValidation` 先校验 `time` 必填；time 为空时会在表单内显示 `time 不能为空` 并禁用写入 / 写入并继续按钮，不再把空 time 请求发到后端后才展示错误。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（连续写入可见回执）：主 IDE Workbench 的 Slice Composer 在 `写入并继续下一步` 成功后，会在 Composer overlay 内显示上一条 slice 已写入的回执，并在有 issues 时显示 issue 数量；载入已有 slice 或切回新建模式会清空该回执，避免父层 notice 被 overlay 遮住时作者无法确认上一条是否保存成功。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（删除 issue 来源归因）：主 IDE Workbench 删除 slice 后，会用被删除的 slice 作为删除返回 transient issues 的来源；`recordTransientIssues()` 只在 slice id 对得上时才借用当前选中 slice 的 time/title，避免刷新 timeline 后把删后 issue 错挂到新的当前 slice。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（无选中 slice 的 issue 摘要）：主 IDE Workbench 在无选中 slice 的底部空状态中，会展示当前 Review Queue 的前 3 条 issue 摘要，包括 A/E、code、来源时间或 slice id、subject/attr 与 triage 状态；删除 slice 后即使底部审查工作台因没有 selected slice 而卸载，作者也能看到删后 issue 的具体内容并点击复用现有定位逻辑。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（下一时间跨日进位）：主 IDE Workbench / Preview 的下一条默认 slice 时间现在支持默认数字历的日 / 月 / 年边界进位；最新 slice 在 `23:59:59` 时会推到次日 `00:00:00`，30 日月末会推到下月 1 日，12 月 30 日会推到下一年 1 月 1 日，不再回退到 calendar examples。`app/utils/world-engine-preview.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（Schema 来源路径）：主 IDE Workbench 左栏和独立 Preview 的 Schema 区域现在都会展示 `world-engine/schema.yaml` 与 `world-engine/calendar.yaml` 两个 Project Workspace 内配置文件路径，帮助作者从新 Project 进入“设置 subject schema / calendar”的文件化流程；本轮不实现完整 schema 编辑器，也不改变后端 schema API。`app/utils/world-engine-ide-entry.test.ts`、`app/utils/world-engine-workbench-preview.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（Schema 来源文件打开）：主 IDE Workbench 左栏的 `world-engine/schema.yaml` / `world-engine/calendar.yaml` 路径 chip 现在可以直接打开对应 Project Workspace 文件；点击时会复用 `index.vue` 现有 `openWelcomeWorkspacePath()` 文件树入口，保存中的 Slice Composer 会阻止打开，存在 Workbench 会话草稿时会先确认关闭并放弃草稿。`app/utils/world-engine-ide-entry.test.ts`、`app/utils/world-engine-workbench-preview.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（示例世界 schema 入口保护）：主 IDE Workbench 现在会在展示 / 执行内置示例世界前复用 `validatePreviewDemoSchema()` 校验当前 schema；schema 不适配时顶栏和空状态示例按钮会禁用并用 title 暴露原因，空 Project 则按是否已有 World Engine subject 降级到 `创建 Subject` 或 `新建 Slice`，避免 `ming-ding-zhi-shi-2` 这类自定义 schema 点进必然报错的示例入口。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（连续推演 subject 语境）：主 IDE Workbench 的 `写入并继续下一步` 现在会保留作者当前 selected subject 语境；当角色 schema 没有 `events`、默认 mutation 回退到 `world.events` 时，下一步草稿仍按原角色上下文生成，父 Workbench 也用 `contextSubjectId` 保持焦点，不再因为上一条实际写入 subject 是 `world` 就把连续推演带到 `world`。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（State Snapshot 前态时间）：主 IDE Workbench 的 State Snapshot 现在会在选中 slice 缺少 `previousTime` 或 mutation 详情时按需读取 `/api/projects/world-engine/slices/:sliceId`，把 detail 合并回 timeline，并用 detail 返回的真实全局 `previousTime` 查询 before 状态；不再把 subject-filtered / Review Queue 懒加载 / 最近窗口列表里的前一项当作前态，减少多 subject slice before / after 对比偏早的问题。detail 回填当前列表已有 slice 时会原位替换，不再因为全局 `previousTime` 不在当前过滤列表里把 selected slice 移到末尾。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充：`list` / `collection` 现在允许 `set` 作为整组替换，value 必须是 array，数组项继续按 `itemType` / `type` 校验；`createSubject` 生成的 init slice 可通过 `editSlice` 原样保存，主 Workbench 载入初始化切片后不再因为 `events set []` / `inventory set []` 被后端拒绝。主 Workbench 创建 subject 或同步主体系统后，会优先定位到触及新 subject 的最近 slice，让左侧 subject 选择、中间 timeline 和右侧 snapshot 更一致；删除当前 slice 后则保持无选中 slice，新三栏 SliceList 不再把空选中自动跳到第一条可见 slice；Slice Composer 只会默认写向已注册的 World Engine subject，当前焦点是待接入主体时会提示先同步主体系统；顶栏“编辑 Slice”可在选中 timeline slice 后直接打开 Composer 并载入该 slice，“删除 Slice”可直接走二次确认后的真实删除。独立 Preview 手动创建 subject 后也会自动刷新 State Query 和下一写入时间；点击 subject 会清掉旧 type 查询条件并立即刷新 State Query；默认 slice mutation 复用 `defaultMutationForPreviewSubject()`，不再硬编码 `events/listAppend`；打开已有 Project 时如果默认 `world` subject 不存在，会回落到第一个真实 subject，并只刷新仍是系统自动生成的 mutation 草稿；当前 subject 没有 `events`、但项目存在 `world.events` 时，默认手写 slice 会回退到 `world.events listAppend`，避免 `ming-ding-zhi-shi-2` 选中真实角色后第一屏默认写 `hp set 100`；Slice Composer 的 Builder 表单也会同步到默认 mutation 的实际 subject / attr / op / value，避免 textarea 已是 `world.events`、Builder 仍停在 `player` 后误触“替换所选”；Slice Composer 保存后若本次提交 mutations 不命中当前 subject 过滤，会自动回到整体世界视角，避免刚写入的 slice 被旧 `player` 过滤隐藏并触发时间线跳走；下一条 slice 默认时间优先从已加载 timeline 的最新时间推导，`ming-ding-zhi-shi-2` 当前会从 `复兴纪元488年 1月15日 14:00:05` 推到 `14:00:06`，不再回到 `复兴纪元1年`，且同一天内小时边界会从 `14:59:59` 推到 `15:00:00`。前期对应 HTTP/API、前端窄测试与 `bun run typecheck` 已通过；本轮小时边界补充跑通 `app/utils/world-engine-preview.test.ts`，未自动执行浏览器验证。

World Engine 本轮补充（State Snapshot issues）：主 IDE Workbench 的 State Snapshot 现在会接收并展示 `state/query` 与完整 `GET /state` 返回的 issues；选中 slice 查看触及主体状态或展开完整世界状态时，读时 E issue 不再只停留在 API 返回值里。State issue 行也已把 code 列加宽并保留 title，避免 `broken-relative` 等长 code 挤压溢出。对应静态契约已补到 `app/utils/world-engine-ide-entry.test.ts`，本轮跑通该窄测试与 `bun run typecheck`，未自动执行浏览器验证。

World Engine 本轮补充（snapshot issues 接线清理）：主 Workbench 已删除未展示的 `previousSnapshotIssues` 死状态，避免代码表达出“前态 issues 已进入审查工作台”的假心智；底部审查工作台继续使用 `previousSnapshotSubjects` 做 before / after，前态 issues 若要展示需后续明确设计。源码与测试入口已确认无 `previousSnapshotIssues` 残留，`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 通过。

World Engine 本轮补充（Review issue 定位）：主 Workbench 的 Review Queue issue 点击定位现在会先确认目标 slice 是否在当前已加载 timeline 中；目标存在时会清理 search / kind / status / subject 阻挡过滤、切到 issue subject 并展开审查工作台，目标不存在时提示当前 timeline 无法定位，不再把 `selectedSliceId` 指向不存在的 slice。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 通过，未自动执行浏览器验证。

World Engine 本轮补充（Review Focus 精确 issue）：`highlightedMutationFocus` 已增加可选 `issueKey`，Review Queue 点击 issue 后会按具体 key 计算当前队列索引与底部审查工作台的 code/message/status；同一 slice / subject / attr 下多条 issue 时不再退化为第一条同属性 issue。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 通过，未自动执行浏览器验证。

World Engine 本轮补充（Review Focus watcher 保护）：Review Queue 点击 issue 时会自动切到对应 subject 过滤，但这会触发 `selectedSubjectIds` watcher；现在该自动切换使用一次性保护，保留刚设置的 issue focus。普通手动 subject 过滤仍会清空旧 issue focus。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 通过，未自动执行浏览器验证。

World Engine 本轮补充（未加载 issue focus）：Review Queue 点击未加载到当前 timeline 的 issue 时，现在只提示无法定位并清空 `highlightedMutationFocus`，不再在当前 slice 上显示 `manual-focus` 审查上下文，避免作者误以为当前 slice 是目标。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 通过，未自动执行浏览器验证。

World Engine 本轮补充（subject filter 清空）：`selectedSubjectIds` watcher 现在在 subject 过滤清空时也会清空 `highlightedMutationFocus`，避免作者回到整体世界视角后仍看到旧 issue 高亮；Review Queue 自动切换 subject 过滤的一次性 focus 保护不变。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 通过，未自动执行浏览器验证。

World Engine 本轮补充（subject timeline 服务端过滤）：`GET /api/projects/world-engine/slices` 已支持 `subjectIds=...` 与 `subjectMode=any|all`，service / repository 会按 slice mutations 在查询层过滤，避免主 Workbench 长时间线下只对最近 200 条做前端 subject 过滤而漏掉较早切片。真实 `WorldEngineWorkbenchDialog` 左栏 subject 选择、切片卡片“只看 subject timeline”、移除 / 清空 subject 过滤、切换任一/全部 subject 时会做 timeline-only reload；schema、subjects 和 RAG overview 仍只在完整刷新时读取。`server/api/projects/world-engine/[...segments].test.ts`、`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 通过，未自动执行浏览器验证。

World Engine 本轮补充（issue slice 懒加载定位）：新增 `GET /api/projects/world-engine/slices/:sliceId`，可按 id 读取单个 slice 的 metadata、mutations 和读时 issues。真实 `WorldEngineWorkbenchDialog` 的 Review Queue 点击当前 timeline 未加载的 issue slice 时，会先调用该接口把目标切面放入当前 timeline，再清理 search / kind / status / subject 阻挡过滤并定位到具体 issue；只有 sliceId 为空或读取失败时才提示无法定位。`server/api/projects/world-engine/[...segments].test.ts`、`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 通过，未自动执行浏览器验证。

World Engine 本轮补充（slice detail previousTime）：`GET /api/projects/world-engine/slices/:sliceId` 现在会额外返回 `previousTime`，由后端按真实 instant 查询前一切面后格式化为项目日历字符串。主 Workbench 的 State Snapshot 前态查询会优先使用 `slice.previousTime`，避免 Review Queue 懒加载较早切面后，因为该 slice 被追加到当前数组末尾而拿错 before / after 上下文。`server/api/projects/world-engine/[...segments].test.ts`、`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 通过，未自动执行浏览器验证。

World Engine 本轮补充（懒加载 slice timeline 合并）：主 Workbench 新增 `mergeWorldWorkbenchTimelineSlice()`，Review Queue 懒加载 issue slice 后会优先按 `previousTime` 插入到当前已加载前序切片之后；没有 `previousTime` 时放到开头，找不到前序时才保守追加。这样作者从 Review Queue 定位 issue 后，列表视觉顺序更接近真实时间线。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 通过，未自动执行浏览器验证。

World Engine 本轮补充（Review issue subject timeline）：主 Workbench 的 Review Queue 点击 issue 后，现在不仅会懒加载目标 slice，还会按 issue subject 重新读取服务端 filtered timeline；若目标 slice 超出最近窗口，则把已懒加载的目标切片再合并回列表，避免左栏已经切到 subject、主列表仍是旧数据窗口的假视角。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 通过，未自动执行浏览器验证。

World Engine 本轮补充（待接入 subject timeline 提示）：主 Workbench 左栏选择尚未注册到 World Engine 的 `simulation/subjects` 主体时，现在会显示顶部 notice，说明该主体暂无 World Engine 时间线，并提示先使用“同步主体系统”或选择已注册 subject。这样作者不会把空 timeline 误读成角色没有历史切片。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 通过，未自动执行浏览器验证。

World Engine 本轮补充（待接入 subject 清空旧 slice）：当主 Workbench 左栏只选中待接入 subject 时，timeline 空状态会同步清空当前 `selectedSliceId`，避免 Slice List 已为空、右侧 Inspector 仍展示上一条旧切片。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 通过，未自动执行浏览器验证。

World Engine 本轮补充（待接入 subject 焦点保持）：待接入-only subject 过滤后，主 Workbench 会把 `focusedSubjectId` 恢复到作者刚选的待接入 subject，并清空旧 issue focus，避免 `applyDefaults()` 根据旧 slice 改写焦点后让 Slice Composer 默认目标串线。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 通过，未自动执行浏览器验证。

World Engine 本轮补充（主体同步初始化时间）：主 Workbench 的 `主体系统待接入` 面板现在会显示本次同步使用的初始化时间；同步逻辑仍使用 schema calendar 的第一个示例时间创建 World Engine subject 身份，不改变后端 `createSubject` 的 init 追加语义。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 通过，未自动执行浏览器验证。

World Engine 本轮补充（主体同步冲突文案）：当前同步 subject 的初始化时间若已有非 init slice，前端冲突文案会把后端 `editSlice` 建议转成 Workbench 可执行动作：在 Timeline 载入该时间的 slice 显式合并，或修改初始化时间。`app/utils/world-engine-preview.test.ts`、`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 通过，未自动执行浏览器验证。

World Engine 本轮补充（主体同步部分成功刷新）：主 Workbench 批量同步 `simulation/subjects` 时，如果前面已有 subject 成功接入但后续请求失败，现在会刷新已成功接入的 subject、聚焦它们，并显示“已接入 N 个主体系统 subject，但后续同步失败”的错误提示，避免成功项继续留在待接入列表里。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 通过，未自动执行浏览器验证。

World Engine 本轮补充（主体同步时间可编辑）：主 Workbench 的 `主体系统待接入` 面板现在提供初始化时间输入框，默认仍使用 schema calendar 的第一个示例时间；作者遇到非 init slice 冲突后，可直接把同步初始化时间改到相邻时间再重试。项目切换或关闭 Dialog 时会清空覆盖值。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 通过，未自动执行浏览器验证。

World Engine 本轮补充（preferred subject 空时间线）：主 Workbench 创建或同步 subject 后，如果目标 subject 没有 schema default、因此没有生成任何初始化切片，`applyDefaults()` 现在会保持该 subject 的空时间线视角并清空选中 slice，不再回落到旧 slice 并改写 `focusedSubjectId`。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 通过，未自动执行浏览器验证。

World Engine 本轮补充（无选中 slice 空状态）：主 Workbench 无选中 slice 的空状态现在按当前视角显示，不再把 subject 空时间线误写成 Project 没有 slice；Project 真空、subject 时间线空、待接入 subject 和已有 slice 但未选择会得到不同文案，并提供新建 Slice、清空 subject 过滤或一键示例世界等对应动作。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（mixed subject 新建目标）：主 Workbench 在 mixed pending / registered subject 过滤下打开 Slice Composer 时，会在 focused subject 未接入但当前过滤仍包含已注册 subject 的情况下，优先使用最后一个已注册 subject 作为新建目标；没有已注册 subject 时仍保留“先同步主体系统”的提示路径。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（待接入 subject 全部过滤）：主 Workbench 现在会阻止包含待接入 subject 的过滤进入“全部 subject”模式；选择 subject 时会自动切回“任一 subject”，用户手动切换时会提示待接入主体还没有 World Engine 切片，避免不可满足过滤把 timeline 显示成假空。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（Slice Composer 写入并继续）：主 Workbench 的 Slice Composer 在新建 slice 模式下新增 `写入并继续下一步`。成功写入后仍会刷新真实 timeline、定位新 slice、记录 action issues，但不会关闭 Composer；子编辑器会立即切到下一条新 slice 草稿，并把刚保存的时间临时并入 `usedTimes` 避让，父层刷新完成后不再重挂 Composer，避免抹掉作者已经开始输入的下一步内容。编辑已有 slice 时不显示该按钮，避免“保存编辑”和“继续新建”混淆。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（Slice Composer 继续模式 subject 焦点）：`写入并继续下一步` 保存多 subject slice 时，父层会把 `focusedSubjectId` 对齐到本次保存的后续推演上下文；普通多 subject slice 使用最后一个 mutation subject，后续已补充角色默认事件回退到 `world.events` 时保留原 selected subject，避免下一条 clean 草稿被旧 focused subject、slice 首个 subject 或 fallback 的 `world` 语境改写默认目标。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（Workbench 关闭草稿保护）：关闭整个主 Workbench 时现在会汇总会被丢弃的会话态草稿，包括 Slice Composer 草稿、Inspector metadata 草稿和 mutation value 草稿，并用“当前 Workbench 有未保存内容：...。确定关闭并放弃吗？”统一确认，避免作者改了 Inspector 或底部 value 后误关静默丢失。只关闭 Slice Composer 浮层时仍只检查 Composer 自己的草稿，不会阻止保留在主 Workbench 的其它草稿。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（Workbench v-model 关闭入口）：主 Workbench Dialog 的 `@update:model-value` 不再直接透传父层，而是走 `handleWorkbenchModelUpdate()`；当值为 `false` 时会统一进入 `requestWorkbenchClose()`，复用 Slice Composer / metadata / value 会话草稿确认，避免直接 v-model 关闭路径绕过草稿保护。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（Project 切换草稿保护）：主 Workbench 现在会通过 `hasUnsavedDraftsChange` 向 `app/pages/index.vue` 上报 Slice Composer / metadata / value 会话草稿状态；主 IDE 顶部切换 Project 前，如果 Workbench 打开且存在 World Engine 草稿，会先询问“放弃草稿并切换 / 取消”。取消后不切换 Project，确认后先关闭 Workbench 再进入原有未保存文件检查和 Project 切换，避免 `projectPath` watcher 直接清空草稿。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（Bookshelf 切换草稿保护）：`NovelBookshelfDialog` 新增 `beforeWorkspaceSwitch` 宿主回调；主 IDE 把 World Engine 草稿确认函数传给 Bookshelf，创建 / 切换 Project、删除当前 Project 前都会先确认是否放弃 Workbench 会话草稿，避免 Bookshelf 内部入口绕过顶部 Project 切换保护。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（Drafts 跨时间线找回）：主 Workbench 顶栏 `Drafts` 入口现在从 metadata / value 草稿 summary 汇总所有草稿 slice id，不再只依赖当前 `slices`；点击入口清空阻挡过滤后，如果目标草稿 slice 不在当前 timeline，会复用 `GET /slices/:sliceId` 懒加载回来并打开对应处理面板，避免 subject 过滤或最近 200 条窗口把会话草稿挡空。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（保存编辑保持当前 slice 可见）：主 Workbench 保存 Inspector metadata 或底部 mutation value 后，会用保存后的 slice 形态检查当前 subject / kind / search / status 过滤；如果过滤会挡住刚保存的 slice，则清掉对应过滤，避免 `SliceList` watcher 自动跳到第一条可见 slice，让作者误以为保存后上下文串线。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（Review triage 稳定 key）：真实 Workbench 的 Review Queue 会话态确认 / 忽略状态不再依赖 issue 数组下标；持久 issue key 现在基于 `sliceId + subjectId + attr + code + message + occurrence`，同一 slice 的 issue 顺序变化时，作者已确认或忽略的问题不会掉回 open。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（Review triage identity 继承）：真实 Workbench 的 Review Queue 会同时按 key 和 `sliceId + subjectId + attr + code + message` 业务 identity 查找会话态确认 / 忽略状态；写入 / 编辑 / 删除返回的 transient issue 后续变成 persisted slice issue 时，会继承用户已处理状态，不会因为 issue 来源切换掉回 open。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（metadata 保存失败保留草稿）：右侧 Inspector 提交 metadata 保存时不再在真实 API 成功前删除本地 draft；如果 `editSlice` 因时间冲突或其它错误失败，作者输入仍留在 Inspector / Drafts 中，只有外部 slice 成功同步到草稿值后才自动清理。`app/utils/world-engine-ide-entry.test.ts`、`app/utils/world-engine-workbench-preview.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（value 保存 busy 保护）：主 Workbench 会把真实 API 的 `loading || actionBusy` 传给底部 Mutation Editor；保存 mutation value 期间，单条 / 批量 apply、单条 / 批量 reset 和清空全部 value 草稿入口都会禁用，用户入口函数也会直接返回，避免慢请求下重复提交 `editSlice` 或在外部 slice 回流刷新前误清草稿；`resetKey` 触发的系统级重置仍可清空草稿。`app/utils/world-engine-ide-entry.test.ts`、`app/utils/world-engine-workbench-preview.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（metadata 保存 busy 保护）：主 Workbench 会把真实 API 的 `loading || actionBusy` 传给右侧 Inspector；保存 metadata 期间，`time / kind / title / summary` 字段和保存 / 还原按钮都会禁用，`applyPatch()` / `resetDraft()` 用户入口也会直接返回，避免慢请求下重复提交 `editSlice` 或保存中继续输入被成功回流覆盖。`FormInput` / `FormSelect` / `FormTextarea` 已补 `disabled` prop 支撑该路径。`app/utils/world-engine-ide-entry.test.ts`、`app/utils/world-engine-workbench-preview.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（删除 slice 清理会话草稿）：主 Workbench 删除当前 slice 成功后会调用 `clearSessionStateForDeletedSlice()`，清理该 slice 的 metadata/value draft summary、transient issues、review focus 与 snapshot/full snapshot 缓存，避免子组件卸载后父层 `Drafts` 入口继续指向已删除记录。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（删除 slice 保留其它草稿）：主 Workbench 删除当前 slice 时会通过 `draftDiscardSliceId / draftDiscardVersion` 通知 Inspector 与 Mutation Editor 精确丢弃被删 slice 的内部草稿；如果仍有其它草稿 slice，则先进入 `draft` 视角并选中第一个剩余草稿 slice，再刷新 timeline，避免 `selectedSlice` 短暂无值导致子组件卸载并丢失其它 slice 的草稿内容。`app/utils/world-engine-ide-entry.test.ts`、`app/utils/world-engine-workbench-preview.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（Workbench 顶部提示互斥）：主 Workbench 新增 `setWorkbenchError()` / `setWorkbenchNotice()`，保存、删除、同步、Review Queue 定位、subject timeline 提示、Slice Composer 保存以及子组件 `@error` / `@notice` 回调都会通过 helper 显示顶部 banner；非空错误会清掉旧成功提示，非空成功 / 提示会清掉旧错误，避免连续操作后作者同时看到旧绿条和新红条。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（route 切换草稿保护）：主 IDE 的 `syncWorkspaceRoute()` 现在也会在 route/query 触发 Project Workspace 切换前调用 `confirmWorldEngineWorkbenchDraftDiscardForProjectSwitch()`；顶部 Project 切换、Bookshelf 切换和深链/地址栏类 route 切换都先确认 World Engine Workbench 会话草稿，避免子组件随 `projectPath` 变化重置后无声丢稿。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（Project 切换确认无副作用）：`confirmWorldEngineWorkbenchDraftDiscardForProjectSwitch()` 现在只返回用户是否同意放弃 World Engine 草稿，不再在确认阶段关闭 Workbench 或清掉 `worldEngineWorkbenchHasUnsavedDrafts`；如果后续普通 workspace 未保存文件对话取消切换，World Engine 会话草稿仍保留。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（route 取消 URL 回滚）：主 IDE 新增 `restoreCurrentWorkspaceRoute()`；当 route/query 触发 Project Workspace 切换但被 World Engine 草稿确认或普通 workspace 未保存文件对话取消时，URL 会恢复到当前实际 workspace（普通 Project 恢复为当前 `currentNovelId`，user-assets 恢复为 `workspace/.nbook`），避免地址栏和真实上下文分叉。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（Composer 上下文切换 busy 保护）：`WorldEngineMutationEditor` 的 `submitSlice()`、`loadSelectedSlice()`、`discardDraftAndLoadSelectedSlice()`、`clearEditMode()` 现在都会在 `props.busy || saving` 时直接返回；Header 的 `载入所选 Slice`、`新建模式`、`放弃草稿并载入` 也会在父层刷新 busy 期间禁用。这样 `写入并继续下一步` 后作者仍可继续输入下一步草稿，但不能在 timeline 刷新未完成时切换编辑上下文。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（Composer 保存中关闭保护）：`WorldEngineMutationEditor` 新增 `savingChange` 事件，保存请求开始 / 结束会向父层同步 `sliceComposerSaving`；主 Workbench 顶部同步状态会纳入该保存状态，并在保存请求飞行中阻止关闭 Slice Composer 或整个 Workbench，避免写入结果在后台完成而作者失去反馈上下文。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（保存中 Project 切换保护）：`WorldEngineWorkbenchDialog` 会把 `sliceComposerSaving` 通过 `savingChange` 同步给主页面；`confirmWorldEngineWorkbenchDraftDiscardForProjectSwitch()` 在 Workbench 正在保存 Slice 时会提示等待保存完成并阻止顶部 Project 切换、Bookshelf 切换和 route/query 切换，不再进入“放弃草稿并切换”分支。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（保存中顶栏动作保护）：主 Workbench 新增 `workbenchActionBusy = loading || actionBusy || sliceComposerSaving`，顶栏刷新 / 新建 / 编辑 / 删除 / 示例世界、Drafts 草稿入口、主体系统同步和空状态动作在 Slice Composer 保存请求飞行中会禁用；Slice Composer、Subject Creator、底部 Mutation Editor、Inspector 的 `busy` 也改为接收该状态，避免保存中并发刷新或写世界。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（保存中函数层保护）：主 Workbench 新增 `blockSliceComposerSaving()`，保存请求飞行中会在用户入口函数层阻止示例世界、主体系统同步、删除 slice、选择 slice、Review Queue 定位、subject timeline/filter 切换、打开新建 / 编辑 Composer 和 Drafts 定位等动作，避免组件事件或后续重构绕过按钮 disabled 后改变当前保存上下文。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（Slice List 保存中上下文保护）：中间 Slice List 新增 `busy` 契约，真实 Workbench 传入 `workbenchActionBusy`；保存请求飞行中 search / kind / status / Draft Queue / 可见切片 stepper / 维护切片开关会禁用，过滤更新改走父层 wrapper 并由 `blockSliceComposerSaving()` 拦截，`filteredSlices` watcher 也不会自动改选 slice，避免保存回流前作者上下文被列表过滤带跑。`app/utils/world-engine-ide-entry.test.ts`、`app/utils/world-engine-workbench-preview.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（Composer 保存中表单保护）：Slice Composer 保存请求飞行中会禁用 `time/title/kind/summary/mutations` 表单字段和 Mutation Builder；Builder 的字段更新、object 行编辑、mutation 载入 / 追加 / 替换 / 删除 / 移动等父层事件函数也会在 `saving` 时直接返回，避免慢请求下作者继续输入旧草稿后被成功回流覆盖或误标记为 clean。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（空状态同步主体系统）：主 Workbench 中间空状态新增 `sync-subject-system` 动作；当 Project 没有 World Engine slice 或当前选择的 subject 全部是待接入主体，且存在 `simulation/subjects` 待接入 summary 时，中间主画布会直接显示 `同步主体系统` 按钮，复用左栏 `syncPendingSubjectSystemSubjects()` 与初始化时间禁用条件，减少首次打开 Project 时寻找入口的成本。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（Slice Composer 新建模式 dirty guard）：主 Workbench 的 Slice Composer 在编辑已有 slice 后，如果内部点击 `新建模式` 且存在未保存 title / summary / kind / time / mutations 草稿，会先确认“当前编辑器有未保存草稿，确定切换到新建模式吗？”。取消会保留当前编辑草稿，确认后才回到新建 slice 模式，避免误点清空本轮推演。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（Slice Composer 打开态保留 dirty）：主 Workbench 顶部入口只会在 Composer 原本关闭时重置 `sliceComposerDirty`；Composer 已经打开后，`新建 Slice` 会请求子编辑器切回新建模式并沿用内部未保存草稿确认，`编辑 Slice` 则仍需子编辑器确认后才会载入所选 slice，不会由父层直接误清 dirty guard。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（Slice Composer 关闭 dirty guard）：主 Workbench 的 `WorldEngineMutationEditor` 现在会把内部 `hasDirtyDraft` 通过 `dirtyChange` 事件同步给 Dialog；Slice Composer 右上角关闭按钮在存在未保存 title / summary / kind / time / mutations 草稿时会先确认“当前 Slice Composer 有未保存草稿，确定关闭吗？”，取消则保留浮层和草稿。Workbench 自身关闭请求也接入 `requestWorkbenchClose()`；关闭整个 Workbench 或按 Esc 时会走当前统一草稿确认，汇总 Slice Composer、metadata 和 value 会话草稿。保存成功、关闭成功、重新打开和切换 Project / 关闭 Dialog 会清理 dirty 状态。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（当前视角文案）：真实 Workbench 与 mock preview 顶部当前视角的 subject filter 模式已从内部 `any/all` 改为“任一 subject / 全部 subject”，与 Slice List 模式控件保持一致。`app/utils/world-engine-ide-entry.test.ts`、`app/utils/world-engine-workbench-preview.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（subject timeline 刷新保持服务端过滤）：主 Workbench 新增 `refreshWorldForCurrentTimeline()`，常用刷新路径会先刷新 schema / subjects / RAG overview，再在当前有已注册 subject 过滤时继续调用服务端 subject timeline。顶栏刷新、保存编辑、创建 / 同步 subject、删除 slice、Slice Composer 保存不再把单 subject / 多 subject 视角退回“最近 200 条全量结果 + 前端本地过滤”；删除后仍通过 `autoSelectSlice: false` 保持无选中 slice。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（subject mode 清理 review focus）：主 Workbench 手动切换“任一 subject / 全部 subject”时会清空旧 `highlightedMutationFocus`，避免 timeline 刷新或 selected slice 改变后，底部审查工作台仍按旧 Review Queue issue 计算位置或高亮。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（清空 subject 过滤复位模式）：真实 Workbench 清空 subject 过滤、进入 Drafts 草稿视角时会把隐藏的 `subjectFilterMode` 复位为 `any`；mock preview 的 `clearSubjectFilter()` 同步复位，避免整体世界视角下看不见的旧 `all` 模式在下一次多 subject 选择时突然生效。`app/utils/world-engine-ide-entry.test.ts`、`app/utils/world-engine-workbench-preview.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（空 subject 过滤 mode guard）：真实 Workbench 在 `applyDefaults()` 刷新后若 selected subjects 被过滤为空，会同步把 `subjectFilterMode` 复位为 `any`；mock preview 移除最后一个 subject chip、恢复浏览器草稿但 subject ids 已全部失效时也会复位，避免隐藏 `all` 模式在下一次多 subject 选择时突然生效。`app/utils/world-engine-ide-entry.test.ts`、`app/utils/world-engine-workbench-preview.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（Composer 已知时间窗口）：主 Workbench 新增 `knownSliceTimes` 会话态缓存，完整 `loadWorld()` 后重置为当前整体 timeline 窗口，subject 过滤刷新和 Review Queue 懒加载 issue slice 后并入局部时间；Slice Composer 的 `usedTimes` 现在使用 `knownSliceTimes + 当前 slices` 去重并集，减少 subject 过滤视角下默认下一时间撞到其它 subject 已占用 instant 的概率。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 本轮补充（knownSliceTimes 顺序）：`mergeKnownSliceTimes()` 现在只把局部 timeline / 懒加载切片中新发现的时间补到已知窗口前方，保留完整 `loadWorld()` 时间窗口的末尾作为默认下一时间基准，避免较早 subject 切片或旧 issue slice 抢走 Slice Composer 的“推演下一步”时间基准。`app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 已通过，未自动执行浏览器验证。

World Engine 最新补充：round-139 已在 round-113/114 新路线同步、round-115 关键词收口、round-116 Preview action issues 展示语义、round-117 `createSubject` 空切面修复、round-118 稳定设计文档同步、round-119 默认模板链路测试、round-120 Workbench 示例世界观察视角修复、round-121 状态生命周期修复、round-122 create subject issues 前端展示、round-123 API DTO 契约测试、round-124 Preview create subject id 同步、round-125 示例 subject 类型冲突提示、round-126 profile 提示词修正、round-127 工具/文档 default 契约收紧、round-128 Workbench 删除选择修复、round-129 Preview Project 提示重置、round-130 源码注释收口、round-131 Preview 新建 Project 提示顺序修复、round-132 Preview 初始 Project fallback、round-133 Preview 加载去重、round-134 无效 Project path fallback、round-135 旧 helper 清理、round-136 Workbench Edit 初始草稿体验修复、round-137 schema clean 修复和 round-138 schema-aware 初始 mutation 基础上，把初始 mutation 派生规则收敛为 `defaultMutationForPreviewSubject()` util 并补行为测试。`WorldEngineMutationEditor` 创建时会直接用已加载 schema calendar 经 `suggestSliceTime()` 生成默认 time，并从当前选中 subject 或首个 subject 派生初始 mutation；初始 mutation 会优先使用该 subject 类型的 `events` attr；当前 subject 没有 `events`、但项目存在 `world.events` 时回退到 `world.events`；没有 `world.events` 时才使用该类型第一个 attr。Builder 表单会跟随默认 mutation 的实际 subject / attr / op / value，避免切到 Edit tab 时空 time、固定写向 `world`、第一屏 Builder 固定写向不存在的 `events`，或 textarea 与 Builder subject 不一致。如果 Edit tab 先渲染、schema 后到达，schema watcher 自动补默认 time 前会记录草稿是否 clean，补完后仍把系统默认值标记为 clean，避免误触 dirty guard 阻止 subject 同步；`WorldEngineSubjectCreator` 也显式 import `ref`，与本文件其它 Vue API import 风格对齐。`loadProjects()` 现在只通过 `selectPreviewProjectPath()` 从 preferred path、URL query 和当前选择中挑选仍存在于 Project 列表里的路径；空 query 或已经不存在的旧链接不再阻断 fallback，会回退到 Project 列表第一项；已无生产引用的 `firstProjectPath()` 和旧语义测试已删除，避免后续误用“只要非空就可选”的过期规则。如果需要程序化更新 `selectedProjectPath`，会在同步 watcher 抑制窗口内完成赋值并清理会话态，避免同一次 Project 列表刷新触发两次 `loadWorld()`；用户手动下拉切换 Project 仍会清理 state/action/editing/notice/error 并重新读取世界数据。`createProject()` 仍先等待 `loadProjects(project.projectPath)` 完成切换和旧提示清理，再显示“已创建”提示。当前源码注释、`README` default 决策段、`sqlite-and-api` facade 注释和 `create_world_subject` 工具描述都明确：只有非空 schema default 才写入 init slice；没有 default 时只注册 subject 身份，不创建空切面；工具测试也已钉住该描述。独立 Preview 切换 Project 现在会和 state/action/editing 一起清空提示，避免成功/失败消息跨 Project 留在页面顶部。主 IDE Workbench 删除当前 slice 后刷新 timeline 会保持无选中 slice，让 Inspector 不再立刻显示另一条切面；普通加载、写入和一键示例世界仍保留默认选中逻辑。profile 现在明确 `create_world_subject` 只有 schema default 非空时才写入 init slice，空 default 类型只注册 subject 身份，避免 Agent 把 round-117 后端契约解释回“总会创建初始化切面”。现在如果示例 id 已存在但类型不匹配，错误会显示 `erina(需要 character，当前 location)` 这类信息，方便用户在浏览器验收时定位是哪个已有 subject 阻止了示例世界写入。Preview 手动创建 subject 时，请求体仍 trim 输入 id，但成功后会用返回的 `subjectId` 回写表单、State Query、Mutation Builder 和默认 slice mutation，避免输入中含空格时后续查询 / Builder 继续使用原始文本。HTTP API 已断言一键示例世界多 subject 创建返回 `{subjectId, issues}`，确保前端聚合 subject issues 的 DTO 依赖有后端测试证明。Workbench mock preview reducer 已显式处理 `collectionRemove`，避免 mock 预览与正式 reducer 心智漂移。`POST /subjects` 返回 `{subjectId, issues}`；独立 Preview 的手动创建 subject、一键示例世界，以及主 IDE Workbench 的 Subject Creator / 一键示例世界现在都会读取并展示这些 issues，避免只有 slice 写入 / 编辑 / 删除显示 action issues。独立 Preview 切换 Project 时会清空旧 State Query 结果；主 IDE Workbench 在无 Project、加载失败、空世界和切换 Project 时会清空上一 Project 的 selected subject/slice、State Query 与 action issues，避免两个 Project 都有 `world` 时旧状态误留在界面上。Workbench 现在写入示例 slice 后会选中新 slice，并直接查询该 slice 触及的所有 subject，让用户第一眼看到 world/location/character/item 的 reduce 结果；Subject Creator 注释也已对齐“只有 schema default 非空才会写入初始化切面”。默认模板链路测试会从模板创建 Project Workspace、初始化 Project SQLite、读取 `world-engine/schema.yaml` 与 `calendar.yaml`、创建 world/location/character/item、写入示例 slice，并断言 `queryState` 返回 `{subjects, issues: []}`。Preview / Workbench 写入与编辑 slice 后只读取 `{sliceId, issues}`，删除 slice 读取 `{issues}`，State Query 读取 `{subjects, issues}`；一键示例世界也会保留并提示 slice 写入返回的 issue 数，避免示例入口绕过反馈面。Timeline 显示 slice issue badge，Selected Slice 检查器展示 issue 明细并提供 trash 删除入口，State Query 摘要会展示 query issues；独立 Preview 现在把写入 / 示例世界 / 创建 subject / 删除 slice 返回的问题放进“本次操作 issues”，不再用 `stateIssues` 临时承载删除返回值。后端 `createSubject` 在 subject schema 没有 default mutation 时只创建 subject 身份，不再创建无 mutation 的 init slice，保持“切面 = 状态变更”约束；有 default mutation 时仍会创建 init slice 或追加到同 instant 既有 slice；Agent 工具描述也已明确这一点。独立 Preview 删除了旧表单、结果区和 helper，保留 Project 搜索、schema-aware Mutation Builder、一键示例世界、collectionRemove 当前状态候选、object value 护栏和组件拆分结构。后端新增读时 `dangling-ref` E issue：正常写入仍会校验 ref，读时扫描主要覆盖旧数据、手工 DB 损坏或未来删除 subject 场景。`delete_world_slice` 工具描述已收敛为物理删除 cleanup，不暗示可恢复撤销；`worked-example` 回退说明已改为不可恢复 `deleteSlice`，不再描述逐条逆操作。文档已统一为同一 instant 只能有一个切面、修改走 `editSlice`、回退走不可恢复 `deleteSlice`、不存旧值字段、不自动改写后续切面、Agent/API 使用项目日历字符串、E/A issues 是一致性反馈通道。真实浏览器验收仍需用户明确授权后执行。

World Engine Workbench mock preview 最新补充：`/world-engine.workbench-preview` 的 issue 审批已彻底从右侧 Inspector 移出；Inspector 现在只负责 slice metadata、touched subjects、State Snapshot 与 raw JSON。主画布 Slice Card 继续用 compact issue rows 展示 `A/E / code / subject / attr / status` 并负责定位；底部区域用户可见名称已从 Mutation Editor 收束为“审查工作台”，顶部模式为 `问题处理 / Subject 视图 / 总变更`，聚焦 issue 时自动进入问题处理。Review Focus 现在展示同 subject + attr 路径相关链路的前一个 / 当前 / 后一个 mutation，并用 mock snapshots 给出对应 slice 的 before / after；A issue 标注为 write / edit 一次性提醒并说明当前 DTO 没有 source mutation，E issue 标注为 reduce / query / list 持续现算问题。状态 JSON 展示已统一复用公共 `JsonViewer`：Inspector State Snapshot 的 subject attrs、Inspector `原始状态 JSON`、审查工作台 subject 视图的 `此时状态` 都改为 JsonViewer 承载；raw state 传入 `{ at, scope, subjects }` 对象而不是预格式化字符串，底部面板也删除了旧 `stateRows` 单层表格和 `WorldWorkbenchPreviewAttrRow` 类型。此前按用户 7 点反馈完成一轮修正：Inspector metadata draft 只在 `time / title / summary / kind` 真正变化时进入 Draft Queue，单纯切换 slice 不再误生成 metadata 草稿；Inspector State Snapshot 的 object / array / primitive / null 多层显示能力现在由 `JsonViewer` 承载；补齐 `worldEngine.workbenchPreview.sliceContext` 中英文 i18n；删除 Inspector 底部低价值 `Schema excerpt` 和 Sidebar 顶部 Schema 四个统计小卡片；Slice Card 的 subject group 支持整卡点击和 Enter / Space 键盘聚焦。`/world-engine.workbench-preview` 继续作为 mock-only UI / UX 工作台推进，当前顶栏已新增全局 `Drafts` 汇总入口，可统一显示 metadata / value 草稿 slice 数，并在点击后清空阻挡过滤、进入 `status=draft`、自动打开 Inspector / 底部审查工作台处理首个草稿；右侧 Inspector 视觉顺序也调整为 Touched Subjects / State Snapshot 优先；左侧 Sidebar 收起态新增 collapsed summary rail，保留 active / selected / open review / value draft subject 状态；中间 Slice List 的 open / done / clean / draft 统计卡现在可直接作为 status 快捷过滤入口，且计数保留 search / kind / subject 条件但忽略当前 status，避免快捷入口被自身过滤清零；状态分布统计已收束为 `collectResultStats`，当前完整过滤统计和快捷卡稳定计数共用同一份计算；状态快捷卡支持再次点击当前状态恢复 `status=all`；状态快捷卡和 status 分段控件已增加 `aria-pressed`，让过滤选中态具备一致的按钮语义；下方 status 分段控件也已复用同一份 `statusShortcutStats` 上下文计数，`clean` 补充数量展示，避免同屏两个 status 入口数字不一致；kind 分段控件新增 `kindShortcutSlices` / `kindShortcutCountMap` 上下文计数和 `aria-pressed`，在当前 search / subject / status 视角下展示各 kind 分布；多 subject 的 `任一 / 全部` 模式、左侧 `active / open / done / value` 筛选和 subject 行选中也已补齐 `aria-pressed`；底部审查工作台的模式切换、touched subject、subject 导航范围，以及右侧 Inspector 的完整 State Snapshot 展开也已补齐 `aria-pressed`；左侧 Sidebar、右侧 Inspector 和底部审查工作台现在统一使用 `useResizablePanel` 支持拖拽尺寸，尺寸随浏览器 mock 草稿 v4 持久化并在 reset 时恢复默认。对应 walkthrough 记录在 `docs/tasks/59-world-engine-workbench-redesign/walkthroughs/2026-06-20-topbar-draft-summary.md`、`docs/tasks/59-world-engine-workbench-redesign/walkthroughs/2026-06-20-inspector-snapshot-priority.md`、`docs/tasks/59-world-engine-workbench-redesign/walkthroughs/2026-06-20-sidebar-collapsed-summary.md`、`docs/tasks/59-world-engine-workbench-redesign/walkthroughs/2026-06-20-slice-stat-filter-shortcuts.md`、`docs/tasks/59-world-engine-workbench-redesign/walkthroughs/2026-06-20-slice-stat-stable-counts.md`、`docs/tasks/59-world-engine-workbench-redesign/walkthroughs/2026-06-20-slice-stat-stats-refactor.md`、`docs/tasks/59-world-engine-workbench-redesign/walkthroughs/2026-06-20-slice-stat-toggle-filter.md`、`docs/tasks/59-world-engine-workbench-redesign/walkthroughs/2026-06-20-slice-status-aria-pressed.md`、`docs/tasks/59-world-engine-workbench-redesign/walkthroughs/2026-06-20-slice-status-context-counts.md`、`docs/tasks/59-world-engine-workbench-redesign/walkthroughs/2026-06-20-slice-kind-context-counts.md`、`docs/tasks/59-world-engine-workbench-redesign/walkthroughs/2026-06-20-filter-toggle-aria-pressed.md`、`docs/tasks/59-world-engine-workbench-redesign/walkthroughs/2026-06-20-editor-inspector-aria-pressed.md`、`docs/tasks/59-world-engine-workbench-redesign/walkthroughs/2026-06-20-resizable-panels-and-triage-segment.md`、`docs/tasks/59-world-engine-workbench-redesign/walkthroughs/2026-06-20-snapshot-tree-and-draft-fixes.md`、`docs/tasks/59-world-engine-workbench-redesign/walkthroughs/2026-06-20-snapshot-json-viewer.md`、`docs/tasks/59-world-engine-workbench-redesign/walkthroughs/2026-06-20-json-viewer-raw-and-editor-state.md`、`docs/tasks/59-world-engine-workbench-redesign/walkthroughs/2026-06-20-issue-review-surface-refactor.md` 与 `docs/tasks/59-world-engine-workbench-redesign/walkthroughs/2026-06-20-issue-review-panel-context.md`。

World Engine Workbench mock preview issue 解释最新补充：本轮在上一段 issue 审批重构基础上，把 Review Focus 改成程序生成的人话诊断；`base-shifted` / `masked` / `broken-relative` / `dangling-ref` 分别以 A1 / A2 / E1 / E2 说明一次性提醒或持续现算问题。Mutation Context 仍展示同 subject + attr 路径相关的前一个 / 当前 / 后一个 mutation，但不再显示 before / after，而是展示动作、依赖 / 覆盖关系、为什么相关和需要确认什么；对应 walkthrough 为 `docs/tasks/59-world-engine-workbench-redesign/walkthroughs/2026-06-20-issue-human-explanation.md`。

World Engine Workbench Real API 最新补充：主 IDE `WorldEngineWorkbenchDialog.vue` 已替换为 mock workbench 验证过的三栏工作台，并接入真实 `/api/projects/world-engine/**`：读取 schema / subjects / slices，选中 slice 后通过 `state/query` 查询触及 subjects 的当前与前一切片状态，Inspector 展开完整世界时再按需调用 `GET /state?at=...`。metadata 与 mutation value 草稿通过 `POST /slices/:id/edit` 显式保存，删除当前 slice 使用 `DELETE /slices/:id`；创建 subject、一键示例世界、刷新和打开独立 Preview 入口保留。主 Workbench 现在新增 `新建 Slice` 正门，复用真实 API `WorldEngineMutationEditor` 作为 Workbench 内 Slice Composer 浮层，可直接写入新 slice 或载入当前 slice 整块编辑；保存后会刷新 timeline、选中新 slice、清理阻挡过滤，并把返回 issues 接入当前会话 review queue。`WorldEngineMutationEditor` 支持从父层接收已占用时间，使用 `suggestNextPreviewTime()` 推导新建模式默认时间，降低连续推演时同 instant 冲突概率。issue triage、metadata draft 与 value draft 仍是当前 Dialog 会话态，不写 `localStorage`；`/world-engine.workbench-preview` 继续作为 mock-only UI 实验场。目标 vitest 与 `bun run typecheck` 已通过，浏览器视觉验收仍需用户明确授权后执行；本轮 walkthrough 记录在 `docs/tasks/61-world-engine-workbench-real-api/walkthroughs/2026-06-21-real-api-dialog-integration.md` 与 `docs/tasks/61-world-engine-workbench-real-api/walkthroughs/2026-06-21-main-workbench-slice-composer.md`。

World Engine Workbench 主题适配补充：主 IDE 真实 `WorldEngineWorkbenchDialog.vue` 已修正 UI 主题系统适配问题，Workbench Dialog 现在禁用 Teleport，直接留在 `index.vue` 的 `.novel-ide-theme` 宿主内，避免目标选择器先于主题宿主 class 解析导致弹窗脱离项目主题。Workbench 根上的 `--we-*` 语义变量现在从当前 IDE `--bg-* / --text-* / --accent-*` token 派生，不再硬编码浅白绿色，也不再反向覆盖全局主题变量；顶栏 Draft / Inspector warning 状态也改用 `--we-warning-*` 和 `--we-bg-panel`，不再使用固定 `amber-50 / white` 浅色块。目标 vitest 与 `bun run typecheck` 已通过；浏览器已在 `ming-ding-zhi-shi-2` 的 `sepia paper` 下验证真实 Workbench：`.world-engine-workbench-dialog` 位于 `.novel-ide-theme` 内，根背景为 `rgb(244,236,216)`，面板为 `rgb(253,246,227)`，accent 为 `#d97743`，1180 宽视口无水平溢出；截图见 `.agent/workspace/world-engine-theme-sepia-1600.png` 和 `.agent/workspace/world-engine-theme-sepia-1180.png`。记录见 `docs/tasks/61-world-engine-workbench-real-api/walkthroughs/2026-06-21-theme-token-adaptation.md`。

World Engine Project 迁移补充：`workspace/ming-ding-zhi-shi-2` 已启用项目级 `world-engine/schema.yaml` 与 `calendar.yaml`，schema 包含 `world / character / faction / location / item`，并在 `character` 上增加旧主体链接字段。当前 Project SQLite 已通过 World Engine facade 注册 `world` 与 6 个真实 `simulation/subjects` 角色，排除 `sample-npc`；初始化时间采用 `复兴纪元488年 1月15日 14:00:00`，旧 `风信之月` 本轮临时映射为数字 `1月`。因现有 `createSubject` 会写入 `events set []` 而 `editSlice` 不允许 list `set`，旧主体链接字段未追加到同一 init slice，而是写入下一秒 `kind=init` 切片；验证 `getWorldSchema`、`listWorldSubjects`、`listSlices(withMutations)` 与 `queryState(player/armand-brauer)` 均通过且 issues 为空。记录见 `docs/tasks/61-world-engine-workbench-real-api/walkthroughs/2026-06-21-ming-ding-project-world-engine-enable.md`。

World Engine Project 主体系统适配补充：继续修正上一段轻量迁移不足，`workspace/ming-ding-zhi-shi-2` 的 `character` schema 已适配 `simulation/subjects` 六文件契约，新增 `subjectFile / soulFile / visibleState / eventCount / memoryCount / subjectSystemVersion`，并把 6 个真实角色的 `subject.md / soul.md / mind.md / state.md / memory.jsonl / events.jsonl` 写入 World Engine 当前状态。新增 `主体系统六文件初始化` 切片写入 48 条 mutations，新增 `主体经历记忆初始化` 切片把 58 条 `events.jsonl` 记录作为角色 episodic memory 追加到 `events` list；`memory.jsonl` 以 topic->view 形式写入 `memory` object。Facade 验证 `player / mage` state 可 reduce 且 issues 为空；浏览器验证主 IDE Workbench 不再显示 schema 缺失警告，左侧显示 5 个类型和 7 个主体，Slice List 显示六文件主体系统与 events 迁移切片。记录见 `docs/tasks/61-world-engine-workbench-real-api/walkthroughs/2026-06-21-ming-ding-subject-system-adapter.md`。

World Engine Project 主体系统拓扑补充：`ming-ding-zhi-shi-2` 继续从“全文字段迁移”收敛到“文件拓扑 + 可见性边界 + RAG 来源”。`character` schema 新增 `subjectFiles / actorImportPath / leaderOnlyPath / directStatePath / ragIndexSources`；新增 `主体系统拓扑初始化` 切片写入 30 条 mutations，记录每个真实角色的六文件路径、actor Import 文件、leader-only 文件、直接状态文件和 RAG 索引源，facade 验证 `player` state 可 reduce 且 issues 为空。主画布 Slice Card 已压缩长文本 / object / list value 展示，并对每个 subject 超过 6 条的 mutations 显示 `+N mutations`，避免 `subject.md` / `events.jsonl` 这类主体系统内容淹没世界切片列表。目标 vitest、`bun run typecheck` 与浏览器 Workbench 验证通过；截图见 `.agent/workspace/world-engine-compact-subject-system-dialog.png`，walkthrough 见 `docs/tasks/61-world-engine-workbench-real-api/walkthroughs/2026-06-21-subject-system-topology-and-card-density.md`。

World Engine Project 主体系统信息边界补充：`ming-ding-zhi-shi-2` 已继续从“全文字段迁移”收口到“拓扑 + 来源 + 计数”。`character` schema 不再声明 `subjectFile / soulFile / visibleState / mind / memory / events` 这些主体源文件全文或 RAG 镜像字段；新增 `主体系统信息边界收口` init slice，通过 `WorldEngineFacade.writeSlice()` 对 6 个真实角色 unset 旧字段。当前 reduce state 不再暴露 `subject.md` 全知秘密档、`soul.md` actor Import 手册、`mind.md / state.md` 当前文本或 `events.jsonl / memory.jsonl` 条目镜像，只保留 `subjectFiles / actorImportPath / leaderOnlyPath / directStatePath / ragIndexSources / eventCount / memoryCount` 等可检查拓扑和摘要；facade 验证 `player / armand-brauer` 查询 issues 为空。walkthrough 见 `docs/tasks/61-world-engine-workbench-real-api/walkthroughs/2026-06-21-subject-system-information-boundary.md`。

World Engine Project 主体系统 UI 补充：真实 `WorldEngineWorkbenchDialog` 已新增主体系统摘要查询与展示。Dialog 在 schema / subjects / slices 后用窄 `state/query` 读取 character 的 `subjectFiles / actorImportPath / leaderOnlyPath / directStatePath / ragIndexSources / eventCount / memoryCount` 等字段并构造 `subjectSystemSummaries`；左侧 Subjects 行展示“主体系统”接入 badge 与 events / memory 计数，右侧 Inspector 在 State Snapshot 前展示当前切片触及主体的 actor import、leader-only、direct state、RAG 来源并标记 `path only`，让用户不用展开 JSON 就能看懂 `simulation/subjects` 六文件拓扑和信息边界。目标 Workbench vitest 与 `bun run typecheck` 已通过；walkthrough 见 `docs/tasks/61-world-engine-workbench-real-api/walkthroughs/2026-06-21-subject-system-summary-ui.md`。

World Engine 后端/API 最新补充：round-140 按用户调整暂停前端推进，已撤回被打断的 `WorldEngineMutationEditor` 前端草稿；后端修复 `itemType: object` 的 schema default 校验缺口，`list` / `collection` 的默认元素和开放 `object` 的默认子值现在都必须是 JSON object。`list` / `collection itemType=object` 的 mutation item 只校验顶层为 object，不递归要求 item 内每个字段也是 object；开放 `object itemType=object` 则逐 key 校验子值。facade 与 HTTP API 均已补回归测试，后端/API/Agent 目标测试和 typecheck 已通过。

World Engine 后端/API round-141 补充：`createSubject` 现在会在写库前检查重复 subject id，并返回稳定 409 业务错误（包含现有 subject 的 type/name），避免 HTTP API 或 Agent 工具调用者看到 Prisma 唯一约束细节。facade 与 HTTP API 均已补重复 id 回归测试；`agent-tools.md` 和 `sqlite-and-api.md` 已明确 create subject 不是 upsert，调用方应改用已有 subject 或换新 id。

World Engine 后端/API round-142 补充：按 type 查询现在会校验 schema subject type。`queryState({type})`、`listWorldSubjects({type})`、HTTP `POST /state/query`、`GET /subjects?type=` 和 Agent `get_world_state({type})` 在 schema 已声明 subject types 且 type 未声明时返回 400，避免拼错类型被静默解释为空结果；facade / HTTP API / Agent 工具均已补回归测试。

World Engine 后端/API round-143 补充：`dangling-ref` 对 `list` / `collection` ref 元素现在会记录具体元素来源 slice。reduce 会为 `listAppend` / `collectionAdd` 记录 `${attr}[index]` 来源，`collectionRemove` 后会按稳定 JSON 值重排剩余元素来源，避免多个 slice 依次写同一属性时把缺失 ref 误归到最后一次属性变更。facade 已补 collection ref 元素来源回归测试；system profile artifacts 已重新编译，`world.engine` catalog 测试超时上限调到 60s。目标后端/API/Agent 测试通过；`bun run typecheck` 当前仍被无关 `server/low-code-form/*` 类型错误阻塞。

World Engine 后端/API round-144 补充：A issue 的下游检测已从完全相同 attr 扩展为同一路径或父子路径相关。过去整体 `object` set / unset 现在会检查下游子路径相对 op 或绝对 op，例如编辑 `stats` 会对后续 `stats.hp add` 返回 `base-shifted`，避免嵌套 attr 漏掉语义确认提醒；返回 issue 的 `attr` 指向实际下游问题路径，方便 API/Agent/UI 聚焦对应 mutation。facade 已补嵌套父子路径回归测试；system profile artifacts 已重新编译，目标后端/API/Agent 测试与 `bun run typecheck` 均通过。

World Engine 后端/API round-145 补充：父路径整体 `set/unset` 的 A issue 收集现在会按下游子路径分别返回第一条相关提醒，避免 `stats.note set` 这类较早子路径覆盖提醒吞掉后续 `stats.hp add` / `stats.mp add` 的 `base-shifted`。扫描过程中若遇到下游父路径整体覆盖当前路径，则返回 `masked` 后停止该路径，保持整体覆盖语义。facade 已补多子路径 masked + base-shifted 回归测试；已重新编译 `world.engine` profile artifact，目标后端/API/Agent 测试通过；`bun run typecheck` 当前被无关 `server/agent/profiles/profile-home.test.ts` 类型错误阻塞。

World Engine 后端/API round-146 补充：`queryState({attrs})` 的 E issues 现在会跟随 attr 投影范围收窄，查询 `hp` 不再带回同一 subject 上 `location` 的 `dangling-ref` 噪音；查询父属性仍会保留子路径问题，例如 `inventory` 会返回 `inventory[0]`。无 attrs 查询与 `getWorldState` 继续返回完整 E issues。facade 已补 attrs issue scope 回归测试；已重新编译 `world.engine` profile artifact，目标后端/API/Agent 测试与 `bun run typecheck` 均通过。

World Engine 后端/API round-147 补充：`editSlice` 的 A issue 收集已区分原样保存、语义变化和 instant 移动。原样保存已有 mutation 不再重复返回 `base-shifted` / `masked`；移动 slice instant 时会同时从旧位置和新位置观察下游影响，并排除当前 slice 自身，避免“把基准切面移到下游相对 op 之后”时漏掉提醒，也避免自己覆盖自己的假 `masked`。facade 已补原样保存不误报、移动 instant 返回 `base-shifted` 回归测试；已重新编译 `world.engine` profile artifact，目标后端/API/Agent 测试与 `bun run typecheck` 均通过。

World Engine 后端/API round-148 补充：`editSlice` 删除旧绝对 mutation 时也会用旧 mutation 在旧位置观察下游影响，避免删掉 `hp set 80` 后因为更早 default `hp=100` 仍存在而没有 E issue、却静默改变下游 `hp add` 的 reduce 结果。facade 已补“删除旧 `hp set` 后返回下游 `base-shifted`，最终 hp 从 70 变 90”的回归测试；已重新编译 `world.engine` profile artifact，目标后端/API/Agent 测试与 `bun run typecheck` 均通过。

World Engine 后端/API round-149 补充：HTTP API 已补 `editSlice` 删除旧绝对 mutation 时返回下游 `base-shifted` 的契约测试，覆盖日历字符串边界下的 `{sliceId, issues}` 返回语义；facade 测试 afterAll 临时 Project 清理 hook timeout 已放宽到 30s，避免测试规模增长后清理阶段假红。已重新编译 `world.engine` profile artifact，目标后端/API/Agent 测试与 `bun run typecheck` 均通过。

World Engine 后端/API round-150 补充：Agent 工具层已补 `edit_world_slice` 删除旧绝对 mutation 时返回下游 `base-shifted` 的契约测试，确认 round-148 的 `{sliceId, issues}` 语义不只停留在 facade / HTTP API，也会通过 Agent 工具返回；`agent-tools.md` 同步说明 `get_world_state(attrs)` 的 issues 会跟随属性投影范围收窄。目标后端/API/Agent 测试与 `bun run typecheck` 均通过。

World Engine 后端/API round-151 补充：`editSlice` 的 A issue 候选集已从“任一 mutation 变化就重扫整块 slice”收紧为同 instant 下只扫描真正删除 / 修改 / 新增的 mutation，避免只改 `mind` 时为未变化的 `hp set` 重复返回下游 `base-shifted`；移动 instant 时仍把整组 mutation 视为被移动并观察旧 / 新位置。facade 已补未变绝对 mutation 不误报回归测试；已重新编译 `world.engine` profile artifact，目标后端/API/Agent 测试与 `bun run typecheck` 均通过。

World Engine 后端/API round-152 补充：`editSlice` 现在会识别同 instant 纯重排里的相关 mutation 顺序变化。若同一 subject 上同一路径或父子路径相关的 mutation 相对顺序改变，会纳入 A issue 候选，避免 `hp set 80` / `hp set 90` 调换后静默改变下游 `hp add` 的累加基；不同属性且路径无关的重排不会产生提醒噪音。facade 已补相关重排返回 `base-shifted` 与无关重排不误报回归测试；HTTP API 与 Agent 工具也已补日历字符串入口契约测试，目标后端/API/Agent 测试与 `bun run typecheck` 均通过。

World Engine 后端/API round-153 补充：round-152 的重排检测已从“纯重排且长度相同”放宽为“共同保留的相关 mutation 是否发生相对顺序反转”。同一次 `editSlice` 如果新增 / 删除了无关 mutation，同时把保留的 `hp set 80` / `hp set 90` 调换顺序，现在仍会返回下游 `base-shifted`；无关属性重排仍不产生提醒。facade、HTTP API 与 Agent 工具均已补混合编辑回归测试，目标后端/API/Agent 测试与 `bun run typecheck` 均通过。

World Engine 后端/API round-154 补充：`queryState({subjectIds})` 现在会拒绝重复 subject id，HTTP `POST /state/query` 与 Agent `get_world_state` 同步返回 400，避免以前静默去重后返回数量与调用方请求不一致。facade、HTTP API 与 Agent 工具均已补重复 `subjectIds` 回归测试；本轮重新编译 `world.engine` system profile artifact，目标后端/API/Agent 测试与 `bun run typecheck` 均通过。

World Engine 后端/API round-155 补充：Agent 工具 `list_world_slices` 现在不传 `limit` 时默认只返回最近 5 个切面，避免把完整 timeline 倾倒给模型；显式 `limit` 仍由工具 schema 限制在 1..50，HTTP `GET /slices` 保持原显式查询语义。Agent 工具测试已补 7 个切面时默认返回最近 5 个且保持时间正序的回归；目标后端/API/Agent 测试与 `bun run typecheck` 均通过。

World Engine 后端/API round-156 补充：单个 slice 的 mutations 上限 100 已从 Agent 工具 schema 下沉到 `WorldEngineService.validateMutations()`，HTTP `POST /slices` / `POST /slices/:id/edit` body schema 也同步 `.max(100)`，避免非 Agent 入口写入超大切面。facade 已补 `writeSlice` / `editSlice` 超限回归测试，HTTP API 已补写入超限返回 400 的契约测试；目标后端/API/Agent 测试与 `bun run typecheck` 均通过。

World Engine 后端/API round-157 补充：`queryState({attrs})` 现在会拒绝重复属性路径，HTTP `POST /state/query` 与 Agent `get_world_state` 同步返回 400，避免投影对象静默覆盖后返回项数量与调用方请求不一致。facade、HTTP API 与 Agent 工具均已补重复 `attrs` 回归测试；目标后端/API/Agent 测试与 `bun run typecheck` 均通过。

World Engine 后端/API round-158 补充：修正 round-155 的 Agent `list_world_slices` 默认 limit 行为。默认最近 5 个切面现在只在未传 `limit/from/to` 时生效；带 `from` 或 `to` 的区间查询不再默认截断，避免“某段历史有哪些切面”只返回前 5 个。Agent 工具测试已补区间查询不默认截断的回归；本轮重新编译 `world.engine` system profile artifact，目标后端/API/Agent 测试与 `bun run typecheck` 均通过。

World Engine 后端/API round-159 补充：`createSubject` 的 schema default 初始化路径已补齐单 slice 100 条 mutation 容量约束。直接生成 init slice 时，default mutations 超过 100 会返回 400；同 instant 已有 init slice 并追加 default mutations 时，会按追加后的总 mutation 数检查，超过 100 同样拒绝，避免通过 subject 创建绕过 `writeSlice` / `editSlice` 的 service 层限制。facade 与 HTTP API 已补回归测试，目标后端/API/Agent 测试与 `bun run typecheck` 均通过。

World Engine 后端/API round-160 补充：`createSubject` 的 schema default 自动追加现在只允许落到同 instant 已有的 `kind=init` 切面；如果该时间已有 `event/backstory` 等非 init 切面，会返回 409，要求调用方使用 `editSlice` 显式合并或选择其他初始化时间，避免 subject 创建路径悄悄改写普通事件切面。facade 测试已覆盖非 init 冲突时 subject 身份事务回滚，HTTP API 已补 409 回归；目标后端/API/Agent 测试与 `bun run typecheck` 均通过。

World Engine 后端/API round-161 补充：Agent `create_world_subject` 工具描述已同步 round-160 的 init slice kind 契约，明确 schema defaults 只会写入 `kind=init` slice，目标时间已有非 init slice 时需要显式编辑已有切面或选择其他时间。Agent 工具测试新增该 409 冲突回归；因工具描述属于 profile 依赖面，本轮已重新编译 `world.engine` system profile artifact。目标后端/API/Agent/Profile 测试与 `bun run typecheck` 均通过。

World Engine 后端/API round-162 补充：subject id 形状校验已下沉到 `WorldEngineService`。`createSubject` 的 `id`、slice mutation 的 `subjectId`、`queryState(subjectIds)` 都会拒绝空白或带首尾空白的 id，HTTP `POST /subjects` 与 Agent `create_world_subject` 入口同步暴露稳定业务错误，避免 `subject://<id>` 引用 URI、查询入参和存储 id 因隐式空格漂移。facade / HTTP API / Agent 工具已补回归测试，目标后端/API/Agent/Profile 测试与 `bun run typecheck` 均通过。

World Engine 后端/API round-163 补充：`subject://<id>` ref 字符串内部的 `<id>` 现在复用 subject id 形状校验。写入 `subject://` 空 id 或 `subject:// erina ` 这类带首尾空白的 ref id 会返回稳定 400，提示具体属性的引用 id 为空或含首尾空白，而不是误导为普通“引用目标不存在”。facade / HTTP API / Agent 工具已补写入 ref 回归测试，目标后端/API/Agent/Profile 测试与 `bun run typecheck` 均通过。

World Engine 后端/API round-164 补充：`queryState` 的防全量倾倒规则已从 HTTP / Agent 边界下沉到 `WorldEngineService`。现在 Facade 直调 `queryState(projectPath, {})` 也会返回 400，要求提供 `subjectIds` 或 `type`；需要全量世界状态的 UI / 调试 / 导出路径继续使用 `getWorldState`。facade 已补裸 queryState 拒绝与 getWorldState 保持可用的回归，目标后端/API/Agent/Profile 测试与 `bun run typecheck` 均通过。

World Engine 后端/API round-165 补充：schema loader 已收紧 subject type 名形状。`subjectTypes` 下的 type key 不能为空，也不能包含空白，避免 `createSubject`、`queryState(type)`、`listWorldSubjects(type)` 和 `ref(type)` 依赖不可稳定引用的 schema key。facade / HTTP API / Agent 工具已补 schema 读取回归；目标后端/API/Agent/Profile 测试与 `bun run typecheck` 均通过。

World Engine 后端/API round-166 补充：schema attr 名与运行时 attr path 段名已继续收紧。schema attr 名不能为空、不能包含首尾空白或点号，避免 `flattenAttrs()` 投影出的 dotted path 与 `findAttrSchema()` 的路径分隔语义冲突；写入 / 查询 attr path 每段也会拒绝首尾空白。facade / HTTP API / Agent 工具已补 schema 与 attrs 回归；因 profile 依赖哈希变化，本轮重新编译 `world.engine` system profile artifact。目标后端/API/Agent/Profile 测试与 `bun run typecheck` 均通过。

World Engine 后端/API round-167 补充：`WorldSlice.kind` 形状校验已下沉到 `WorldEngineService`。slice kind 仍允许项目自定义，省略时继续默认 `event`，但显式传入时不能为空，也不能包含首尾空白，避免空字符串绕过默认值或生成不可稳定过滤的 timeline 标签。facade / HTTP API / Agent 工具已补写入 kind 回归测试；目标后端/API/Agent/Profile 测试与 `bun run typecheck` 均通过。

World Engine 后端/API round-168 补充：运行时 subject type 入参形状校验已下沉到 `WorldEngineService`。`createSubject(type)`、`queryState(type)`、`listWorldSubjects(type)` 现在和 schema subject type key 使用同一套稳定 key 规则：不能为空，也不能包含任意空白；避免空 schema 或直接 facade/API/Agent 调用把 `" player character "` 这类不稳定 type 写入 subject 身份或查询边界。facade / HTTP API / Agent 工具已补 type 入参回归测试；目标后端/API/Agent/Profile 测试与 `bun run typecheck` 均通过。

World Engine 后端/API round-169 补充：`editSlice(sliceId)` 与 `deleteSlice(sliceId)` 的入参形状校验已下沉到 `WorldEngineService`。`sliceId` 必须使用后端返回值原样传回，不能为空，也不能带首尾空白；HTTP path 入口不再静默 trim `%20...%20`，而是稳定返回 400，避免调用方把错误 id 误判成“切面不存在”。facade / HTTP API / Agent 工具已补 edit/delete sliceId 回归测试；目标后端/API/Agent/Profile 测试与 `bun run typecheck` 均通过。

World Engine 后端/API round-170 补充：稳定设计文档继续做当前契约同步。`agent-tools.md` 已把 `get_world_schema` 从待定问题改成已接入工具，`worked-example.md` 已把项目日历说明改为第一版 `world-engine/calendar.yaml` parse/format 契约，并把 `createSubject` 同 instant 初始化追加描述对齐为只允许追加到 `kind=init` 切面。本轮只改文档，不改变代码行为。

World Engine 后端/API round-171 补充：HTTP query 参数边界已收紧为不静默裁剪。`GET /subjects?type=` 会把带首尾空白的 type 原样交给 service 层校验并返回稳定 400；`GET /slices?limit=&withMutations=&from=&to=` 和 `GET /state?at=` 也不再接受空白包裹的数字、布尔或时间 query，空字符串仍按未传处理。API 契约测试已补该行为，目标后端/API/Agent/Profile 测试 116 条与 `bun run typecheck` 均通过。

World Engine 后端/API round-172 补充：HTTP query 参数继续收紧为单值语义。`GET /subjects?type=`、`GET /slices?limit=&withMutations=`、`GET /state?at=` 等共享 query reader 现在遇到重复 query 形成数组时返回 400：`${key} 只能传一个值`，避免数组值被静默当作未传而扩大查询范围或忽略过滤。API 契约测试已补 type / limit / withMutations / at 数组值，目标后端/API/Agent/Profile 测试 117 条与 `bun run typecheck` 均通过。

World Engine 后端/API round-173 补充：HTTP 与 Agent 工具公开时间入参已禁止 `instant:<number>` raw instant 调试格式。`POST /subjects`、`POST /slices`、`POST /state/query`、`GET /state?at=`、`GET /slices?from=&to=` 以及 Agent `create_world_subject` / `write_world_slice` / `edit_world_slice` / `get_world_state` / `list_world_slices` 的时间字段现在都要求项目日历字符串；底层 `WorldCalendar.parse()` 与 facade `parseTime()` 仍保留 raw instant 兼容给测试和调试。API / Agent 工具已补公开边界回归测试，目标后端/API/Agent/Profile 测试 119 条与 `bun run typecheck` 均通过。

World Engine 后端/API round-174 补充：公开时间入参继续收紧为不静默裁剪。HTTP body 的 `POST /subjects.time`、`POST /slices.time`、`POST /state/query.at` 以及 Agent `create_world_subject.time` / `write_world_slice.time` / `edit_world_slice.time` / `get_world_state.at` / `list_world_slices.from/to` 现在遇到首尾空白会返回稳定 400；HTTP query 时间字段已在 round-171 收紧，本轮补齐 body 与 Agent 工具入口。底层 `WorldCalendar.parse()` 仍保留 trim 给 facade 直调测试/调试使用。API / Agent 工具已补公开边界回归测试，目标后端/API/Agent/Profile 测试 121 条与 `bun run typecheck` 均通过。

World Engine 后端/API round-175 补充：service 层按 type 查询的空字符串校验已补齐。`queryState({type:""})` 现在会返回 `subject type 不能为空`，不再退化成“缺少 subjectIds/type”；`listWorldSubjects({type:""})` 也会返回同一稳定错误，不再因为空字符串 falsy 而绕过校验并返回全量 subject 列表。facade 已补空 type 直调回归测试，目标后端/API/Agent/Profile 测试 121 条与 `bun run typecheck` 均通过。

World Engine 后端/API round-176 补充：`queryState` 的数组入参在 service 层也补齐非空约束。facade/service 直调传 `subjectIds: []` 或 `attrs: []` 现在会返回稳定 400：`subjectIds 不能为空` / `attrs 不能为空`；不再把空数组静默当作未传，避免 `attrs: []` 意外放大成完整状态返回。HTTP / Agent 入口原本已有 minItems schema，本轮补齐底层契约并新增 facade 回归测试；目标后端/API/Agent/Profile 测试 121 条与 `bun run typecheck` 均通过。

World Engine 后端/API round-177 补充：稳定设计文档已同步 round-176 的空数组契约。`agent-tools.md` 和 `sqlite-and-api.md` 现在明确 `queryState` / `get_world_state` 的 `subjectIds`、`attrs` 如果传入必须是非空数组，并继续要求唯一；本轮只改文档，不改变代码行为。

World Engine 后端/API round-178 补充：`Instant` 的 SQLite 64 位存储边界已下沉到 service 层。`createSubject(at)`、`writeSlice(instant)`、`editSlice(instant)`、`getWorldState(at)`、`queryState(at)` 与 `listSlices(from/to)` 现在都会拒绝超出 `[-9223372036854775808, 9223372036854775807]` 的 bigint，返回稳定 400，避免超大项目日历时间或 facade 直调把非法值交给 Prisma/SQLite。HTTP API 已补超大日历字符串回归；设计文档同步说明第一版公开边界会拒绝超 64 位 instant。

World Engine 后端/API round-179 补充：默认 Calendar 现在支持零点前时间的 parse/format 往返。`formatTime(-1n)` 会输出连续纪年的 `复兴纪元0年 12月30日 23:59:59`，`parseTime()` 可解析回 `-1n`；HTTP API 也可用该项目日历字符串创建零点前 init slice。这个修复对齐“Instant 可负、连续数轴不继承无第 0 年历史 bug”的任务定论；若未来需要无第 0 年显示法，再作为 Calendar 扩展单独设计。

World Engine 后端/API round-180 补充：`world-engine/calendar.yaml` 的自定义 `format` 现在会在加载时拒绝重复的可解析时间字段。`year/month/day/hour/minute/second` 都只能出现一次，`hour` 与 `hour:02`、`minute` 与 `minute:02`、`second` 与 `second:02` 视为同一字段；重复时返回稳定 400，避免 parse 阶段暴露 JS 正则重复命名捕获组异常。

World Engine 后端/API round-181 补充：Calendar 单位配置的 YAML number 形式现在必须是 JS safe integer 正整数，避免 `9007199254740992` 这类不安全 number 在 YAML/JS 解析阶段已丢精度后再被转成 BigInt。需要超过 safe integer 的单位值时，仍可使用正整数字符串形式表达，保持大整数配置的精确通道。

World Engine 后端/API round-182 补充：schema 中 `ref(<type>)` 的 `<type>` 现在复用 subject type 稳定 key 规则。目标 type 不能为空、不能包含空白，也不能包含括号；`ref(player character)`、`ref(player(character)` 等配置会在 schema loader 阶段返回稳定 400，避免非法 ref type 被 schema 接受后到写入时才表现成不可匹配类型。

World Engine 后端/API round-183 补充：schema subject type key 与运行时 `createSubject(type)` / `queryState(type)` / `listWorldSubjects(type)` 入参现在都会拒绝括号；schema 中 `ref(type)` 必须指向同一 schema 已声明的 subject type，悬空 ref 会在 schema loader 阶段返回稳定 400。facade 已补 schema type 括号、运行时 type 括号与未知 ref target 的回归测试；本轮不涉及前端。

World Engine 后端/API round-184 补充：schema default 的纯形状 / 类型校验已前移到 schema loader。`getWorldSchema` 现在会拒绝类型不匹配的 default、非法 enum default、错误的 list/collection/object default 和 ref default URI 形状，不再把坏 default 投影给 Agent/API 调用方；ref default 的目标 subject 是否存在仍在 `createSubject` 写入 init mutation 时校验，并保留事务回滚测试。目标后端/API/Agent/Profile 测试与 `bun run typecheck` 均通过；本轮不涉及前端。

World Engine 后端/API round-185 补充：schema enum 候选现在必须按 stable JSON 唯一。`enum: [active, active]` 会在 schema loader 阶段返回稳定 400；对象 enum 候选也会先 canonicalize，`{phase: active, flag: urgent}` 与 `{flag: urgent, phase: active}` 会被视为重复，避免 `getWorldSchema` 给 Agent / UI 投影两个语义相同的选项。Facade 已补标量重复和对象 canonical 重复回归，目标后端/API/Agent/Profile 测试与 `bun run typecheck` 均通过；本轮不涉及前端。

World Engine 后端/API round-186 补充：Calendar 单位配置的字符串形式现在也必须大于 0。此前 `secondsPerMinute: "0"` 会通过 `^\d+$` 校验并转为 `0n`，后续 format/parse 可能走到除零；现在会稳定返回 `secondsPerMinute 必须是正整数`。Facade 已补字符串 0 回归；目标后端/API/Agent/Profile 测试复跑通过。`bun run typecheck` 当前被前端 `app/pages/world-engine.workbench-preview.vue` 既有 props 缺口阻塞：`WorldEngineWorkbenchPreviewSidebar` 缺少 `width`，`WorldEngineWorkbenchPreviewMutationEditor` 缺少 `height`；本轮按“只做后端/API”范围未改前端。

World Engine 后端/API round-187 补充：显式存在的 `world-engine/calendar.yaml` 根配置现在必须是 object。此前数组或标量形式会被当作“没有有效字段”并静默回退默认 Calendar；现在返回稳定 400：`calendar 配置必须是 object`。空文件和缺文件仍使用默认 Calendar。Facade 已补数组根配置回归；目标后端/API/Agent/Profile 测试通过。`bun run typecheck` 当前被前端 i18n 既有缺口阻塞：`app/i18n/locales/en-US.ts` 缺少 `worldEngine`；本轮按“只做后端/API”范围未改前端。

World Engine 后端/API round-188 补充：显式配置 Calendar `era` 时现在必须是字符串。此前 `era: 123` 会被忽略并静默回退默认 `复兴纪元`，现在返回稳定 400：`era 必须是字符串`；空字符串仍允许，用于项目选择无 era 前缀的格式。Facade 已补非法 era 回归；目标后端/API/Agent/Profile 测试与 `bun run typecheck` 均通过。

World Engine 后端/API round-189 补充：显式存在的 `world-engine/schema.yaml` 根配置现在必须是 object。此前数组或标量形式会被当作“没有 subjectTypes 字段”并静默回退空 schema；现在返回稳定 400：`schema 配置必须是 object`。空文件和缺文件仍使用空 schema。Facade 与 HTTP API 已补回归测试；目标后端/API/Agent/Profile 测试 128 条与 `bun run typecheck` 均通过。本轮按用户最新范围只做后端/API，不做前端。

World Engine 后端/API round-190 补充：schema 中 subject type / attr 的显式 `desc` 现在必须是字符串。此前 subject type 的非字符串 `desc` 会被静默丢弃，attr 的非字符串 `desc` 可能被投影给 API / Agent；现在统一在 schema loader 阶段返回稳定 400：`desc 必须是字符串：...`。Facade 与 HTTP API 已补回归测试；目标后端/API/Agent/Profile 测试 129 条通过。`bun run typecheck` 当前被前端 mock preview 既有类型问题阻塞：`app/components/novel-ide/world-engine/workbench-preview/WorldEngineWorkbenchPreviewInspector.vue` 的 `renderNode` 缺少递归返回类型标注；本轮按用户最新范围只做后端/API，未改前端。

World Engine 后端/API round-191 补充：`listSlices.limit` 与 HTTP `GET /slices?limit=` 现在必须是 JS safe integer 正整数。此前 `9007199254740992` 这类超出 safe integer 的纯数字 query 会通过格式校验并以已丢精度的 JS number 进入查询；现在返回稳定 400：`limit 必须是安全正整数`。本轮不改变 HTTP timeline 的业务最大上限策略，只补数值精度边界。Facade 与 HTTP API 已补回归测试；目标后端/API/Agent/Profile 测试 129 条与 `bun run typecheck` 均通过。

World Engine 后端/API round-192 补充：schema `type: int` 的 default 与 mutation value 现在必须是 JS safe integer。此前 `9007199254740992` 这类超出安全整数范围的 JSON number 会通过 `Number.isInteger()` 并进入世界状态；现在 schema loader、service 与 HTTP API 都会稳定返回 400：`必须是安全整数`。`float` 仍只要求 finite number。Facade 与 HTTP API 已补 default / mutation 回归测试；目标后端/API/Agent/Profile 测试 130 条与 `bun run typecheck` 均通过。

World Engine 后端/API round-193 补充：`int add` 的 reduce 结果现在也必须保持 JS safe integer。此前 base 和 delta 都各自安全时，`base + delta` 仍可能超出安全整数范围并被写入 reduce 状态；现在 reduce 会把该相对 op 作为持久 E issue 返回：`broken-relative / add <attr> 结果超出安全整数范围`，并保持原安全基准值。Facade 与 HTTP API 已补写入返回 issues 与 query issues 的回归；目标后端/API/Agent/Profile 测试 132 条与 `bun run typecheck` 均通过。

World Engine 后端/API round-194 补充：所有 `add` 的 reduce 结果现在都必须仍是 finite number。此前 base 与 delta 都是 finite number 时，`Number.MAX_VALUE + Number.MAX_VALUE` 这类结果会变成 `Infinity` 并污染 reduce 状态；现在返回持久 E issue：`broken-relative / add <attr> 结果不是有限数`，并保留原有限基准值。Facade 与 HTTP API 已补写入返回 issues 与 query issues 的回归；目标后端/API/Agent/Profile 测试 134 条与 `bun run typecheck` 均通过。

World Engine 后端/API round-195 补充：Agent 工具层不再用 `JSON.stringify` / `JSON.parse` 清洗 mutation `value`。`write_world_slice` / `edit_world_slice` 现在会在工具边界递归校验严格 JSON 值，`NaN`、`Infinity`、函数、symbol、`Date` 等非普通对象，或对象 / 数组内部的 `undefined` 会直接返回输入错误，避免被静默改成 `null`、字符串或丢字段后写入世界状态。Agent 工具已补回归测试；目标后端/API/Agent/Profile 测试与 `bun run typecheck` 均通过。

World Engine 后端/API round-196 补充：严格 JSON value 校验继续下沉到 service 最终边界。`writeSlice` / `editSlice` 现在只把普通对象或 null prototype 对象视为 JSON object，`Date` 等非普通对象不会再因 `Object.values(date)` 为空而通过校验、随后被 `JSON.stringify` 改写成字符串。facade 直调已补回归测试，确认非 HTTP / Agent 调用也不能绕过该边界；目标后端/API/Agent/Profile 测试 135 条与 `bun run typecheck` 均通过。

World Engine 后端/API round-197 补充：World Engine catch-all HTTP API 的 path segment 解码现在会把非法 percent encoding 包装成稳定 400：`API path 编码不合法：<segment>`，不再让 `decodeURIComponent` 的原生 `URIError` 冒出。HTTP API 已补 malformed path 回归测试；目标后端/API/Agent/Profile 测试 136 条与 `bun run typecheck` 均通过。

World Engine P0 真实驾驶补充：round-198 按用户要求暂停继续补输入边界和 guard，没有修改功能代码，只用独立 `/world-engine.preview` 真实跑作者首轮链路。核心模型链路可用：新建 Project、创建示例世界、写入 `erina.hp add -10`、编辑已有 slice 合并事件、删除最新 slice、`queryState` reduce 均符合预期。首个真实卡点是示例世界后写入表单仍停在已有 slice 的同一 instant，作者自然写下一条事件会直接收到“该时间已有切面，请使用 edit_world_slice 合并...”错误；后续 P1 应优先修前端写作流，而不是继续加后端边界。同步记录的体验问题包括 Project 下拉被大量测试 Project 污染且加载慢、写入 / 编辑后 State Query 不自动刷新、同 instant 错误暴露 Agent/API 工具名、Timeline 编辑/删除图标发现性不足。P2 append-only 分叉仍需单独提交 Prisma/schema/repository 迁移计划，确认后再实现。

World Engine P1 真实驾驶修复补充：round-199 修复独立 `/world-engine.preview` 两个首轮写作阻塞点。Preview 现在复用 `suggestNextPreviewTime()`，在一键示例世界、写入 slice、编辑 slice 成功后把写入表单 time 推进到下一个未占用 instant，避免作者下一条自然写入继续撞同一 slice；写入 / 编辑成功后如果 State Query 已有 subject/type scope，会自动刷新 query 结果，让状态面板跟上刚保存的世界变化。目标测试 `bunx vitest run app/utils/world-engine-preview.test.ts app/utils/world-engine-ide-entry.test.ts` 已通过；`bun run typecheck` 当前仍被无关 `WorldEngineWorkbenchPreviewSliceList.vue` 中 `filteredMutationCount` / `filteredResultStats` 缺失类型阻塞。

World Engine P1 真实驾驶修复补充：round-200 把同 instant 冲突的前端展示从后端 / Agent 工具名转成 UI 可执行动作。后端仍返回包含 `existingSliceId/time/title` 的稳定错误，前端通过 `formatWorldEngineConflictMessage()` 在 Preview 和主 Workbench 中提示用户到 Timeline 找到该时间 slice、点击“载入编辑”合并本次变更，或把 time 改到相邻时间；`edit_world_slice` 不再直接出现在 UI 错误文案里。目标测试 `bunx vitest run app/utils/world-engine-preview.test.ts app/utils/world-engine-ide-entry.test.ts` 20 条通过，`bun run typecheck` 通过。

World Engine P1 真实驾驶修复补充：round-201 把真实 Preview / Workbench 中的关键 slice 操作入口从纯图标改为可见文字按钮。独立 Preview Timeline 的编辑 / 删除、主 Workbench Timeline 的编辑、Selected Slice 检查器的删除按钮现在都显示 icon + 文案，并补 `aria-label`；这降低了同 instant 合并和物理删除回退入口的发现成本。目标测试 `bunx vitest run app/utils/world-engine-ide-entry.test.ts` 通过；`bun run typecheck` 当前被无关 `WorldEngineWorkbenchPreviewMutationEditor.vue` 中缺失 `mutationContextBeforeValue` / `mutationContextAfterValue` 的类型错误阻塞。

World Engine P1 真实驾驶修复补充：round-202 处理独立 `/world-engine.preview` 的 Project 列表污染 / 加载慢问题。`/api/projects` 默认全量行为不变；Preview 现在请求列表时会传 `limit=80`，排除 `workspace/world-engine-test-`、`workspace/world-engine-api-test-`、`workspace/world-tools-test-` 这些历史测试 Project 前缀，并把 route / preferred / selected Project 作为 include 补回，保证当前链接或刚创建的 Project 仍可见。服务端 `listNovels()` 增加同名可选参数，默认无过滤时继续使用短 TTL 缓存；目标测试 `bunx vitest run server/utils/novel-chapter.test.ts`、`bunx vitest run app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 均通过。真实浏览器复验仍需用户明确授权后执行。

World Engine P1 真实驾驶修复补充：round-203 改善独立 `/world-engine.preview` 新建 Project 的默认标题和 slug 可读性。Preview 默认标题现在使用 `世界引擎试用 YYYY-MM-DD HH-mm`，不再生成缺分隔的 `2026620`；Project slug 基础名生成统一收敛到 `buildWorkspaceSlugBase()`，中文继续转拼音音节，连续英文 / 数字段会作为整体保留，因此日期时间会生成类似 `shi-jie-yin-qing-shi-yong-2026-06-20-23-56`，不再拆成 `2-0-2-6`。`/api/projects` 与旧兼容 `/api/novels` 均使用同一 helper；目标测试 `bunx vitest run server/workspace-files/novel-workspace.test.ts`、`bunx vitest run app/utils/world-engine-ide-entry.test.ts` 与 `bun run typecheck` 均通过。真实浏览器复验仍需用户明确授权后执行。

World Engine P2 append-only 分叉计划补充：round-204 已提交迁移与实现计划，尚未改代码。当前证据显示 Project Prisma schema 仍是 `WorldSlice.instant @unique`，Project SQLite 初始化 SQL 仍创建 `WorldSlice_instant_key`，repository 仍按全局 instant 查询，`op-behavior-matrix.md` 也明确分叉尚未落地。计划建议新增 `WorldBranch`、`WorldSlice.branchId`、`WorldMutation.branchId`，用 `ProjectMetadata.worldEngine.activeBranchId` 存 active branch；现有数据迁到固定 root branch `main`，Project DB schemaVersion 升到 2，查询按 root→active 的 active path reduce。推荐语义是 rollback 非破坏性创建新 branch 并切换 active，switchBranch 只改 active，子分支只允许写 `instant > forkInstant`，inherited slice 在子分支只读，deleteSlice 继续作为 active branch 自有 slice 的物理 cleanup。等待用户确认后再执行 Prisma schema、SQLite migration、repository/service、HTTP/Agent 工具和文档更新。

World Engine Workbench Preview 最新补充：`/world-engine.workbench-preview` 的技术工作台 mock 页面继续完成 subject 查看路径、整体世界巡检、首屏空间和编辑闭环打磨。左侧 Sidebar 本轮新增 subject activity / review stats：subject 行展示最近出现时间、最近 kind、mutation 数和 triage-aware review 状态，顶部显示 active / open / done subject 汇总；`旧剑` 这类 review 相关主体在左侧即可看到 `open` 或 `done` 信息，不用先进入 review 过滤。中间 Slice List 已显示 subject filter chips 并支持逐个移除过滤，顶栏过滤上下文改用 subject name；Mutation Editor subject 视图会跟随外部 subject 过滤自动切到对应 subject。Slice List 本轮新增非 subject 维度过滤：按当前 mock slices 自动生成 `init / event / backstory` kind 过滤，并提供 triage-aware `open / done / clean` issue 状态过滤；`清空状态过滤` 可恢复完整时间线，整体世界视角能直接定位 backstory、仍需处理的 open 切片或已处理的 done 切片。右侧 Inspector 的 `Touched Subjects` 现在可点击聚焦 subject，并同步底部 Mutation Editor 与右侧 State Snapshot 展开状态；Inspector 也补齐 `Review Issues` 区块，review slice 会直接展示 issue level、code、subject、attr 和 message，点击 issue 可聚焦对应 subject，并联动 Mutation Editor 查看 mutation 与切片前后状态；本轮进一步新增 `Review Queue`，mock 增加 `masked / erina / memory.旧剑状况` issue，Inspector 可在 review issues 之间上 / 下一个跳转，并自动切换 slice、subject 和 mutation target。Inspector 本轮继续加入 mock-only issue triage，review issue 可标记为 `待处理 / 已确认 / 已忽略`，Review Queue 显示 `open / done / confirmed / ignored` 进度，状态随浏览器 v3 草稿刷新恢复，`重置 mock` 会清空 triage；Review Queue 默认改为 `只看 open` 连续处理模式，保留 `全部 issue` 回看，确认当前 issue 后可直接跳到下一个 open，open queue 清空时显示完成状态。Slice Card 现在会接收 preview-only `sliceReviewSummary`，卡片 badge 从 raw `N issues` 改为 `open X/Y` 或 `done X/Y`，用户在主画布即可看出处理进度；确认 issue 后切片会自动退出 open 过滤并进入 done 过滤。issue 点击和 queue 跳转都会推进到 attr 级定位，Mutation Editor 命中行会显示 warning 背景和 `issue target` 标签，让用户直接看到具体问题变更。Mutation Editor 现在还会在 issue target 存在时显示 `Review Focus` 状态条，保留当前 issue code、subject、attr 和 message，并提供 `清除定位`，让高亮状态有明确上下文和生命周期。Preview route 本轮新增浏览器本地 mock 草稿持久化：slice metadata、mutation value、面板状态和 issue triage 状态会保存到 `localStorage`，刷新后恢复，顶栏显示 `浏览器临时 mock` 或 `浏览器草稿 HH:mm:ss`；`重置 mock` 会清除本地草稿、恢复默认数据，并通过 `resetVersion` 同步清空 Sidebar / Slice List 的本地过滤，避免 reset 后仍停留在 review 过滤结果。浏览器复测时还修复了 Inspector 草稿同步问题，同一个 slice id 下 reset 或恢复 metadata 时，`time/title/summary/kind` 表单会跟随外部 slice 字段刷新。Mutation Editor 默认改为折叠信息条，保留当前 slice / mutation / active subject 摘要；点击 Inspector subject、Inspector issue 或 Slice Card 内 subject group 的聚焦图标时会自动展开。Slice Card 内 subject group 现在同时提供“聚焦检查”和“只看 subject timeline”两个图标入口，可直接从主画布切到单 subject 视角；Mutation Editor 顶栏也已修正 1180 桌面视口下的换行 / 竖排问题。Mutation Editor 已从只读检查面板推进到可编辑 mock 面板：subject 视图和总视图均可编辑 mutation value，应用后会同步 Slice Card、右侧 State Snapshot 与顶栏 notice；非法 JSON 草稿只显示局部错误，不污染 mock snapshot。`WorldEngineWorkbenchPreviewValueInput` 已接入 schema-aware 控件，支持 text、number、bool select、ref subject select、enum select 和 JSON textarea，减少用户手写 value 格式的负担；本轮进一步补齐 `collectionRemove` 当前状态候选下拉，mock 新增 `艾莉娜把旧剑交给莫然` 切片，`inventory collectionRemove` 会从当前 snapshot 生成 `旧剑 · old-sword` 候选，reduce 后 `erina.inventory` 为空、`moran.inventory` 持有旧剑且 `old-sword.owner` 指向莫然。Mutation Editor 现在还会接收 previous snapshot，在 subject 视图和总视图里展示 mutation attr 的 `切片前 / 切片后` 对照；交剑切片可直接看到 `inventory` 从 `["subject://old-sword"]` 变为 `[]`。mock reducer 已抽到 `world-engine-workbench-preview-state.ts`，从 schema default + subject 身份重新 reduce 全量 snapshots，避免第一张 slice 的相对 mutation 被重复叠加；value parser / formatter 已抽到 `world-engine-workbench-preview-value.ts`。目标 vitest 已从静态契约扩展到 4 条测试，覆盖 schema default、相对 mutation reduce、mutation value patch、`collectionAdd` / `collectionRemove` 行为，以及 JSON-like 输入、数字、布尔、null 和非法 JSON 解析边界。`旧剑` 过滤、`王都` Inspector 点击、默认折叠空间、卡片内 `王都` timeline 筛选、`capital.name` value 编辑、交剑切片 `collectionRemove` 下拉、切片前后状态对照、`backstory` 过滤、`open` 过滤、`done` 过滤、`clean` 过滤、清空状态过滤、Inspector review issue 聚焦、issue target 行级高亮、Review Focus 清除定位、本地草稿跨刷新恢复、reset 清除草稿、左侧 subject activity stats、左侧 subject open/done stats、Review Queue open-only/全部模式、mock issue triage 确认 / 忽略 / 重新打开、triage 刷新恢复、triage-aware Slice Card badge 和 reset 清空均已做浏览器复验；schema-aware input 本轮通过 typecheck、目标 vitest 和浏览器 smoke，确认 text / ref select / number / JSON textarea / collection item select / before-after diff / kind-health filter / review issue inspector / issue target highlight / review focus strip / local draft persistence / subject activity stats / review queue navigation / mock issue triage / triage-aware slice filters / open-only review queue / triage-aware subject stats 控件可渲染。本轮仍不接真实 API，不改后端 DTO。

World Engine Workbench Preview 本轮补充：左侧顶部 `active / open / done` subject stats 已从只读摘要升级为本地快捷过滤按钮，可快速收窄左侧 subject 列表；该过滤不会修改中间 timeline 的 subject 选择，保持左栏查找和主画布世界视角分离。Review Queue 的 `只看 open / 全部 issue` 模式已从 Inspector 内部状态提升到页面顶层，并写入浏览器 mock 草稿；刷新后会保留用户当前的处理 / 回看模式，旧 v3 草稿缺省时默认回到 `open`。Sidebar 本轮继续增加当前状态过滤 chip 和 subject review 分布展示；subject 行会显示 `done / ok / ignored` 紧凑 badge，并在 title 中保留 `total / open / confirmed / ignored` 完整分布。Mutation Editor subject 视图的上 / 下一个相关 slice 导航增加范围切换：默认按当前 subject 全量轨迹跳转，也可切到当前 subject 过滤组合。Slice List 的 search / kind / status 过滤状态已从组件内部提升到页面顶层，并写入浏览器 mock 草稿；本轮进一步抽出 `world-engine-workbench-preview-filter.ts` 共享过滤 util，Slice List 和 Mutation Editor 的 `过滤组合` 导航共用同一套 subject / kind / status / search 判断，底部上 / 下一个会按当前主画布可见结果跳转。顶栏和 Slice List 工具栏现在会把 `search / kind / status / subject mode` 汇总成当前视角与 `当前筛选` chip，恢复浏览器草稿后用户可直接看到叠加过滤条件，并从主画布清除或切换。Slice List 本轮继续新增当前结果摘要条，展示 `visible slices / subjects touched / open slices+issues / review done / clean slices`，让用户切换过滤后直接判断时间线健康分布；顶部 subject mode 也改成上下文式，无过滤显示 `整体世界`，单 subject 显示单主体，多 subject 时才显示 `任一 / 全部 subject` 切换。Inspector 本轮新增当前切片 `Slice Health` 摘要，并把右侧信息顺序调整为 metadata -> 当前 slice health -> 当前 slice issues -> 全局 Review Queue -> subjects / snapshot；clean slice 不再让全局队列抢占当前上下文，open issue slice 会先显示当前问题再显示跨切片队列。Slice List 标题行本轮新增当前可见切片 stepper，显示 `current / visible total`，可在当前过滤结果内上 / 下一个切片浏览；subject / kind / status / search 过滤后不会跳出主画布可见结果。Inspector metadata 本轮增加 dirty 状态：未修改时显示 `已同步` 并禁用 `应用到预览`，修改后显示 `未应用修改`、启用应用并显示 `还原`，避免重复提交无变化 patch。目标 vitest 与 `bun run typecheck` 均已通过；浏览器验证改用 Chrome DevTools Protocol，已覆盖 metadata 初始同步态、修改 title 后 dirty 态、还原回同步态，以及上一轮的可见切片 stepper、Inspector health 和三栏摘要条验证。

World Engine Workbench Preview Draft Queue 补充：`/world-engine.workbench-preview` 的中间 Slice List 顶部新增统一 `Draft Queue`，按时间线顺序合并 Inspector metadata draft 与 Mutation Editor value draft。队列项显示 `time / title / kind / meta / value N`，点击后会清除可能遮挡目标的局部过滤、切到 `status=draft` 并选中对应 slice；草稿提交和清空仍留在 Inspector / Mutation Editor 内，主画布队列只负责发现与定位。本轮仍不接真实 API、不改后端 DTO；目标 vitest、`bun run typecheck` 与浏览器 metadata+value 草稿交互验证均已通过。

World Engine Workbench Preview metadata draft preview 补充：主画布现在不只显示抽象 `meta draft`，也会预览未应用 metadata 草稿内容。Inspector 上报的 `WorldWorkbenchPreviewMetadataDraftSummary` 已携带 `draftTime / draftTitle / draftSummary / draftKind`；Slice List 的 `Draft Queue` 优先显示草稿后的 time/title/kind 并带 `preview` 标签；Slice Card 在 metadata draft 存在时显示 `draft preview`、草稿标题/摘要/time/kind，并保留 `已应用：...` 原标题提示，便于用户区分预览态与已应用态。目标 vitest、`bun run typecheck` 和浏览器草稿标题预览验证均已通过；验证后已重置浏览器 mock 草稿。

World Engine Workbench Preview Inspector diff 补充：右侧 Inspector 的 Metadata 区块新增 `Metadata Draft Diff`，当 `time / kind / title / summary` 有未应用草稿时，会以 `field / applied / draft` 三列展示提交前差异；默认同步状态不显示 diff，点击 `还原` 后 diff 会消失并回到 `已同步`。这补齐了主画布 `draft preview` 之后的提交前审阅环节。目标 vitest、`bun run typecheck` 和浏览器 dirty/reset 交互验证均已通过。

World Engine Workbench Preview Draft Queue surface routing 补充：中间 `Draft Queue` 现在不只定位草稿 slice，也会直达对应处理面板。Slice List 通过 command props 调用页面层 `openInspectorPanel()` 与 `expandMutationEditorPanel()`；点击 metadata draft 会打开右侧 Inspector，点击 value draft 会展开底部 Mutation Editor，页面仍保留 `status=draft` 上下文。初版自定义事件方案在浏览器验证中不稳定，已收束为显式命令 props 并记录在 walkthrough。目标 vitest 与 `bun run typecheck` 通过；浏览器已验证 value draft 队列项可从收起态展开 Mutation Editor，metadata draft 隐藏 Inspector 自动打开路径因自动化无法稳定触发右栏隐藏控件，仍需后续人工或更稳定浏览器控制复验。

World Engine Workbench Preview Inspector toggle badge 补充：顶栏 `Inspector` 开关现在会显示 metadata draft 状态。页面新增 `metadataDraftSliceCount` 与 `inspectorButtonTitle`；存在未应用 metadata 草稿时，按钮显示 `meta N` badge，隐藏状态下使用琥珀提示态，title 会说明打开 / 隐藏 Inspector 时草稿可处理或会保留。目标 vitest、`bun run typecheck` 和浏览器 badge 交互验证均已通过，验证后已重置 mock 草稿。

World Engine Workbench Preview Draft Queue filter reset 补充：`Draft Queue` 顶部 `查看草稿切片` 现在调用 `showDraftSlices()`，会清空 search / kind / subject 过滤并切到 `status=draft`，与单个草稿项的定位行为保持一致，避免旧搜索词把草稿结果挡成空列表。目标 vitest、`bun run typecheck` 与浏览器搜索过滤场景验证均已通过，验证后已重置 mock 草稿。

World Engine Workbench Preview SegmentedControl 补充：公共 `app/components/common/form/SegmentedControl.vue` 已抽出互斥按钮组能力，支持 count、title、disabled、tone、icon 和 `aria-pressed`；`LowCodeRadioField` 已改为复用该组件。`/world-engine.workbench-preview` 的 subject mode、kind/status 过滤、Sidebar `active / open / done / value` 筛选、底部审查工作台 view / Review Queue mode / subject navigation scope 均已收敛到 `SegmentedControl`。Slice List 上方独立 `open / done / clean / draft` status 统计快捷卡已删除，status 只保留 filter toolbar 这一处，计数并入 status options。目标 vitest 与 `bun run typecheck` 均已通过；记录见 `docs/tasks/59-world-engine-workbench-redesign/walkthroughs/2026-06-20-segmented-control-refactor.md`。

World Engine Workbench Preview 面板控制补充：Sidebar 展开态收起按钮已移入 Schema 标题行右侧，不再额外占用一列；type filter 改用 common `FormSelect`；Preview 和真实 `WorldEngineWorkbenchDialog` 在 Inspector 隐藏后都会显示右侧恢复 rail；底部审查工作台收起 / 展开增加 height 动画。目标 vitest 与 `bun run typecheck` 均已通过；记录见 `docs/tasks/59-world-engine-workbench-redesign/walkthroughs/2026-06-21-panel-controls-and-select-refinement.md`。

World Engine Workbench 主题补充：全局 IDE theme token 已从单一 accent 扩展出 `info / success / warning / danger` 语义状态色，并同步 SSR fallback；第一轮优先精修 `sepia paper` 的低饱和状态色。真实 `WorldEngineWorkbenchDialog` 新增 `world-engine-workbench-theme` 作用域，header/body 共用从全局 `--status-*` 派生的 `--we-*`，顶部同步状态、error/notice、Draft 和主体系统待接入提示不再依赖硬编码 amber/rose/emerald。目标 vitest 与 `bun run typecheck` 均已通过；记录见 `docs/tasks/59-world-engine-workbench-redesign/walkthroughs/2026-06-21-semantic-status-theme-tokens.md`。

World Engine Workbench 审查工作台动效补充：底部审查工作台在既有 height transition 基础上，正文区域新增 `world-review-panel-body` Transition，展开 / 收起时增加 opacity 与 translateY 动画；preview 和真实 Workbench 因共用组件同步生效。目标 vitest 与 `bun run typecheck` 均已通过；记录见 `docs/tasks/59-world-engine-workbench-redesign/walkthroughs/2026-06-21-review-panel-body-transition.md`。

World Engine 第八十八轮补充：主 IDE Workbench Mutation Builder 已增加“复制所选”，可把当前选中的 mutation 克隆到下一位、自动选中副本并回填 Builder；mutation 列表复制已抽成 `duplicateMutationAt()` 纯函数，并有行为测试覆盖。

World Engine 第八十九轮补充：主 IDE Workbench Mutation Builder 已补齐 `list/collection itemType=object` 的顶层 JSON object 输入，schema shortcut 默认值会生成 `{}`，Builder 会显示 JSON textarea，并在提交前要求 value 必须是非数组 JSON object。

World Engine 第九十轮补充：主 IDE Workbench 的开放 object 行编辑器现在会继承根 object 的 `itemType` 渲染 value 控件和默认值；`itemType=object` 的开放 key 会显示 JSON textarea 并默认 `{}`，`ref(item)`、数值、布尔等 itemType 也会走对应控件。

World Engine 第九十一轮补充：开放 object 行编辑器的 `itemType` 继承现在只在 key 非空后触发，空 key 行保持普通 value 输入，避免新建空行时过早显示 JSON textarea。

World Engine 第九十二轮补充：开放 object 行编辑器现在会用 trim 后的 key 做 value 控件推导、固定 field 匹配和临时 attr 投影，避免 key 前后空格导致显示控件与最终写入不一致。

World Engine 第九十三轮补充：主 IDE Workbench Mutation Editor 顶部标题、载入所选 Slice、新建模式、编辑提示和 dirty guard 提示已拆为 `WorldEngineMutationEditorHeader`，父编辑器继续负责 slice 保存、dirty guard 状态和事件处理，`WorldEngineMutationEditor.vue` 文件体量回到 800 行以下。

World Engine 第九十四轮补充：主 IDE Workbench Mutation Builder 的默认 op 与默认 value 刷新已拆开，schema shortcut 仍会选择默认 op，但 attr / subject / value mode 刷新只更新 value，不会把用户手动选择的 `collectionRemove` 等合法 op 重置回 `collectionAdd`。

World Engine 第九十五轮补充：主 IDE Workbench Mutation Builder 现在会接收当前 State Query 结果；当用户选择 `collectionRemove` 且当前 subject 的 collection attr 在查询结果中是数组时，value 输入会切换为已有项下拉，便于从 `inventory` 等 collection 中选择要移除的值。Workbench 的 slice 与 state 查询快照已改用 `shallowRef`，避免递归 JSON 状态触发 Vue 类型深层展开。

World Engine 第九十六轮补充：`collectionRemove` 的已有项下拉现在会在候选项出现且当前 Builder value 不匹配候选集合时自动写入第一项，避免浏览器视觉上显示第一项、实际提交却仍是旧值或空值。

World Engine 第九十七轮补充：`collectionRemove` 候选项推导已从 Vue 组件抽到 `collectionRemoveValueOptions()`，并补充行为测试覆盖完整 attr key、点分嵌套路径、对象 collection 项格式化和非数组状态回退；`WorldEngineMutationBuilder.vue` 回落到 158 行。

World Engine 第九十八轮补充：主 IDE Workbench Mutation Editor 的 slice metadata、mutations JSON、校验提示和提交按钮已拆为 `WorldEngineSliceDraftForm`，Mutation Builder 通过 slot 保持在表单中间；父编辑器降到 796 行，后续继续扩展前仍建议优先拆分。

World Engine 第九十九轮补充：独立 Preview 的 World State、Subjects、Timeline 和 State Query 展示已拆为 `WorldEnginePreviewStatePanel`，页面继续负责 API 与表单状态；`world-engine.preview.vue` 从 799 行降到 746 行，避免后续扩展时反复踩 800 行限制。

World Engine 第一百轮补充：独立 Preview Builder 的默认 op 与默认 value 刷新已与主 IDE Workbench 对齐，schema shortcut 仍可填入默认 op，但 subject / attr 刷新只更新 value，不再把用户手动选择的 `collectionRemove` 等合法 op 重置回默认 op。

World Engine 第一百零一轮补充：独立 Preview Builder 现在也接收当前 State Query 结果，并在 `collectionRemove` 时复用 `collectionRemoveValueOptions()` 生成已有 collection 项下拉，同时自动同步第一候选值；Preview 与 Workbench 的 collection 删除辅助能力对齐。

World Engine 第一百零二轮补充：后端 `WorldEngineFacade` 已补 `collectionAdd` / `collectionRemove` 回归测试，覆盖 `collection ref(item)` 重复添加去重、删除已有 ref 项以及 queryState reduce 后只保留未删除项。第一百零三轮补充：后端已补齐 `itemType: object` 契约，schema loader 允许 `list/collection/object` 的 `itemType` 使用 `object`，但仍不允许 `scalar type: object`；service 层会要求对应 item value 必须是 JSON object，并用 facade 测试覆盖 object collection 稳定 JSON 去重/删除、object list append 与非 object item 拒绝。第一百零四轮补充：代码审查发现开放 object 子路径会把 `itemType: object` 临时解析成非法的 `scalar type=object`，现已修复为解析成 object 属性，并补 facade 测试覆盖 `memories.second` 这类开放 object 子值写入与非 object 值拒绝。第一百零五轮补充：HTTP API 测试已覆盖通过日历字符串写入 object collection/list/open-object 子值、按 stable JSON 删除 object collection 项、查询 state 以及非 object item 返回 400，证明浏览器入口背后的 API 边界也支持 object itemType。第一百零六轮补充：前端 `resolvePreviewAttrPath()` 与 Workbench object 行编辑器已对齐开放 object 的 `itemType: object` 语义，手写 `deepMemory.first` 这类路径会被视为 object 属性而不是 `scalar type=object`，并补 util 测试覆盖。第一百零七轮补充：独立 Preview Mutation Builder 已为 object/list<object>/collection<object> value 切换 JSON textarea，并在追加 / 替换 mutation 前用共享 util 校验必须是非数组 JSON object，减少浏览器试用时把普通字符串提交到后端才报错的情况。第一百零八轮补充：独立 Preview Mutation Builder 也支持手写 attr path，可输入 `memory.师门` 这类开放 object key，页面侧复用 `resolvePreviewAttrPath()` 推导 op、value hint、默认值和 object 校验；Preview 页面当前约 755 行。第一百零九轮补充：Workbench object value 校验改用共享 `isJsonObjectValue()`，移除本地重复判断，减少 Preview / Workbench object 校验语义漂移。第一百一十轮补充：`collectionRemove` 当前状态候选会保留 JSON-like 字符串类型，`"80"` / `"true"` / `"null"` 会作为 JSON 字符串字面量提交，避免被宽松解析误转为 number / boolean / null。第一百一十一轮补充：一键示例世界 schema 预检已改用 `previewAttrValueType()` 判断值类型，`list/collection` 会优先按 `itemType` 校验，错误或缺失值类型会在预检阶段提示。第一百一十二轮补充：Workbench enum 下拉已改用共享 `formatJsonInputValue()` 生成提交值，JSON-like 字符串枚举项会保留为字符串，不会被宽松解析误转类型。

部署最新补充：Windows Launcher 更新入口现在会查询 GitHub Releases，列出带 `neuro-book-windows-x64.zip` 和 `SHA256SUMS` 的 stable / canary / alpha / beta / rc 版本供用户选择，校验 SHA256，备份旧 `app/` / `launcher/` / root scripts 后切换新版并保留 `data/`；内置 `runtime/bun/` 在自动更新中保留当前版本，避免替换正在运行的 `bun.exe`。

GHCR / Product Docker 最新边界：app 镜像仍保留源码目录用于容器内排障，但 final image 使用 Bun runtime，不再携带或依赖根 `node_modules`。容器启动走 `scripts/deploy/docker-product-entrypoint.sh`，先用 `bun .output/server/scripts/db/prisma-migrate.mjs --deploy` 执行 SQLite migration，再用 `bun .output/server/scripts/deploy/product-start.mjs` 启动服务；`product-start.mjs` 会先调用 `.output/server/scripts/build/prepare-system-assets.ts --sync-user-assets` 做启动前 system assets / profile artifact 同步，通用 `.output` 后处理必须把该脚本复制进 `.output/server/scripts/build/` 并做缺失门禁；GHCR / 通用 `.output` runner 在无根 `node_modules` 时可用 `.output/server/package.json` 判定 Product Runtime 并读取版本，避免 profile artifact compiler / Profile Workbench worker 误入源码模式；管理员命令同样通过 `bun .output/server/scripts/cli/create-admin.ts` 运行产品内入口，部署机和容器启动时不执行 `bun install` / `bun run generate`。

模型 Provider 侧已把 `openai-compatible` 收敛为增强版 OpenAI 兼容 adapter，默认接收、标准化并回放 `reasoning_content`，覆盖 MiMo、DeepSeek 网关等 Agent thinking 场景；严格 OpenAI 官方接口使用 `openai-official`，避免把 provider extension 字段发给官方 OpenAI。

本文档记录仓库级现状。每次重大任务完成后，需要同步更新本文档和对应 active `docs/tasks/<order>-<task-slug>/README.md` 或 archived `docs/tasks/archived/<task-slug>/README.md`。

## Current Focus

- Inline AI 编辑链路已从旧 `/api/writing/continue` 迁到 Agent session：`NovelPromptBar` 重构为默认收起的 Inline AI Prompt Bar，只在 Markdown / 纯文本可编辑文件启用；TipTap selection menu 的 sparkles 改为“加入 AI 引用”，追加 `[[path#Lx-Ly]]` selection chip 和 hidden payload；`AgentChatSurface` 可自动创建 / 复用 `inline.editor` session 并发送 visible message + structured `input`；新增系统 profile `inline.editor`，使用 `read` / `edit` / `write` / `report_result` 直接修改目标文件。旧 writing continue API、DTO 和 OpenAPI SSE 特判已删除。
- Skills 最新补充：`anti-ai-slop` 当前定位为中文文本润色 skill，保持原目录和 CLI 命令不变；其步骤 3 已加入 stop-slop 风格的快速审查清单和 Directness / Rhythm / Trust / Authenticity / Density 五维 50 分评分，static/LLM 规则也已扩展到公式化设问、商务黑话、隐藏行动者、节奏单调、金句感和段尾机械升华等文本润色问题。
- RP writer 合同：`rp.writer` profile initial 保持为空；`rp.leader` 通过 `invoke_agent.message` 发送完整 Writer Brief。Writer Brief 已收敛为轻量 XML 骨架：`<context>` 内 Markdown 链接是唯一 read 白名单入口，`<materials>` / `<beats>` / `<style>` 承载素材、节拍和写作提示，并允许自定义语义 tag；`rp.writer` 收到 Brief 后自检，缺关键材料或缺输出路径时用 `report_result.result` 提问/报错，材料足够时写入 Brief 指定路径并用 `report_result.result` 汇报落点，不再使用 check/render 阶段输入或旧补充字段。
- 文件化 workspace：按 Project Workspace 隔离，根 `project.yaml` 是项目身份与展示元数据真值源；前端通过 SSE 订阅真实文件变化，保存时使用 `mtimeMs` 做冲突检测。
- Markdown Studio：收敛为 Notion-like 富文本模式 + 源码模式；Markdown 原文仍是唯一真相，TipTap 负责运行时 ProseMirror 富文本表面，表单 Markdown 编辑器也复用同一底层能力并只做工具栏包装。
- 前端 i18n：已接入 `@nuxtjs/i18n`，使用 `strategy: "no_prefix"` 保持现有 URL 不变；默认语言和 fallback 都是 `zh-CN`，首个非中文 locale 是 `en-US`。语言切换入口位于设置页 Browser State / 前端设定，选择写入浏览器 `localStorage["nbook.locale"]`，不写入 Project Workspace 或后端配置。已迁移 Dialog、JS Dialog API、FormSelect、API fallback、登录页、管理员用户页、Novel IDE Header、设置页与子面板主要固定文案、主 IDE shell 的通知 / 确认 / prompt 文案，以及 Agent、Workspace、Markdown Studio 和相关 composable fallback；后续继续按模块迁移 Plot、RAG 和 Profile Template Editor。
- 引用与内容节点：统一 inline ref、structured refs、content node 校验、warning 策略、`state.md` 当前状态，以及 `retrieval` / profile context memory 上下文策略；内容节点级 `inject.profiles` / `inject.always` 已硬切删除，profile-scoped context 进入 `agents/{profile}/context.md`，程序访问状态写入 `.nbook/context-access/{profile}.json` 并渲染 `agents/{profile}/generated.md`；`knowledge[]` 使用自然语言字符串并校验其中的 Markdown 内容节点链接；内容节点不再生成通用 `writingTip` frontmatter。
- 默认 Project 模板：`lorebook/` 已对齐当前 reference，默认生成 `world/`、`character/`、`location/`、`faction/`、`item/`、`event/`、`system/`、`instruction/` 和 `note/`；`manual/` 说明书目录采用跑团手册结构，默认包含 `README.md`、`world-guide.md`、`rules-guide.md`、`gm-guide.md`、`reference.md`、`player-guide/README.md`、`player-guide/character-creation.md`、`player-guide/playable-characters/README.md` 和 `player-guide/playable-characters/player.md`，用于快速开始、玩家手册、角色创建、预设角色入口、世界观导览、规则指南、GM 手册和主持人速查屏；默认模板现在为 `lorebook/`（显示为“世界书”）、`manual/`、`agents/`、`manuscript/`、`simulation/`、`reference/`、`upload/` 及关键二级分类目录生成 `directory-index` frontmatter，用中文 title 和 Lucide icon 驱动文件树展示，其中 `simulation/entities/` 显示为“实体”；初始化 note 收敛为 `story-concept`、`project-profile`、`opening-seed`，AI 使用说明进入 `instruction/creation-boundaries`，不再生成旧 `rule/writing-style`、`synopsis`、`project-positioning`、`theme` 或 `initial-plot-seed`。
- Agent 系统：主路径已硬切到 Pi-based `server/agent`。旧 v2 已归档到 `server/agent-v2` 与 `assets/agent-v2`，不再进入 typecheck、测试或运行时；旧 Prisma `AgentThread` / `AgentMessage` 数据模型已删除。新主路径使用 TypeBox profile contract、active TSX Profile DSL、`ProfileTurnPlan` harness 合同、Pi-compatible message/event/tool 语义、JSONL append-only session、动态 `.nbook/agent/profiles` catalog、`.nbook/agent/skills` SkillCatalog、`read/write/edit/apply_patch/bash` 基础工具、`create_agent/invoke_agent/get_agent/detach_agent` agent 工具，以及已迁移的 task / Plot / SQL 业务工具。普通 Project agent 的工具 cwd 统一为 Workspace Root `workspace/`，当前项目通过 `projectPath` 和 `AppendingSet` runtime reminders 表达；项目文件优先使用 `project-slug/...`，`workspace/project-slug/...` 仅作为兼容别名；user-assets agent cwd 仍是 `workspace/.nbook`。`RuntimeContext` DSL 已硬切删除，运行位置通过 `RuntimeLocationReminder` 写入 session，用户当前 Project Workspace / selected file 通过 `WorkspaceFocusReminder` 写入 session，Plan mode availability 也通过可见 reminder 写入 session，`ModelContext` 只保留真正 model-only 的动态上下文。独立 `skill` 工具已禁用，Agent 通过 `SkillCatalog` 的 `location` 用 `read` 打开 `SKILL.md`，reference 由入口文档按需继续读取。18 runtime pipeline/hooks 重构已落地第一批：新增 `SessionWritePlan` / `SessionWriteExecutor` 和第一版 `ToolSessionWriteSink`，session 文件布局硬切为 `.nbook/agent/sessions/<session-id>.jsonl`，follow-up queue DTO 对象化，waiting/resume lifecycle 和 partial assistant metadata 已接入，active `profile.ingest()` 合同已删除，`defineAgentRuntime({ hooks })` 已能在 `prepareRun` / `prepareTurn` / `ingestTurn` / `prepareNextTurn` / `settleRun` 执行自定义 hook并返回 write plans、runtimeState、runtimeMessages、requestOptions/toolKeys patch；`defineAgentRuntime()` 也已能展开内置 runtime bundle，普通 profile 默认 runtime 会得到 `builtin.profilePrompt`、`builtin.sessionContext`、`builtin.transcriptPersistence`、`builtin.reportResult` hook；其中 `builtin.profilePrompt` 已显式启用 profile prepare system prompt，且自定义 runtime 不组合时不会仅因 `prepare()` 返回 `systemPrompt` 注入 provider 或 snapshot；`builtin.transcriptPersistence` 已显式返回 `transcript: "persist"`，且没有 transcript hook 时不再隐式持久化 assistant/toolResult；`builtin.reportResult` 已显式启用缺失 report_result 的 reminder retry，自定义 runtime 不组合时不会仅因 `allowedToolKeys` 包含 `report_result` 自动提醒；自动 compaction 已统一由 profile 顶层 `compaction` 配置显式启用，没有 `compaction` 配置时手动 `/compact` 报错，自动路径只在上下文超过模型窗口时返回明确错误；`builtin.sessionContext` 已先控制 `prepare().modelContextMessages` 注入 provider context；其他 built-in hook 当前作为后续行为迁移锚点，active 默认行为仍由 harness 现有路径提供；hook `runtimeState` 按 hook name namespace 隔离，同名 hook 返回对象时会浅合并；hook context 现在暴露 typed `ctx.initial` 和只读 `ctx.session` facade，可通过 `ctx.session.read()` 读取当前或其他 session，并通过 `ctx.session.agentDialogueContent()` 从 source active path 构造 Agent Dialogue Content；`ingestTurn` 支持 `transcript: "runtime_only"` 跳过默认 assistant/toolResult 落盘，同时保留同 run 的 `report_result` / `settleRun` 能力，缺失 `report_result` 的 reminder 在 runtime-only 下也不会写入 session history。`RunFrame` / `TurnSnapshot` / `TurnOutcome` / `RunLoopResult` 已落地且类型边界已抽到 `server/agent/harness/run-kernel-types.ts`，其中 `RunLoopResult` 是 `completed | waiting | failed` 判别联合，failed 分支携带结构化 `InvocationErrorInfo`；Run Kernel stage error phase 包装已抽到 `server/agent/harness/run-kernel-error.ts`，ingest/compaction/model stage 抛错时会保留更准确的 lifecycle error phase；Turn Transaction outcome 应用已抽到 `server/agent/harness/turn-transaction.ts`；turn continuation / shouldStop 纯判定已抽到 `server/agent/harness/turn-continuation.ts`，partial failure helper和 failed ingest draft 已抽到 `server/agent/harness/turn-failure.ts`，RunFrame 状态写回已抽到 `server/agent/harness/run-frame-state.ts`，prepare-next-turn reducer 已抽到 `server/agent/harness/prepare-next-turn.ts`；`runLoop()` 已改为逐轮创建冻结 TurnSnapshot、执行 TurnOutcome、再进入 ingest；tool-side `savePoint` writes 会排入 RunFrame并与本轮 transcript 同 batch flush，`immediate` writes 仍实时发布；自动 compaction 已迁到 turn save point 后、下一轮前，触发后会重建 RunFrame context，启用入口是 profile 顶层 `compaction` 配置而不是 runtime built-in。缺失 report_result 的 reminder retry 已迁到同一个 RunFrame 的 continuation / next-turn边界，不再由 finalize 递归重跑旧 loop。harness active 路径中的 linked-agent entry、tool custom state、profile prepare writes、variable patch audit、client variable ack 和 compaction entry 已通过 executor 发布。17 session title/summary summarizer 已按 18 runtime hooks 落地：builtin profile key 硬切为 `summarizer`，旧 `session.summarizer` 源文件和 compiled artifact 已删除；source profile 可声明 `summarizer` companion，harness 在 source invocation completed 后后台 coalesced 调度 hidden system summarizer session；summarizer 在 `ModelContext` 中从 source active path 构造 Agent Dialogue Content，使用 `runtime_only` transcript，不保存自身 assistant/toolResult 历史；成功后通过 active-leaf scoped projection 写回 source `title` / `summary`，并把 running/dirty/error/token 状态投影到 session snapshot。`writer` 已硬切为“一章节一 agent”，输入使用 `chapterPaths` 和 `lorebookEntries: string[]`，自动展开本章 Chapter Plot 并只写显式章节 `index.md`；writer 文风/参考资源已迁到 Project `agents/writer/{styles,references}`，系统默认资源位于 `agent/profiles/builtin/writer.home/{styles,references}`。正式 HTTP 和前端抽屉已迁到 `/api/agent/sessions/**` 的 session/invocation/event contract，旧 thread/subagent 前端心智已替换为 session/linked agent；临时 `/api/agent-v3/**` 和旧 `/api/agent/threads/**` 已删除。
- Agent runtime 最新补充：compaction 配置已从 Profile DSL `<Compaction>` 节点迁到 `defineAgentProfile({ compaction })` 顶层静态配置；`ProfileTurnPlan` 不再携带 compaction。自动 compact 成功后，harness 会在下一轮前重新注入一次 `HistorySet` 初始化消息，再重建 `RunFrame.messages`。compaction planner 的 recent cut 现在按模型可见 entry 计算，包含普通 `message` 与 visible `custom_message`；summary 输入默认只包含普通 `message`，避免把 HistorySet/Import/system-reminder 复述进摘要。builtin profile 顶层 `compaction: {}` 统一走默认值：`enabled: true`、`reserveTokens: 25_600`、`keepRecentTokens: 24_000`。
- Agent runtime 最新补充：`builtin.sessionContext` 现在不仅控制 `prepare().modelContextMessages` 注入 provider context，也控制 `historyInitMessages`、`modelContextAppendingMessages`、`appendingMessages` 是否写入 session history；`stateWrites` 仍作为 profile 私有状态写入。
- Agent runtime 最新补充：`ProfileTurnPlan` 持久化编译已抽到 `server/agent/harness/prepare-run.ts` 的 `compilePrepareRunWritePlan()`；`prepare()` 现在只返回 `{ plan, writePlan }`，不再直接执行 session 写入；`invokeAgent()` 在 `prepareRun` 阶段统一执行 profile prepare write plan，后续可把 Default Persistence Hook 接到更明确的 `prepareRun` reducer。
- Agent runtime 最新补充：`builtin.sessionContext` 已从静态扫描迁到可执行 `prepareRun` built-in hook；`prepare()` 和首轮 `modelContextMessages` 注入都读取 `prepareRunHooks.sessionContext`。`builtin.reportResult` 也已改为可执行 `prepareRun` built-in hook，缺失 `report_result` 的 reminder retry 从第一轮 continuation 判定开始读取 `prepareRunHooks.reportResultReminder`，不再静态扫描 profile runtime。
- Agent runtime 最新补充：`invokeAgent()` 内的 prepareRun 编排已抽成私有 `prepareRun()` helper，集中处理 resolution restore、prepareRun hooks、profile prepare/write plan、pending prompt 写入和首轮 RunFrame 输入组装；入口主流程更接近 Coordinator -> prepareRun -> runLoop -> settleRun -> cleanup。
- Agent Plan Mode 最新补充：计划目录已收敛为当前 Project Workspace 的 `.agent/plan/`；普通 Project agent 从 Workspace Root cwd 写计划文件时使用 `<project>/.agent/plan/<slug>.md`，`exit_plan_mode.planFilePath` 仍传 Project Workspace 相对 `.agent/plan/<slug>.md` 供审批 UI 预览。用户手动关闭 Plan Mode 会写入 exit reminder，不再让下一轮模型继续看到 “Plan mode still active”。
- Agent runtime 最新补充：accepted invocation 的 terminal 处理已抽成 `completeInvocation()` / `failInvocation()` helper，单轮 turn 的执行事务也已收敛到 `runTurnTransaction()`；active leaf 移动新增 `SessionWritePlan.moveLeaf` op，retry/tree/empty 控制面写入现在也走 executor；follow-up queue 会通过 `agent.followUpQueue` projection custom state 恢复 snapshot；SSE public event 已硬切为 projected runtime event，`kind: "pi"` 改为 `kind: "runtime"`，`session_state_changed` 只携带 lightweight live state，`agent_end` / `turn_end` 不再携带完整 messages/toolResults，`InvokeAgentResult.events` 已删除；旧后端 `session/slash-commands.ts` 已删除，slash command 只保留前端识别到正式 command/tree HTTP 入口这一条路径；compaction 模块已硬切为调用方注入写入函数，不再自行 append repo。
- Agent runtime 最新补充：invocation 现在带内部 caller identity（`user | agent | sidecar | system`），缺省为 `user`；HTTP 用户入口固定 user caller，不暴露可伪造的内部身份。`invoke_agent` 工具会把 source session/profile/toolCallId 写成 agent caller，summarizer / follow-up 等内部触发写成 system caller，sidecar run 使用 sidecar caller 但保持原有 sidecar reminder 关闭策略。`builtin.reportResult` 已按 caller identity 控制缺失 `report_result` reminder：用户直聊默认不提醒，非用户 caller 且目标 profile 暴露 `report_result` 时继续通过 continuation/next-turn 注入 reminder。`simulator.leader` 已新增 `bash`、移除 `report_result`，弱化 OutputSchema 为普通文本合同，并通过 `reference/agent/workspace-tool-use.md` 共享 workspace tool-use 规则；它现在作为 `simulation/` runtime owner 维护 subjects/entities/runs、调度 actor simulator，并用普通 Markdown handoff 返回。
- Agent initial/payload 最新补充：profile DSL 创建期合同已从 `inputSchema` 硬切为 `initialSchema` / `InitialSchema`，runtime context 使用 `ctx.initial`；每轮 invocation 可通过可选 `payloadSchema` / `PayloadSchema` 接收结构化 payload，profile 读取 `ctx.invocation.payload` 和 `ctx.invocation.message`。HTTP/API invoke 增加 `title` 与 `input` payload，agent-facing `invoke_agent` 支持 `prompt | continue | steer | followup`、`message?: string` 和 `input?: object`，返回统一 `result.message/data` 与简洁 `stats`。`get_agent_profile` 保持 `{ profileKey }` 参数，只向 agent 暴露 `InitialSchema`、`PayloadSchema`、`OutputSchema` 和 `toolKeys`。
- Agent profile tool API 最新补充：profile 作者 API 已从 `defineProfileTools({...})` / `tools.*()` 硬切为 `toolset(builtin...)`。内置工具引用统一进入分组 `builtin`，profile 自带工具使用 `defineProfileTool()`，插件引用使用 `pluginTool()`；顶层 `toolKeys` 现在表示主 run 执行工具子集，运行时 root 最大工具集合改名为 `rootToolKeys`，provider-visible tools/schema 继续来自 root 集合，sidecar 仍用自己的 `toolKeys` 收窄执行权限。
- Agent profile 最新补充：`leader.default`、`leader.assets`、`rp.leader` 和 `simulator.leader` 的创建 initial 已统一为空对象。当前 Project 归属由 session `projectPath` / `WorkspaceFocusReminder` 表达，`rp.leader` 与 `simulator.leader` 根据当前 Project 推导 `manual/` 和 `simulation/` 路径；前端默认 profile 新建入口继续传 `initial: {}` 即可创建这些 leader session。
- Agent profile 最新补充：新增共享职责地图 `reference/agent/profile-routing.md`，并由 `leader.default`、`leader.assets`、`rp.leader`、`simulator.leader` 在 `HistorySet` 导入。入口 leader 现在能看到常用 profile 的简短职责、适合/不适合任务和错位转场建议；当当前任务明显不属于自身职责时，应说明原因并建议用户新建或切换到对应 agent。
- Agent session 最新补充：历史 session 读取已兼容缺失或不可运行 profile。`listSessions`、snapshot、live state 和 relations 会返回 `summary.profileAvailability` / `profileIssueMessage`，profile 缺失时 pending approval 仅降级识别全局审批工具；真正会继续运行或复制运行依赖的入口（prompt/continue/steer/followup/resolution、`new`、`compact`、`tree + invoke`）会拒绝并提示恢复 profile 或新建/切换其他 profile。前端在当前 session 不可运行时进入只读历史模式，禁用发送、Plan Mode、模型/思考参数、compact、编辑保存和重跑等会影响后续运行的动作，同时保留查看历史、复制、切换分支、归档和关联 agent 面板。
- Agent runtime 最新补充：`SidecarProfilePass` V1 已进入 Harness 核心机制，profile 可声明 `prepareRun` / `settleRun` 旁路；sidecar 沿用当前 session/profile/session tree，在父 run 当前节点 fork 旁路 leaf 执行，sidecar transcript 持久化到该旁路分支但不成为主 active path，并通过 `merge()` 注入 runtimeMessages、persistedMessages、runtimeState 或 writePlans。`runtimeMessages` 只进父 run 模型上下文；`persistedMessages` 写入父 session active path，第一版只允许 user message、origin 为 `harness`，`prepareRun` 合并后若 provider-visible context 超出模型窗口会直接失败。provider-visible tools 和 schema 保持 profile 最大集合以保护工具/schema 缓存，sidecar `toolKeys` 作为执行权限子集且只能引用 root `tools`；`report_result.data` 只用于主路结构化结果且保持 optional，旁路必须通过 `report_sidecar_result({ result, data: { [sidecarName]: payload } })` 返回；`report_sidecar_result.data` 的模型可见 schema 是按 sidecar name keyed 的 profile-stable union，执行期再按 active sidecar 精确校验 `data[activeSidecar.name]`。`simulator.actor` 已接入 `actor.context-load` / `actor.memory-save`，创建 input 只接收 `{ subjectPath }`；`actor.context-load` 把 actor-safe context 写入 `report_sidecar_result.result`，`data` 只返回 `{ "actor.context-load": {} }`，并在主 run 前持久化注入 `<actor-sidecar-context>`；`actor.memory-save` 在主 run 后维护 subject-facing `events.jsonl`、`memory.jsonl` 与 `mind.md`，`data` 同样只返回 keyed 空对象。Subject RAG 第一版使用 `{project}/.nbook/subject-rag.sqlite` 作为 SQLite + sqlite-vec 可重建缓存，事实源仍是 subject JSONL 文件；`subject_rag_search` 读取顶层 `embedding` 服务配置，Node/Nitro runtime 使用 `node:sqlite` 加载 sqlite-vec，Bun smoke 继续可用，写入工具只标 dirty，search 前同步重建 dirty source；Agent 工具层必须显式传单一 source `["events"]` 或 `["memory"]`，不默认双搜也不允许一次搜两层；Global 配置 provider/model/API Key/baseURL/dimensions，Global 启用但模型/维度/timeout 留空会写默认 `text-embedding-3-small` / `1536` / `30000ms`，Project 只覆盖 model/dimensions，未配置时明确报错，不做关键词 fallback，embedding 超时会明确失败而不是让工具永久执行中。`subject_memory_update` 会把 subject-facing facts 数组交给真实 `memory.curator` profile 生成 JSON Patch，工具层应用、校验并写回 `memory.jsonl`。IDE 左侧已新增 Project 级 `RAG` 可视化入口，可浏览当前 Project subjects 的 events/memory、执行真实 subject-scoped RAG 搜索、重建索引，并对 JSONL 事实源做基础 CRUD。
- Agent profile 最新补充：profile 工具合同已硬切到 root `tools` binding，生产代码不再接受 `allowedToolKeys` / `mainRunAllowedToolKeys` / `mainRunToolKeys`。`tools: toolset(builtin...)` 决定 profile 最大工具集合和模型可见 schema；顶层 `toolKeys` 收窄主 run 执行权限，sidecar `toolKeys` 收窄旁路执行权限，二者都只能引用 root tools；runtime `prepareTurn.turnSnapshotPatch.toolKeys` 也只裁剪执行权限，不改变 provider-visible tools/schema。`report_result` 现在只从 profile root binding 和主路 `OutputSchema` 生成 profile-stable provider-visible schema；`report_sidecar_result` 从全部 sidecar `sidecarDataSchema` 生成按 sidecar name keyed 的 profile-stable provider-visible union schema。active sidecar 必须通过 `report_sidecar_result.data[activeSidecar.name]` 返回旁路 payload，缺 key、传错 key、额外 key 或 payload 类型不匹配都会在 tool execution 阶段产生模型可见 error toolResult 并允许同一 run 修正。`create_agent.initial` 保持任意 JSON object，不做 profile-aware union，也不再把 JSON string 静默 normalize；模型应先调用 `get_agent_profile` 查看目标 `InitialSchema`，再传真实对象。`invoke_agent.input` 是每轮 payload object，按目标 `PayloadSchema` 校验；未声明 `PayloadSchema` 的 profile 收到 payload 会报错。
- Agent profile settings 最新补充：profile 可通过 `settingsForm: defineLowCodeForm(...)` 声明自有可视化设置，运行时从 `ctx.settings` 读取 defaults、Global Config patch 与 Project Config patch 合并后的值。第一版低代码 form 支持 TypeBox 类型校验、默认值、动态 options、自定义校验，以及 `text` / `textarea` / `number` / `switch` / `select` / `combobox` / `radio` / `checkbox` 组件；field path 限制为顶层字段，`checkbox` 表示多选且 option value 只支持 `string | number`。设置页的“Agent Profile 模型”面板已扩展为模型参数 + Profile 预设配置，Project scope 按字段展示“继承 / 覆盖”。保存时服务端严格校验非法 settings，运行时遇到损坏 stored settings 会回退 profile defaults。
- Writer profile 最新补充：普通 `writer` 已从“一章节一 agent”改为长期可复用正文写作工位，创建期 `initial: {}`；每轮 `invoke_agent.message` 承载写作任务，`invoke_agent.input` 按 `WriterPayloadSchema` 传唯一写入 `path` 与建议读取的 `threadIds/sceneIds/plotIds/lorebookEntries/readablePaths`。writer prepare 只渲染 `<target_file>` 与 `<suggested_context>`，不再自动读取 Chapter Plot、lorebook 或 readablePaths；writer 可按需使用 `get_story_thread`、`get_story_scene_context`、`get_story_plot_context`、`get_chapter_plot` 和文件 read，并只写 `payload.path`。
- Agent profile 最新补充：结果工具拆分的缓存稳定版已补齐残余边界。缺失结果 reminder 现在按当前 turn `executionToolKeys` 判断是否能执行期望结果工具，不再被 provider-visible root tools 误触发；sidecar 中错用 `report_result` 连续失败时，Runtime Error 摘要按期望的 `report_sidecar_result` 收口并保留真实工具错误；`final_message_as_result` 只允许明确 string `sidecarDataSchema`，union/object 等复杂旁路结果必须使用 `parse_final_message_json` 或 `report_sidecar_result`。
- Agent profile 最新补充：工具定义层已收敛到底层 `AgentToolDefinition` / `defineAgentTool()`，profile 作者侧通过 `defineProfileTool()` 暴露同一能力。内置工具通过 definition `.runtime()` 汇总注册；profile root `tools` 可通过 `toolset(builtin...)` 引用轻量内置 binding、直接内联 profile 自带 definition，或用 `pluginTool(key)` 引用已注册插件工具。Harness 解析工具时先查当前 profile 私有 definition，再回退全局 registry；自带工具作用域限定在当前 profile run，自带审批工具复用现有 suspend + resolution toolResult 语义。工具聚合入口已整理为 `server/agent/tools/index.ts`，控制工具与 agent 协作工具分文件维护；`createBuiltinTools()` 为无参入口，agent 协作工具只支持 session context 内的 `executeWithContext`。
- Agent runtime 最新补充：TSX Profile DSL 新增显式 `Import` string fragment，profile 可在 `System` 或 `HistorySet > Message` 中加载共享文本上下文；V1 只允许 `AGENTS.md`、`reference/**`、`docs/**`，支持 Markdown heading 截取、UTF-8 `maxBytes` 截断和 fenced block 输出，缺失文件默认返回空字符串，`required=true` 时保持严格。`leader.default` 已通过 `Import` 加载 `reference/agent/leader-default.md`、`reference/content/markdown-dialect.md` 和 `reference/agent/project-workspace-guide.md`，把工具、Task Management、多 Agent、SQL、Plan Mode、Skills、Markdown 扩展和 Project Workspace 文件语义从长 system prompt 拆到稳定参考；Profile Workbench 已能解析、展示和拖入 `Import` 节点；Nitro build 后会复制 `AGENTS.md`、`reference/`、`docs/` 到 `.output/server/`，保证 compiled runtime 可解析导入。
- Agent profile 最新补充：新目录规范后的 prompt engineering V1 已落地。`simulator.leader` 成为写作模式和 RP 模式共用的世界模拟主管，负责 simulation 裁决、subject 调度、state commit 建议、writer-safe brief 和 director handoff；`director` 成为剧情导演，负责 Thread / Scene / Plot 设计和 Plot System 落库，不写正文也不维护 simulation state；`simulator.actor` 作为唯一 subject simulator profile，旧 actor legacy profile 已删除。Plot 工具新增 `create_story_plots`，支持同一 Scene 批量追加行动级 Plot；Plot Agent Spec 已沉淀到 `reference/plot/agent-spec.md`，写作 workflow skills 和 agent reference 已同步使用 `simulator.leader` / `director` / `simulation/` 口径。
- Agent profile 最新补充：RP 正文归属已收紧为“所有世界内用户可见正文都由 `rp.writer` 写”。`rp.leader` 仍负责开局引导、化身确认、IC/OOC 审查、调用 `simulator.leader` 和编写 Writer Brief，但不直接撰写开场白、初始化 prose 或常规 Tick prose；开场白 / 初始化正文固定通过 `rp.writer` 写入 `simulation/runs/ticks/000000-initial-state/prose.md`，`rp.leader` 最终只组装正文链接和元场景引导。
- User-assets 同步已改为黑名单式 managed sync：`assets/workspace/.nbook` 下除本地状态、session、compiled artifact、profile metadata 和专用 profile/variable 源外的大部分系统资源都会进入 `.profile-sync-state.json` 跟踪，包含 `agent/skills/**`、`templates/**`、`agent/profile-templates/**`、`agent/bin/**`、`agent/scripts/**`、`agent/config/**` 和 `agent/profiles/builtin/writer.home/**` 默认资源；未手改用户覆盖会随系统更新自动更新，已手改且系统更新时保留用户文件并通过 sync warning / Diff Workbench 查看差异。系统删除传播第一版不自动删除用户旧文件。
- Config 系统：配置已拆为 Boot Config、Global Config、Project Config 和 Browser State。Boot Config 使用根 `config.yaml`；Global Config 使用 `workspace/.nbook/config.json`；Project Config 使用 `workspace/{project}/.nbook/config.json`；正式 API 是 `/api/config/*`，旧 `/api/settings/*` 与 `/api/workspace-settings` 已删除。
- 外部小说素材：`novel-import-tomato-reference` 第一版支持免费小说 epub 与 Tomato Novel Downloader 本地结果导入到当前 Project Workspace 的 `reference/tomato/`，用于后续拆书和结构分析；SillyTavern 导入使用 canonical skill `novel-import-silly-tavern-card`，CLI 位于同名 skill 目录。CLI 支持本地 `.json` / `.raw.json` / best-effort `.png` inspect，默认把稳定文本从 `reference/silly-tavern/` 导入到 `lorebook/character`、`lorebook/location`、`lorebook/faction`、`lorebook/species`、`lorebook/world/rule`、`lorebook/system`、`lorebook/item`、`lorebook/event` 或 `lorebook/note`；混合职责条目不自动拆分，低置信稳定设定进入 pending note，动态 worldbook 条目归档到 `reference/silly-tavern/{slug}/dynamic-worldbook/`，卡片主体开场素材归档到 `reference/silly-tavern/{slug}/card-body/`，`--rp` 额外归档动态机制到 `reference/silly-tavern/{slug}/simulation-migration/`，不创建 `simulation/subjects`、`simulation/entities` 或 `simulation/runs`。
- 文档治理：把稳定参考、调研资料、任务 walkthrough 分开维护。
- 全站鉴权：默认启用的 cookie session 登录、管理员用户管理页、主界面右上角头像菜单、登录失败限流、复杂密码生成、Global Config `auth.enabled` 开关和初始管理员创建脚本已经接入。

## Module Status

| Module | Status | Notes |
| --- | --- | --- |
| Workspace | Active | 已转向 portable Project Workspace，术语以 [reference/workspace/TERMS.md](reference/workspace/TERMS.md) 为准：Workspace Root 默认 `workspace/`，Project Workspace 默认 `workspace/{project}/`，根 `project.yaml` 是项目唯一真相源。内容节点模板和默认 Project 目录模板已切到 Bundled Workspace Template `assets/workspace/.nbook/templates/...`，用户覆盖层是 `workspace/.nbook/templates/...`；`workspace project create` 默认使用 `project-directory-templates` 创建 Project Workspace、写入 `project.yaml`、初始化 Project SQLite，并随默认模板生成 `simulation/`；支持 `--target <dir>` 指定实际 Project Workspace 目录，目标已存在且显式传 `--template project-directory-templates` 时可补齐缺失模板文件；manuscript 模板使用 `manuscript/001-volume/001-chapter/` 的 volume / chapter 层级；Project Workspace 与 user-assets 文件树 snapshot 由统一 index manager 持有 watcher、dirty/rebuild、revision 和 subscriber，SSE 只订阅 index 更新；文件树和干净编辑器会响应真实文件变化，网页/磁盘同时修改时走保存冲突对话框。 |
| Markdown Studio | Active | 主编辑器只保留富文本和源码两种模式；图片、inline code、workspace/domain reference 和表格在富文本侧使用真实 ProseMirror node / mark，并序列化回 Markdown；`StructuredTextEditor` 不再维护独立 schema，只作为表单工具栏包装层。 |
| Config | Active | 新配置真值源为 `/api/config/*`：Boot Config `config.yaml` 只放启动/部署字段，Global Config `workspace/.nbook/config.json` 保存 auth/models/agent/UI/editor 长期偏好，Project Config `workspace/{project}/.nbook/config.json` 只覆盖 registry 允许的字段；Provider API Key GET 脱敏、PUT 支持保留/清空/覆盖。设置 Dialog 已升级为配置中心，可显式切换 Global Config / Project Config / Browser State，并能选择任意 Project Config 编辑默认模型、默认 Profile、Agent Profile 共同默认模型参数和单个 Profile 模型覆盖。 |
| Auth | Active | 默认开启的全站 cookie session 鉴权已接入；Global Config `auth.enabled` 可关闭整站守卫；包含登录页、管理员用户管理页、主界面账户菜单、登录失败限流、复杂密码生成、用户状态/角色/密码重置和初始管理员脚本。 |
| Reference | Active | 文件化内容节点 refs 不再承载 visibility；Agent prompt 已收敛为 inline ref 表达自然提及、structured refs 表达稳定系统关系；Plot refs 已迁到内容节点路径，不再依赖数据库 Lorebook。 |
| Plot / Story | Active | 剧情系统已迁入 Project SQLite：每个 Project Workspace 的 `.nbook/project.sqlite` 保存 Story / StoryPhase / StoryThread / StoryScene / StoryPlot / StorySceneRef，不再依赖 App SQLite 的 `Novel` / `novelId`。refs 指向设定/角色/地点时使用 `lorebook/.../` 内容节点路径，剧情内部对象仍使用 thread/scene/plot URI；数据库不再维护 `Volume` / `Chapter` 表，Scene 挂章使用 `chapterPath` 指向 `manuscript/.../` content-node 目录。Plot 入口会把 Agent 常见的 `project-slug/manuscript/...` / `workspace/project-slug/manuscript/...` 归一为内部 `manuscript/...`，但持久化值仍保持 Project Workspace 相对路径。 |
| Agent v2 | Archived | 旧实现已移动到 `server/agent-v2` 与 `assets/agent-v2`，只作为迁移参考，不进入 active typecheck、测试或运行时。旧 `AgentThread` / `AgentMessage` Prisma 模型已删除。 |
| Agent | Active | Pi-based harness 已成为 `server/agent` 主实现：TypeBox profile、active TSX Profile DSL、`ProfileTurnPlan`、动态 `.nbook` profile/skill catalog、JSONL session、slash command、approval resume、linked agents、compaction、Pi 风格基础文件工具、agent 工具、task 工具、Plot 工具、SQL 工具和真实 provider smoke 均已跑通。18 runtime pipeline/hooks 第一批已接入 `SessionWritePlan` / `SessionWriteExecutor`、`ToolSessionWriteSink`、扁平 session 文件路径、follow-up queue 状态、waiting/resume lifecycle、partial assistant metadata、内部 `RunFrame` / `TurnSnapshot` / `TurnOutcome` / `RunLoopResult` 和最小 `defineAgentRuntime({ hooks })` runner；缺失 report_result 的 reminder retry 已迁到同一个 RunFrame 的 continuation / next-turn 边界，`report_result` 模型可见字段已从 `walkthrough` 严格改为 `result`，`ingestTurn` 支持 runtime-only transcript，且 runtime-only 下的 report_result reminder 不落 session，`prepareRun` / `prepareNextTurn` hook 已能注入不落 session 的 runtimeMessages，`defineAgentRuntime()` 已能展开内置 runtime bundle，默认 session runtime 已有 built-in hook，profile 顶层 `compaction` 配置是 compaction 唯一启用入口，`builtin.profilePrompt` 已显式启用 profile prepare system prompt，且自定义 runtime 不组合时不会注入 provider 或 snapshot；`builtin.transcriptPersistence` 已显式返回默认 persist，且自定义 runtime 不组合 transcript persistence 时不会隐式落 assistant/toolResult；`builtin.reportResult` 已显式启用缺失 report_result 的 reminder retry，自定义 runtime 不组合时不会仅因 `allowedToolKeys` 包含 `report_result` 自动提醒；没有 `compaction` 配置时手动 `/compact` 报错，自动路径只在上下文超过模型窗口时返回明确错误；`builtin.sessionContext` 已先控制 `prepare().modelContextMessages` 注入 provider context，其余 built-in hook 仍作为后续行为迁移锚点；hook runtimeState 按 hook namespace 隔离并支持对象浅合并，hook context 已暴露 typed `ctx.initial` 和只读 `ctx.session.read()` / `ctx.session.agentDialogueContent()` facade，Run Kernel 类型边界已抽到 `server/agent/harness/run-kernel-types.ts`，Run Kernel stage error phase 包装已抽到 `server/agent/harness/run-kernel-error.ts`，Turn Transaction outcome 应用已抽到 `server/agent/harness/turn-transaction.ts`，turn continuation / shouldStop 纯判定已抽到 `server/agent/harness/turn-continuation.ts`，partial failure helper / failed ingest draft 已抽到 `server/agent/harness/turn-failure.ts`，RunFrame 状态写回已抽到 `server/agent/harness/run-frame-state.ts`，prepare-next-turn reducer 已抽到 `server/agent/harness/prepare-next-turn.ts`，tool-side savePoint writes 会和本轮 transcript 同 batch flush，自动 compaction 已迁到 turn save point 后、下一轮前并会重建 RunFrame context，`settleRun` hook 已能读取正常 run 的 `reportResult` 并写最终 projection。旧 `profile.ingest()` active 合同已删除，默认普通 profile 行为暂仍由旧 loop提供，完整独立 Run Kernel 模块和内置 hook bundle 仍在 18 后续。正式 HTTP 入口是 `/api/agent/sessions/**`；前端 Agent 抽屉已迁到 session snapshot + event hub + command/tree/invocation contract，保留聊天、停止、审批恢复、模型选择、Plan Mode、`/compact`、linked agents、edit/retry/rollback/fallback 的核心入口；`/clear` 现在通过 tree 清空同一 session 的 active leaf。Session 选择弹窗已升级为带 profile/status/relation/limit 筛选的近期 session 管理入口，默认只显示 leader profile；linked agent 面板现在同时显示当前 session 绑定出去的 agent 和绑定当前 session 的上游 agent。Harness 已支持运行中显式 `steer` / `followup`：`steer` 在 safe point all drain 并继续当前 ReAct loop，`followUp` 在 loop 结束后 one-at-a-time 开启 fresh loop；前端 Composer 已接入“引导/队列”按钮语义、Ctrl/Meta 快捷入口、pending queue chips 和成功通知。Harness 运行错误会写入 `invocation_lifecycle.errorInfo`，前端投影为模型不可见的 Run Error 卡，并用 invoke HTTP 返回做兜底通知。变量系统第一版已接入：invoke 传 `clientState`，`ctx.initial` 回到 profile 静态输入，Profile 通过 `ctx.vars` / `<Variable>` / `<VariableSchema>` 读取 `client/global/project/session` 变量，profile 内 `variableDefinitions` 可随 profile artifact 注册 `session.*` 变量，`variable_schema` / `variable_read` / `variable_patch` 已注册为 Agent 工具并按注册 schema 校验读写；`ctx.workspace` / studio 初始化旧入口已从 active 代码删除，`leader.default` 的 Project Workspace 和 Plot Focus runtime 提醒已改为普通内联 `Reminder`。`client.*` 前端写回 ack 管线和 Workspace Root / Project 变量 definition `.compiled` 仍是后续 TODO。active user profile 覆盖已通过 `.system-profile-metadata.json` / `.profile-sync-state.json` 同步到最新版，`leader.default` 实际运行时可见 `get_agent_profile`；writer 已硬切为 `chapterPaths` 单章节合同，retrieval 输入已精简为 prompt-only，输出 `{ entries, note? }` 给 Leader 判断，Leader 调 writer 时只传 `entries[].path` 数组；user-assets 同步现在默认管理 bundled `.nbook` 大多数资源，包含 skills、templates、profile-templates、bin/scripts/config 和 `agent/profiles/builtin/writer.home/**` 默认资源，黑名单排除本地状态、sessions、compiled artifact 与专用 metadata，未手改自动跟随、已手改返回 diff warning。 |
| Skills | Active | 新 Agent 后端只扫描 `assets/workspace/.nbook/agent/skills` 与 `workspace/.nbook/agent/skills`；v2 的写作流程、初始化、番茄导入、SillyTavern 角色卡导入、RP模式、skill-creator 和 TSX profile editing skills 已整体迁到 v3 系统 skill root，`RP目录初始化` 已删除，`assets/agent-v2/skills` 仅作为归档参考。独立 `skill` 工具已禁用；模型通过 `SkillCatalog` 看到 key/name/description/when_to_use/location，需要使用时用 `read` 打开对应 `SKILL.md`，reference/scripts/templates/examples 按入口说明按需读取。`stop-slop` 已作为系统 skill 加入 `assets/workspace/.nbook/agent/skills/stop-slop/`，用于消除 AI 写作套路；`Import` DSL 现在允许读取系统 skill 文档，普通 `writer` profile 首轮 `HistorySet` 默认导入 `stop-slop/SKILL.md`。RP / simulation profiles 当前状态：`rp.leader` 已作为 builtin profile 实现，是 RP 用户交流、开局引导、化身创建、体验边界和陪伴式统筹层，读取 `manual/` 与 `agents/rp.leader/`，按“处境 -> 行动 -> 世界回应 -> 新选择点”组织 Tick，并在需要世界裁决、人物/环境反应或 state commit 时调用 `simulator.leader`；`RP模式` skill 的普通入口也已改为优先创建或切换到 `rp.leader`。`simulator.leader` 仍是写作模式和 RP 共用的世界模拟主管，读取 `AGENTS.md`、`agents/simulator.leader/context.md`、最近 Tick、相关 subject/entity 状态、lorebook、reference 和 Plot，统一调度 `simulator.actor` 并输出 writer-safe brief；旧 `leader.rp` 已删除且不作为新合同名。`simulator.actor` 主 run 直接 Import 自己的 `soul.md`（第一人称扮演手册、无 frontmatter、无秘密）作为身份，并按 invoke 传入的 `kind`（player/npc）注入行为规则：player 化身不抢话、不自创关键行动，只把上级 directive 第一人称自然化复述；npc 可按 soul.md 性格自主反应。主 run 不读取 `subject.md`（全知秘密档，只给 simulator.leader），只消费 actor.context-load sidecar 持久化注入的 actor-safe context 和 actor-facing message；context-load 是纯 RAG 检索器（不读任何文件原文），memory-save sidecar 维护 `events.jsonl`、`memory.jsonl` 与 `mind.md`（不读写 `subject.md`/`soul.md`/`state.md`），`state.md` 由 simulator leader 裁决；冷启动初始记忆由 simulator.leader 直接落进 `events.jsonl` / `memory.jsonl`，`memory-seed.md` 中转文件已移除。`rp.writer` profile input 为空，只消费上级注入的 writer brief；它保留“小猫之神”写作预设并默认导入 `stop-slop` skill，不自主遍历 `simulation/`、`lorebook/`、`manual/`、`agents/` 或 `reference/`，若 brief 明确指定路径才读写文件，不再要求 `report_result.data.prose`；默认 Project 模板已将 profile guidance 收敛到 `agents/`，新增 `agents/rp.leader/` 作为 RP 引导上下文和长期记忆占位，`simulation/` 只保留 `subjects/`、`entities/` 和 `runs/` 示例 Tick。系统 skill 还包含 `profile-system-guide`，用于让 `leader.assets` 用通俗方式协助用户理解和修改 harness / TSX profile / skill / user-assets 覆盖层，并提供文档索引、profile compile CLI、恢复系统版本和新建模板指导；该 skill 已新增 reference 文档，详细说明 session truth、invoke 生命周期、ProfileTurnPlan 分区、TSX DSL、user-assets 覆盖和 compile/preview/run 区别。TSX Profile Workbench 第一版已接入用户 profile 源码写入、新建模板、文件 diagnostics、prepare preview、创建 session 入口，并可解析/局部写回稳定的 `<ProfilePrompt>` TSX DSL tree；用户 profile 编辑已切为自动轻量 DSL 解析 + 手动后台 worker 编译，保存不触发自动编辑期 runtime profile loader，创建 Session 需要最近一次编译通过且源码未改动；普通 runtime catalog 已硬切为 `.compiled` 运行真相源，系统 compiled artifact 随 system assets 预编译发布，用户 compiled artifact 只能由 Workbench 或 `profile compile` 手动生成；profile 和变量 definition compiled artifact 已改为稳定文件名，hash 只保存在 manifest 中，编译和 user-assets 同步都会清理未引用旧产物；旧 `scripts/compile-profile.ts` / `scripts/check-profile.ts` / `scripts/profile-compile-cli.ts` 已被 `profile status/check/compile/preview` CLI 替换；旧 `profile-templates` / `user-profile-templates` API 仍作为 501 tombstone，不再作为新合同。 |
| Docs | Active | 文档治理继续按 `reference/` 稳定参考、`docs/tutorials/` 用户教程、`docs/tasks/` 任务 walkthrough、`docs/research/` 调研资料分层；仓库根 `spec/` 已硬切改名为 `reference/`，`reference/README.md` 作为 NeuroBook Reference Bookshelf。Agent reference 现以 `reference/agent/README.md` 为入口，包含 profile、Import、Run Kernel hooks、Sidecar 和 harness 黑盒合同；content reference 现以 `reference/content/README.md` 为入口，并把旧 lorebook information-control 长文拆为 `directory-protocol.md` 与 `information-control.md`，新增 `manual.md` 作为 Project 说明书目录稳定规范。站点新增“从第一本书到第一次 RP”基础教程，覆盖 Project Workspace、Skill 初始化、前三章写作、SillyTavern 角色卡导入和 `simulation/` 世界模拟；站内 `/agent/`、`/profile/`、`/profile-tsx/` 路由已恢复为当前合同导读页，详细实现合同继续链接到 `reference/`；旧 `docs/modules/agent/tools-reference.md` 已移除，避免 active 文档继续暴露 v2 工具名。 |

Agent Profile Home 最新补充：profile home 第一版已固定为 `{Project Workspace}/agents/{profileKey}/`，不增加 `profileHomeRoot` 配置项；`defineAgentProfile()` 可声明 `home: defineProfileHome({init, upgrade, reset})`，runtime prepare、prepare preview 和 Agent Profile 模型完整设置面板会在 Project scope 下 ensure home。`ctx.home` 提供受限文件 API 与 `home.json` metadata，profile manifest `version` 作为 home upgrade 目标版本。低代码表单新增通用 `resource-preset` 字段和服务端 `defineResourcePreset()` / `profileHomeResource()` resolver 协议，Project settings 保存会先模拟整批 resource mutations 的最终 key 并完成校验，通过后才执行资源写入和配置保存；Global settings 禁止资源写入。writer 文风/参考预设已迁到 profile home 资源，系统默认资源从旧 `agent/writing-presets` 移到 `agent/profiles/builtin/writer.home/{styles,references}`；Project template、context-access 写入、profile prompt 和 reference 文档已同步到 `agents/{profile}/`。相关 focused tests 与 `bun run typecheck` 已通过。

## TODO

- 跑通剧情系统的核心写作链路。
- 跑通 Lore / mention / reference 的端到端体验。
- 补齐 Writer / Planner / Critic / Editor 的多 Agent 工作流。
- 简化 story 工具和 content node schema 的重复定义。
- 继续把旧章节面板和预览页从 DB Chapter 心智收敛到 manuscript content-node。
- 收尾 Project API 命名：前端 store 和部分兼容 DTO 仍沿用 `Novel` / `currentNovelId` 命名承载 `projectPath`，后续需要在不扩大行为面的前提下重命名为 Project 术语。
- 继续清理非 Plot 链路中的旧 `lorebook://` 引用入口。
- 继续稳定 Markdown Studio/表单包装层的标题、inline code、引用、图片和表格编辑体验。
- Agent Composer 已按 Task 40 收敛为单 Composer、纯文本输入和前端禁用 waiting_user steer/followup；已补根级引用 chip 渲染、`$ skill` 首次加载刷新和原地大文本模式，后续补一次真实浏览器交互验收。
- 设计泛用自然语言编辑工具：输入目标文件、自然语言操作说明和可选上下文消息数量，由轻量模型辅助修改文件；后续可用于 Agent 记忆系统、RP 状态维护和常规文件编辑减负。
- 建立 token statistics、写作历史 statistics 和缓存命中统计。
- 扩展番茄小说导入：补基础数据、评论、全站搜索、正文下载含段评和图片等能力。
- 将 workspace 保存冲突视图与后续 Git 版本控制能力整合。
- 独立发版脚本需要保持两段式 profile 产物刷新：源码构建期继续运行 `profile:metadata` 生成 `.system-profile-metadata.json`，`product:stage` 还要在 Product Root 内重新 `profile compile --all --system`，并断言 Profile Workbench worker 的 `tsx/esm/api` 可从 `.output/server/node_modules` 解析，同时断言系统 `.compiled/*.mjs` 不含 CI/本机绝对 Product require 或 `.building.mjs` 临时引用，确保 artifact 可迁移且 dependency hash / 依赖解析都指向产品内 vendor。用户 assets 同步会基于 `.profile-sync-state.json` 自动更新未手改的系统 profile 覆盖，手改或缺 state 时保留用户文件并提示遮蔽。发布入口只保留 `bun run release -- <stable|prerelease|canary|alpha|beta|rc>`；正式版要求 SemVer `X.Y.Z`、干净工作区和 `--yes --push`，也可用 `--next patch|minor|major` 显式自动增长；先行版本按 SemVer channel/tag 校验并继续创建 GitHub prerelease，可用 `--next patch|minor|major` 基于当前发布线选择基础版本增长，当前发布线取 `package.json.version` 和当前 HEAD 最近 SemVer tag 中较新的版本，其中 alpha/beta/rc 默认扫描已有 tag 自动递增序号，并会把完整 prerelease version 写入 `package.json.version`。
- Release CLI / Product version 最新修正：canary / alpha / beta / rc 真实发布时若目标完整 prerelease version 不同于当前 `package.json.version`，必须加 `--push`，脚本才会写入 `package.json`、创建 `chore(release): <tag>` commit、推送 release commit，再用该 commit 创建 GitHub prerelease；未加 `--push` 会在写文件前停止，`--target` 不能与自动版本 bump 混用。Product 版本不再依赖 `release-meta.json`；桥接版发布后的下一次 release 删除 deprecated `app/release-meta.json` 占位文件。
- 设置页后续继续增强 raw/effective 差异展示；当前配置中心已支持 Global / Project / Browser State 目标切换、Project selector、Project 默认模型、默认 Profile 和 Agent Profile 模型覆盖。
- 前端 i18n 后续继续扩展覆盖：Plot、RAG、Profile Template Editor 等模块级 UI；服务端返回中文错误仍需后续错误 code/message contract 才能完整英文化。
- 清理动态 profile 渐进迁移 fallback：生产注册路径已切到 assets profile + builtin contract，系统 assets profile 也已改成 `defineAgentProfile`；后续删除旧源码 builtin class，或把仍被复用的 prompt helper 迁成稳定公共 helper。
- 重新设计 TSX profile 的 TypeBox Schema Builder：旧 Zod 低代码 schema 编辑不再作为新合同保留；新的 Profile Workbench 第一版只做 schema 只读展示和 TypeBox 骨架生成，后续再基于 v3 profile 经验补可稳定定位的局部辅助编辑。
- 为 skill catalog 增加类似 tool 的白名单或启用控制，避免所有可发现 skill 默认进入模型可见 catalog。
- 小说写作流程和 emulation 第一版已落地到系统 skill、默认 Project 模板和 leader/RP prompt：写作 workflow skill 已统一为 `novel-workflow-01-*` 到 `novel-workflow-10-*` 的 canonical key，技法入口使用 `novel-technique-commercial-rhythm`，导入入口使用 `novel-import-*`。当前运行目录仍是 `simulation/`，写作中的 `emulation` 暂作为世界运行态推进概念。`simulation/runs` 默认模板改为 `current.md`、`index.md`、`ticks/000000-initial-state/report.md` 和 `prose.md`，`input.md` / actor packets / commit log / tool log 等机械产物留给后续 workflow/runtime 自动生成。
- 后续如果支持加载第三方 profile，新增 profile 审查 skill，用于检查陌生 `.profile.tsx` 的危险代码、工具权限、schema contract 和提示词行为；当前动态 profile 不做 sandbox，按用户可信本地代码处理。
- 后续如有需要，再把系统默认 profile 暴露到系统设置；当前默认 Agent Profile 写入 Global / Project Config，system default 仍为小说 `leader.default`、用户 assets `leader.assets`。
- 让 workspace 默认 Profile 设置页复用 profile catalog 的 loadStatus / issue 信息，避免 contract-only 或 missing profile 在设置列表中被当作普通可运行项展示；当前运行路径已阻止 contract-only profile 执行。
- 收紧 Import 白名单的 `workspace/` 放行：当前为支持 `simulator.actor` 主路 Import 自己的 `soul.md`，`isAllowedImportPath` 整体放开了 `workspace/**` 前缀（仍保留 `..` 越界检查），相当于任何 profile 都能 Import 任意 Project 运行态文件。后续需要按 profile / subject 收敛为细粒度授权，避免越权读取其他 Project 或其他 subject 的运行态文件。
- 外部 Project（workspace 容器外的绝对路径）下 `simulator.actor` 主路 Import `soul.md` 会失败，因为 Import 按 repo-root 解析 `workspace/${subjectPath}/soul.md`；当前 RP 小说都在 `workspace/` 下可接受，后续支持外部 Project 时需要换更通用的 soul 注入方式。
- Agent 前端迁移后补一次浏览器交互验收：重点验证多窗口事件同步、followUp queue、approval resume、Plan Mode、model command、compact、edit/retry/rollback/fallback 和流式工具卡片。
- Agent SSE 前端合同第一轮已落地：`/api/agent/sessions/:id/events` 已有 stream manager、自动重连、snapshot single-flight、连接状态、run phase、`contentIndex` assistant block 合并和手动重连/刷新历史入口；snapshot 已区分 `eventCursor` 恢复点与 `latestSeq` 事件流尾部，刷新运行中 session 可 replay 尚未落盘的 runtime transcript；后续保留真实浏览器验收与工具执行输出流式后端贯通。
- 后续为 Agent session 增加 provider 级 `systemPrompt` metadata/hash 记录，避免 TSX profile 源码变化后旧 session 的系统提示不可追踪；当前前端不把 systemPrompt 展示成聊天消息。
- Agent session event hub 后续支持跨进程/多实例广播；第一版是单进程内存 replay，不保证实时 fan-out。重启/dev reload 恢复已收敛为 `(eventEpoch, per-session seq)` cursor + connected handshake + snapshot reset；同进程运行中刷新通过 transcript replay anchor pin 住 EventHub replay buffer，snapshot `eventCursor` 会回到本轮未落盘 transcript 前；waiting snapshot 可从 session active path hydrate pending approval 对应的 waiting activeInvocation，`continue(resolution)` 可在新 harness 上复用原 logical `invocationId`。当前已补齐 session admission queue 和 `SessionWriteExecutor` per-session write queue，避免并发 resolution 双提交和同 session JSONL leaf 写入交错。
- TSX Profile Workbench 后续补完整辅助编辑：当前第一版已能从系统模板新建、保存、轻量解析、手动后台编译、诊断、预览、创建自定义 profile session，并解析/局部写回稳定 `ProfilePrompt` DSL tree；TypeBox Schema Builder、工具权限 checklist 局部替换、复杂 `Watch.render`、跨 helper 的完整 AST round-trip，以及是否为多 profile catalog 编译引入 worker pool 仍待重做。
- Agent 变量系统已接入 profile 编辑期类型补全底层：从 builtin/client、Workspace Root/global、Project Workspace/project 和 profile/session definitions 派生 generated `.d.ts`，让 `ctx.vars.get()`、`<Variable>`、`<VariableSchema>` 和 `watchPath` 获得 path 补全和返回值类型提示；类型产物只作为 authoring aid，不改变 runtime registry/schema 校验真相源。后续补 Workbench 变量选择器和更细的 schema-aware patch builder。
- Pi Agent Harness 迁移后续补 `invoke_agent` 非阻塞调用；v3 agent 工具已收敛为 `create_agent`、`invoke_agent`、`get_agent(id?: number)`、`get_agent_profile`、`get_session`、`detach_agent`；`invoke_agent` 工具支持 `prompt | continue | steer | followup`、`message?: string` 和 `input?: object` payload，返回统一 `result.message/data` 与简洁 `stats`，不返回完整 history；`request_user_input`、`enter_plan_mode`、`exit_plan_mode` 的审批/回答恢复统一走 `continue + resolution`，由 harness 补齐 toolResult 后继续。独立 `skill` 工具已禁用，skill 正文通过 `read` 读取 SkillCatalog location。
- Pi Agent Harness 迁移后续评估文件/变量回溯：变量可通过 session custom state entry reduce，文件回溯需要专门的 `file_snapshot` / `file_patch` entry 或接入 Git/worktree snapshot，第一版不承诺文件内容回滚。
- 观察 `arch` source 模式快速同步脚本的稳定性，并决定是否要把远端部署目标做成可配置 preset。
- 修复 workspace-files API tests 的 Vitest mock API 兼容问题；当前 `server/workspace-files` 通过，`server/api/workspace-files` 的部分测试仍受 `vi.resetModules` / `vi.doUnmock` 不存在影响。
- 重构审批系统，把工具审批一般化为可由工具定义声明的协议。当前审批是 harness 特判：`approvalToolKeys`（`request_user_input` / `enter_plan_mode` / `exit_plan_mode`）命中即 suspend，approved 后不执行工具 execute，只用固定 `tool_approval` / `user_input` resolution 落 toolResult，前端渲染（approve/reject、user_input 表单、plan 预览）硬编码在 `pendingApprovalDto` + 前端组件。工具层重构后 `defineAgentTool` 只支持语义 A 的 `approvalRequired`（仅 suspend 确认，approved 落文本 toolResult，不回调 execute）。后续需要做：语义 B（approved 后真正执行工具 `executeWithContext`，在 resolution 链路新增执行分支）、自定义 resolution shape、工具自带审批校验逻辑、动态审批 UI 渲染协议。

## Risks

- 文档和代码容易漂移，需要每次重大任务结束时更新仓库状态和任务 walkthrough。
- Agent prompt 背景信息过多时，模型可能过度迎合噪声，后续要控制信息注入。
- Markdown Studio 的 ProseMirror node / mark 与 Markdown 序列化如果不同步，容易产生源码模式和富文本模式行为漂移。
- Agent 前端已迁到新 session contract，但尚未做浏览器交互验收；多窗口同步、流式工具卡和审批恢复仍需要在真实开发服务器里走一遍。

## Recent Tasks

| Task | Status | Walkthrough |
| --- | --- | --- |
| Agent RP Mode Planning | Planning | [docs/tasks/01-agent-roleplay-mode/README.md](docs/tasks/01-agent-roleplay-mode/README.md) |
| RP Manual Directory And RP Leader Naming | Implemented | [docs/tasks/45-rp-manual-directory-and-rp-leader-naming/README.md](docs/tasks/45-rp-manual-directory-and-rp-leader-naming/README.md) |
| Pi Agent Harness Migration | In Progress | [docs/tasks/02-pi-agent-harness-migration/README.md](docs/tasks/02-pi-agent-harness-migration/README.md) |
| Config System | Done | [docs/tasks/03-config-system/README.md](docs/tasks/03-config-system/README.md) |
| TSX Profile Workbench | Active | [docs/tasks/04-tsx-profile-workbench/README.md](docs/tasks/04-tsx-profile-workbench/README.md) |
| Leader Profile v2 Adaptation | Done | [docs/tasks/05-leader-profile-v2-adaptation/README.md](docs/tasks/05-leader-profile-v2-adaptation/README.md) |
| Leader Default Prompt Parity | Updated | [docs/tasks/06-leader-default-prompt-parity/README.md](docs/tasks/06-leader-default-prompt-parity/README.md) |
| Project Identity Migration | Planning | [docs/tasks/09-project-identity-migration/README.md](docs/tasks/09-project-identity-migration/README.md) |
| Portable Project Workspace | In Progress | [docs/tasks/11-portable-project-workspace/README.md](docs/tasks/11-portable-project-workspace/README.md) |
| Profile Variable Types | Implemented | [docs/tasks/12-profile-variable-types/README.md](docs/tasks/12-profile-variable-types/README.md) |
| Agent SSE Front-End Contract | Implemented | [docs/tasks/14-agent-sse-front-end-contract/README.md](docs/tasks/14-agent-sse-front-end-contract/README.md) |
| Agent Session Management | Implemented | [docs/tasks/15-agent-session-management/README.md](docs/tasks/15-agent-session-management/README.md) |
| Agent Steer / FollowUp Composer | Implemented | [docs/tasks/16-agent-steer-followup-composer/README.md](docs/tasks/16-agent-steer-followup-composer/README.md) |
| Agent Composer Plain Text Input | Implemented | [docs/tasks/40-agent-composer-plain-text-input/README.md](docs/tasks/40-agent-composer-plain-text-input/README.md) |
| Inline Editor Agent | Implemented | [docs/tasks/55-inline-editor-agent/README.md](docs/tasks/55-inline-editor-agent/README.md) |
| Session Title/Summary Enhancement | Implemented | [docs/tasks/17-session-title-summary-enhancement/README.md](docs/tasks/17-session-title-summary-enhancement/README.md) |
| Agent Runtime Pipeline Hooks | Implemented | [docs/tasks/18-agent-runtime-pipeline-hooks/README.md](docs/tasks/18-agent-runtime-pipeline-hooks/README.md) |
| Agent Sidecar Profile Pass | Implemented | [docs/tasks/23-agent-sidecar-profile-pass/README.md](docs/tasks/23-agent-sidecar-profile-pass/README.md) |
| Agent Profile Tool Bindings | Implemented | [docs/tasks/47-agent-profile-tool-bindings/README.md](docs/tasks/47-agent-profile-tool-bindings/README.md) |
| Agent Tool Definition Layer | Implemented | [docs/tasks/48-agent-tool-definition-layer/README.md](docs/tasks/48-agent-tool-definition-layer/README.md) |
| Agent SSE Reload Recovery | Implemented | [docs/tasks/24-agent-sse-reload-recovery/README.md](docs/tasks/24-agent-sse-reload-recovery/README.md) |
| Agent Harness Black-Box Tests | Implemented | [docs/tasks/25-agent-harness-black-box-tests/README.md](docs/tasks/25-agent-harness-black-box-tests/README.md) |
| Windows Portable Packaging | Updated | [docs/tasks/26-windows-portable-packaging/README.md](docs/tasks/26-windows-portable-packaging/README.md) |
| Agent Mode Layout | Implemented | [docs/tasks/27-agent-mode-layout/README.md](docs/tasks/27-agent-mode-layout/README.md) |
| Markdown Studio Welcome Agent Onboarding | Implemented | [docs/tasks/50-markdown-studio-welcome-agent-onboarding/README.md](docs/tasks/50-markdown-studio-welcome-agent-onboarding/README.md) |
| Agent Profile Routing | Implemented | [docs/tasks/52-agent-profile-routing/README.md](docs/tasks/52-agent-profile-routing/README.md) |
| Agent Initial/Payload Schema | Implemented | [docs/tasks/53-agent-initial-payload-schema/README.md](docs/tasks/53-agent-initial-payload-schema/README.md) |
| Agent Profile Settings Low-Code | Implemented | [docs/tasks/58-agent-profile-settings-low-code/README.md](docs/tasks/58-agent-profile-settings-low-code/README.md) |
| Agent Profile Home | Implemented | [docs/tasks/60-agent-profile-home/README.md](docs/tasks/60-agent-profile-home/README.md) |
| Frontend I18n | Implemented | [docs/tasks/57-frontend-i18n/README.md](docs/tasks/57-frontend-i18n/README.md) |
| World Engine | In Progress | [docs/tasks/56-world-engine/README.md](docs/tasks/56-world-engine/README.md) |
| World Engine Workbench Redesign | Issue Human Explanation | [docs/tasks/59-world-engine-workbench-redesign/README.md](docs/tasks/59-world-engine-workbench-redesign/README.md) |
| World Engine Workbench Real API Integration | Implemented | [docs/tasks/61-world-engine-workbench-real-api/README.md](docs/tasks/61-world-engine-workbench-real-api/README.md) |
| NeuroBook Directory Protocol | Planning | [task](docs/tasks/28-lorebook-information-control-protocol/README.md), [directory](reference/content/directory-protocol.md), [information-control](reference/content/information-control.md) |
| Reference Bookshelf Reorg | Active | [docs/tasks/30-reference-bookshelf-reorg/README.md](docs/tasks/30-reference-bookshelf-reorg/README.md) |
| Agent Profile Import Node | Implemented | [docs/tasks/29-agent-profile-import-node/README.md](docs/tasks/29-agent-profile-import-node/README.md) |
| User Onboarding Tutorials | Implemented | [docs/tasks/33-user-onboarding-tutorials/README.md](docs/tasks/33-user-onboarding-tutorials/README.md) |
| Agent Prompt Engineering: Simulation And Director | Implemented | [docs/tasks/36-agent-prompt-engineering-simulation-director/README.md](docs/tasks/36-agent-prompt-engineering-simulation-director/README.md) |
| System Assets Preflight | Planning | [docs/tasks/37-system-assets-preflight/README.md](docs/tasks/37-system-assets-preflight/README.md) |
| Profile Context Memory | Implemented | [docs/tasks/38-lorebook-context-memory/README.md](docs/tasks/38-lorebook-context-memory/README.md) |
| Simulation Rollback Mechanism | Planning | [docs/tasks/42-simulation-rollback-mechanism/README.md](docs/tasks/42-simulation-rollback-mechanism/README.md) |
| Subject RAG Memory | Implemented | [docs/tasks/43-subject-rag-memory/README.md](docs/tasks/43-subject-rag-memory/README.md) |
| RAG Visualization | Implemented | [docs/tasks/44-rag-visualization/README.md](docs/tasks/44-rag-visualization/README.md) |
| Full Site Auth | Done | [docs/tasks/archived/fullsite-auth/README.md](docs/tasks/archived/fullsite-auth/README.md) |
| Docker Compose Deployment | Updated | [docs/tasks/archived/docker-compose-deployment/README.md](docs/tasks/archived/docker-compose-deployment/README.md) |
| Provider Reasoning Replay | Done | [docs/tasks/archived/provider-reasoning-replay/README.md](docs/tasks/archived/provider-reasoning-replay/README.md) |
| TSX Profile Template Editor | Preview Debug Workbench | [docs/tasks/archived/tsx-profile-template-editor/README.md](docs/tasks/archived/tsx-profile-template-editor/README.md) |
| Agent TSX Prompt Context | Done | [docs/tasks/archived/agent-tsx-prompt-context/README.md](docs/tasks/archived/agent-tsx-prompt-context/README.md) |
| Agent Plan Mode | Done | [docs/tasks/archived/agent-plan-mode/README.md](docs/tasks/archived/agent-plan-mode/README.md) |
| 内容节点 retrieval / inject | Done | [docs/tasks/archived/content-node-retrieval/README.md](docs/tasks/archived/content-node-retrieval/README.md) |
| 内容节点状态与信息差 | Done | [docs/tasks/archived/content-node-state/README.md](docs/tasks/archived/content-node-state/README.md) |
| 番茄小说导入 Skill | Done | [docs/tasks/archived/tomato-novel-skill/README.md](docs/tasks/archived/tomato-novel-skill/README.md) |
| 文档体系重整 | Updated | [docs/tasks/archived/documentation-reorg/README.md](docs/tasks/archived/documentation-reorg/README.md) |
| Markdown Studio Notion-like 富文本 | Done | [docs/tasks/archived/markdown-studio-notion-rich-text/README.md](docs/tasks/archived/markdown-studio-notion-rich-text/README.md) |
| Workspace 文件实时同步与保存冲突 | Done | [docs/tasks/archived/workspace-file-sync/README.md](docs/tasks/archived/workspace-file-sync/README.md) |
| 用户 Assets 工作区 | Done | [docs/tasks/archived/user-assets-workspace/README.md](docs/tasks/archived/user-assets-workspace/README.md) |

