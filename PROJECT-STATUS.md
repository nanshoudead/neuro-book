# Project Status

## Summary

neuro-book 当前处于快速开发阶段。项目主线正在从数据库中心逐步转向文件化 Workspace Root / Project Workspace，并围绕 Markdown Studio、引用系统、剧情结构、多 Agent 协作、统一配置系统、全站鉴权、管理员后台和主界面账户入口补齐核心体验。

部署侧已硬切 SQLite-only：App SQLite 默认位于 `workspace/.nbook/neuro-book.sqlite`，只保存 `User`、auth、global config 相关应用级数据和全局锁；Project SQLite 位于每个 Project Workspace 内的 `.nbook/project.sqlite`，保存 Story / StoryPhase / StoryThread / StoryScene / StoryPlot / StorySceneRef 等项目结构化数据。PostgreSQL schema、adapter、pool、部署选项和 compose override 已移除，旧 PostgreSQL 数据不提供内置迁移。部署主线已调整为 Product-first：Windows Product Portable 是普通用户默认 release，zip 内包含 `app/` Product Payload、`data/` 运行状态、`runtime/bun/` 内置 Bun 和 `launcher/` Windows Launcher；启动不再 clone 源码、安装 Git/ripgrep、安装依赖或执行 Nuxt build。Product Payload 由 `product/` 本地成品验证根生成：`bun run product:stage` 复制预构建 `.output`、运行资产、脚本、SQLite migrations、`assets/workspace` 与 TSX Profile Workbench 编译所需 runtime source 子集，生成 Product Root `.env`，并在 Product Root 内重新编译系统 profiles，使 `.compiled` artifact 绑定产品内 vendor。Product Root 启动时 `process.cwd()` 必须指向 Product Root，产品根不要求 `node_modules`，依赖由 `.output/server/node_modules` 作为 Nitro 内置 vendor 承载；Windows Launcher 以 `app/` 为服务 cwd，通过 `app/workspace` 目录联接把真实用户数据落到 `data/workspace`，并用内置 Bun 运行管理员创建和用户检测脚本。Profile Workbench 后端 worker 通过 `.output/server/index.mjs` 创建 runtime require 解析 `tsx/esm/api` 与 `tsx` loader，不从 Product Root 裸 import 或回退仓库根依赖；profile artifact compiler v4 会从 runtime 上下文解析并 bundle 普通第三方包，Product artifact 的 require shim 会优先使用当前 Product Root cwd 的 `.output/server/index.mjs`，再从当前 `.compiled` 文件向上定位 Product Root 入口，不再写入构建机绝对 file URL，用户 profile 在 product 内编译后运行时也不依赖根 `node_modules`。`product:stage` 与 `package:windows-portable` 会断言系统 profile artifact 不包含 CI/本机绝对 Product require；单文件 profile 编译也通过 staging 目录提交，避免真实 `.compiled` manifest 引用 `*.building.mjs` 临时文件。产品脚本统一从 `.output/server/scripts/**` 运行，`product:start` 自动加载 Product Root `.env` 并用 Bun 启动 Nitro 入口；`assets/workspace/.nbook/agent/scripts/workspace.ts` 和 `agent/bin/workspace/profile/variable(.cmd)` 在 product 内作为 launcher 指向 `.output/server/scripts/**`，并使用内置/系统 Bun 直接运行；`nbook/*` 通过 `.output/server/node_modules/nbook` 产品内 runtime source 包解析，不依赖开发机源码或根 `node_modules`；`release-meta.json` 在产品形态下固定暴露 `versionKind: "release"`，并保留 `sourceKind` 记录 tag/commit/package 来源，版本 API 只读 Product Root 元数据。`ghcr` / Product Docker 保留为服务器预构建部署，Product Bun 面向已有 Bun 的本机或服务器；`local-git` 和 `source` 降级为源码/过渡路径。Project Workspace 根目录 `project.yaml` 是项目唯一真相源，保存 `kind`、`title`、`summary`；App SQLite 不记录 Project index、Project 状态或 `Novel`/`novelId` mapping。模型 Provider、API Key、Agent 默认 profile、UI/editor 偏好已迁入 Global Config `workspace/.nbook/config.json`，Project 覆盖写入 `workspace/{project}/.nbook/config.json`。这些运行时文件都不进 Git，真实文件不再作为仓库文件维护。

`execute_sql` 已收敛为 Project SQLite-only 工具：不接受 SQLite 文件路径参数，不访问 App SQLite，只能在当前 Project Workspace 上下文中操作 `.nbook/project.sqlite`，继续允许受限 `SELECT` / `WITH` / `INSERT` / `UPDATE` / `DELETE`，并禁止 DDL、多语句、事务控制和危险 PRAGMA。

部署最新补充：Windows Launcher 更新入口现在会查询 GitHub Releases，列出带 `neuro-book-windows-x64.zip` 和 `SHA256SUMS` 的 stable / canary / alpha / beta / rc 版本供用户选择，校验 SHA256，备份旧 `app/` / `launcher/` / root scripts 后切换新版并保留 `data/`；内置 `runtime/bun/` 在自动更新中保留当前版本，避免替换正在运行的 `bun.exe`。

GHCR / Product Docker 最新边界：app 镜像仍保留源码目录用于容器内排障，但 final image 使用 Bun runtime，不再携带或依赖根 `node_modules`。容器启动走 `scripts/deploy/docker-product-entrypoint.sh`，先用 `bun .output/server/scripts/db/prisma-migrate.mjs --deploy` 执行 SQLite migration，再用 `bun .output/server/scripts/deploy/product-start.mjs` 启动服务；`product-start.mjs` 会先调用 `.output/server/scripts/build/prepare-system-assets.ts --sync-user-assets` 做启动前 system assets / profile artifact 同步，通用 `.output` 后处理必须把该脚本复制进 `.output/server/scripts/build/` 并做缺失门禁；GHCR / 通用 `.output` runner 在无根 `node_modules` 时可用 `.output/server/release-meta.json` 判定 Product Runtime 并读取版本元数据，避免 profile artifact compiler / Profile Workbench worker 误入源码模式；管理员命令同样通过 `bun .output/server/scripts/cli/create-admin.ts` 运行产品内入口，部署机和容器启动时不执行 `bun install` / `bun run generate`。

模型 Provider 侧已把 `openai-compatible` 收敛为增强版 OpenAI 兼容 adapter，默认接收、标准化并回放 `reasoning_content`，覆盖 MiMo、DeepSeek 网关等 Agent thinking 场景；严格 OpenAI 官方接口使用 `openai-official`，避免把 provider extension 字段发给官方 OpenAI。

本文档记录仓库级现状。每次重大任务完成后，需要同步更新本文档和对应 active `docs/tasks/<order>-<task-slug>/README.md` 或 archived `docs/tasks/archived/<task-slug>/README.md`。

## Current Focus

- Skills 最新补充：`anti-ai-slop` 当前定位为中文文本润色 skill，保持原目录和 CLI 命令不变；其步骤 3 已加入 stop-slop 风格的快速审查清单和 Directness / Rhythm / Trust / Authenticity / Density 五维 50 分评分，static/LLM 规则也已扩展到公式化设问、商务黑话、隐藏行动者、节奏单调、金句感和段尾机械升华等文本润色问题。
- RP writer 合同：`rp.writer` profile input 保持为空；`rp.leader` 通过 `invoke_agent.message` 发送完整 Writer Brief。Writer Brief 已收敛为轻量 XML 骨架：`<context>` 内 Markdown 链接是唯一 read 白名单入口，`<materials>` / `<beats>` / `<style>` 承载素材、节拍和写作提示，并允许自定义语义 tag；`rp.writer` 收到 Brief 后自检，缺关键材料或缺输出路径时用 `report_result.result` 提问/报错，材料足够时写入 Brief 指定路径并用 `report_result.result` 汇报落点，不再使用 check/render 阶段输入或旧补充字段。
- 文件化 workspace：按 Project Workspace 隔离，根 `project.yaml` 是项目身份与展示元数据真值源；前端通过 SSE 订阅真实文件变化，保存时使用 `mtimeMs` 做冲突检测。
- Markdown Studio：收敛为 Notion-like 富文本模式 + 源码模式；Markdown 原文仍是唯一真相，TipTap 负责运行时 ProseMirror 富文本表面，表单 Markdown 编辑器也复用同一底层能力并只做工具栏包装。
- 引用与内容节点：统一 inline ref、structured refs、content node 校验、warning 策略、`state.md` 当前状态，以及 `retrieval` / profile context memory 上下文策略；内容节点级 `inject.profiles` / `inject.always` 已硬切删除，profile-scoped context 进入 `agent-context/{profile}/context.md`，程序访问状态写入 `.nbook/context-access/{profile}.json` 并渲染 `agent-context/{profile}/generated.md`；`knowledge[]` 使用自然语言字符串并校验其中的 Markdown 内容节点链接；内容节点不再生成通用 `writingTip` frontmatter。
- 默认 Project 模板：`lorebook/` 已对齐当前 reference，默认生成 `world/`、`character/`、`location/`、`faction/`、`item/`、`event/`、`system/`、`instruction/` 和 `note/`；`manual/` 说明书目录采用跑团手册结构，默认包含 `README.md`、`world-guide.md`、`rules-guide.md`、`gm-guide.md`、`reference.md`、`player-guide/README.md`、`player-guide/character-creation.md`、`player-guide/playable-characters/README.md` 和 `player-guide/playable-characters/player.md`，用于快速开始、玩家手册、角色创建、预设角色入口、世界观导览、规则指南、GM 手册和主持人速查屏；默认模板现在为 `lorebook/`（显示为“世界书”）、`manual/`、`agent-context/`、`manuscript/`、`simulation/`、`reference/`、`upload/` 及关键二级分类目录生成 `directory-index` frontmatter，用中文 title 和 Lucide icon 驱动文件树展示，其中 `simulation/entities/` 显示为“实体”；初始化 note 收敛为 `story-concept`、`project-profile`、`opening-seed`，AI 使用说明进入 `instruction/creation-boundaries`，不再生成旧 `rule/writing-style`、`synopsis`、`project-positioning`、`theme` 或 `initial-plot-seed`。
- Agent 系统：主路径已硬切到 Pi-based `server/agent`。旧 v2 已归档到 `server/agent-v2` 与 `assets/agent-v2`，不再进入 typecheck、测试或运行时；旧 Prisma `AgentThread` / `AgentMessage` 数据模型已删除。新主路径使用 TypeBox profile contract、active TSX Profile DSL、`ProfileTurnPlan` harness 合同、Pi-compatible message/event/tool 语义、JSONL append-only session、动态 `.nbook/agent/profiles` catalog、`.nbook/agent/skills` SkillCatalog、`read/write/edit/apply_patch/bash` 基础工具、`create_agent/invoke_agent/get_agent/detach_agent` agent 工具，以及已迁移的 task / Plot / SQL 业务工具。普通 Project agent 的工具 cwd 统一为 Workspace Root `workspace/`，当前项目通过 `projectPath` 和 `AppendingSet` runtime reminders 表达；项目文件优先使用 `project-slug/...`，`workspace/project-slug/...` 仅作为兼容别名；user-assets agent cwd 仍是 `workspace/.nbook`。`RuntimeContext` DSL 已硬切删除，运行位置通过 `RuntimeLocationReminder` 写入 session，用户当前 Project Workspace / selected file 通过 `WorkspaceFocusReminder` 写入 session，Plan mode availability 也通过可见 reminder 写入 session，`ModelContext` 只保留真正 model-only 的动态上下文。独立 `skill` 工具已禁用，Agent 通过 `SkillCatalog` 的 `location` 用 `read` 打开 `SKILL.md`，reference 由入口文档按需继续读取。18 runtime pipeline/hooks 重构已落地第一批：新增 `SessionWritePlan` / `SessionWriteExecutor` 和第一版 `ToolSessionWriteSink`，session 文件布局硬切为 `.nbook/agent/sessions/<session-id>.jsonl`，follow-up queue DTO 对象化，waiting/resume lifecycle 和 partial assistant metadata 已接入，active `profile.ingest()` 合同已删除，`defineAgentRuntime({ hooks })` 已能在 `prepareRun` / `prepareTurn` / `ingestTurn` / `prepareNextTurn` / `settleRun` 执行自定义 hook并返回 write plans、runtimeState、runtimeMessages、requestOptions/toolKeys patch；`defineAgentRuntime()` 也已能展开内置 runtime bundle，普通 profile 默认 runtime 会得到 `builtin.profilePrompt`、`builtin.sessionContext`、`builtin.transcriptPersistence`、`builtin.reportResult` hook；其中 `builtin.profilePrompt` 已显式启用 profile prepare system prompt，且自定义 runtime 不组合时不会仅因 `prepare()` 返回 `systemPrompt` 注入 provider 或 snapshot；`builtin.transcriptPersistence` 已显式返回 `transcript: "persist"`，且没有 transcript hook 时不再隐式持久化 assistant/toolResult；`builtin.reportResult` 已显式启用缺失 report_result 的 reminder retry，自定义 runtime 不组合时不会仅因 `allowedToolKeys` 包含 `report_result` 自动提醒；自动 compaction 已统一由 profile 顶层 `compaction` 配置显式启用，没有 `compaction` 配置时手动 `/compact` 报错，自动路径只在上下文超过模型窗口时返回明确错误；`builtin.sessionContext` 已先控制 `prepare().modelContextMessages` 注入 provider context；其他 built-in hook 当前作为后续行为迁移锚点，active 默认行为仍由 harness 现有路径提供；hook `runtimeState` 按 hook name namespace 隔离，同名 hook 返回对象时会浅合并；hook context 现在暴露 typed `ctx.input` 和只读 `ctx.session` facade，可通过 `ctx.session.read()` 读取当前或其他 session，并通过 `ctx.session.agentDialogueContent()` 从 source active path 构造 Agent Dialogue Content；`ingestTurn` 支持 `transcript: "runtime_only"` 跳过默认 assistant/toolResult 落盘，同时保留同 run 的 `report_result` / `settleRun` 能力，缺失 `report_result` 的 reminder 在 runtime-only 下也不会写入 session history。`RunFrame` / `TurnSnapshot` / `TurnOutcome` / `RunLoopResult` 已落地且类型边界已抽到 `server/agent/harness/run-kernel-types.ts`，其中 `RunLoopResult` 是 `completed | waiting | failed` 判别联合，failed 分支携带结构化 `InvocationErrorInfo`；Run Kernel stage error phase 包装已抽到 `server/agent/harness/run-kernel-error.ts`，ingest/compaction/model stage 抛错时会保留更准确的 lifecycle error phase；Turn Transaction outcome 应用已抽到 `server/agent/harness/turn-transaction.ts`；turn continuation / shouldStop 纯判定已抽到 `server/agent/harness/turn-continuation.ts`，partial failure helper和 failed ingest draft 已抽到 `server/agent/harness/turn-failure.ts`，RunFrame 状态写回已抽到 `server/agent/harness/run-frame-state.ts`，prepare-next-turn reducer 已抽到 `server/agent/harness/prepare-next-turn.ts`；`runLoop()` 已改为逐轮创建冻结 TurnSnapshot、执行 TurnOutcome、再进入 ingest；tool-side `savePoint` writes 会排入 RunFrame并与本轮 transcript 同 batch flush，`immediate` writes 仍实时发布；自动 compaction 已迁到 turn save point 后、下一轮前，触发后会重建 RunFrame context，启用入口是 profile 顶层 `compaction` 配置而不是 runtime built-in。缺失 report_result 的 reminder retry 已迁到同一个 RunFrame 的 continuation / next-turn边界，不再由 finalize 递归重跑旧 loop。harness active 路径中的 linked-agent entry、tool custom state、profile prepare writes、variable patch audit、client variable ack 和 compaction entry 已通过 executor 发布。17 session title/summary summarizer 已按 18 runtime hooks 落地：builtin profile key 硬切为 `summarizer`，旧 `session.summarizer` 源文件和 compiled artifact 已删除；source profile 可声明 `summarizer` companion，harness 在 source invocation completed 后后台 coalesced 调度 hidden system summarizer session；summarizer 在 `ModelContext` 中从 source active path 构造 Agent Dialogue Content，使用 `runtime_only` transcript，不保存自身 assistant/toolResult 历史；成功后通过 active-leaf scoped projection 写回 source `title` / `summary`，并把 running/dirty/error/token 状态投影到 session snapshot。`writer` 已硬切为“一章节一 agent”，输入使用 `chapterPaths` 和 `lorebookEntries: string[]`，自动展开本章 Chapter Plot 并只写显式章节 `index.md`；writing presets 已迁到 `agent/writing-presets/{styles,references}` 并纳入 user-assets 同步。正式 HTTP 和前端抽屉已迁到 `/api/agent/sessions/**` 的 session/invocation/event contract，旧 thread/subagent 前端心智已替换为 session/linked agent；临时 `/api/agent-v3/**` 和旧 `/api/agent/threads/**` 已删除。
- Agent runtime 最新补充：compaction 配置已从 Profile DSL `<Compaction>` 节点迁到 `defineAgentProfile({ compaction })` 顶层静态配置；`ProfileTurnPlan` 不再携带 compaction。自动 compact 成功后，harness 会在下一轮前重新注入一次 `HistorySet` 初始化消息，再重建 `RunFrame.messages`。compaction planner 的 recent cut 现在按模型可见 entry 计算，包含普通 `message` 与 visible `custom_message`；summary 输入默认只包含普通 `message`，避免把 HistorySet/Import/system-reminder 复述进摘要。builtin profile 顶层 `compaction: {}` 统一走默认值：`enabled: true`、`reserveTokens: 25_600`、`keepRecentTokens: 24_000`。
- Agent runtime 最新补充：`builtin.sessionContext` 现在不仅控制 `prepare().modelContextMessages` 注入 provider context，也控制 `historyInitMessages`、`modelContextAppendingMessages`、`appendingMessages` 是否写入 session history；`stateWrites` 仍作为 profile 私有状态写入。
- Agent runtime 最新补充：`ProfileTurnPlan` 持久化编译已抽到 `server/agent/harness/prepare-run.ts` 的 `compilePrepareRunWritePlan()`；`prepare()` 现在只返回 `{ plan, writePlan }`，不再直接执行 session 写入；`invokeAgent()` 在 `prepareRun` 阶段统一执行 profile prepare write plan，后续可把 Default Persistence Hook 接到更明确的 `prepareRun` reducer。
- Agent runtime 最新补充：`builtin.sessionContext` 已从静态扫描迁到可执行 `prepareRun` built-in hook；`prepare()` 和首轮 `modelContextMessages` 注入都读取 `prepareRunHooks.sessionContext`。`builtin.reportResult` 也已改为可执行 `prepareRun` built-in hook，缺失 `report_result` 的 reminder retry 从第一轮 continuation 判定开始读取 `prepareRunHooks.reportResultReminder`，不再静态扫描 profile runtime。
- Agent runtime 最新补充：`invokeAgent()` 内的 prepareRun 编排已抽成私有 `prepareRun()` helper，集中处理 resolution restore、prepareRun hooks、profile prepare/write plan、pending prompt 写入和首轮 RunFrame 输入组装；入口主流程更接近 Coordinator -> prepareRun -> runLoop -> settleRun -> cleanup。
- Agent Plan Mode 最新补充：计划目录已收敛为当前 Project Workspace 的 `.agent/plan/`；普通 Project agent 从 Workspace Root cwd 写计划文件时使用 `<project>/.agent/plan/<slug>.md`，`exit_plan_mode.planFilePath` 仍传 Project Workspace 相对 `.agent/plan/<slug>.md` 供审批 UI 预览。用户手动关闭 Plan Mode 会写入 exit reminder，不再让下一轮模型继续看到 “Plan mode still active”。
- Agent runtime 最新补充：accepted invocation 的 terminal 处理已抽成 `completeInvocation()` / `failInvocation()` helper，单轮 turn 的执行事务也已收敛到 `runTurnTransaction()`；active leaf 移动新增 `SessionWritePlan.moveLeaf` op，retry/tree/empty 控制面写入现在也走 executor；follow-up queue 会通过 `agent.followUpQueue` projection custom state 恢复 snapshot；SSE public event 已硬切为 projected runtime event，`kind: "pi"` 改为 `kind: "runtime"`，`session_state_changed` 只携带 lightweight live state，`agent_end` / `turn_end` 不再携带完整 messages/toolResults，`InvokeAgentResult.events` 已删除；旧后端 `session/slash-commands.ts` 已删除，slash command 只保留前端识别到正式 command/tree HTTP 入口这一条路径；compaction 模块已硬切为调用方注入写入函数，不再自行 append repo。
- Agent runtime 最新补充：invocation 现在带内部 caller identity（`user | agent | sidecar | system`），缺省为 `user`；HTTP 用户入口固定 user caller，不暴露可伪造的内部身份。`invoke_agent` 工具会把 source session/profile/toolCallId 写成 agent caller，summarizer / follow-up 等内部触发写成 system caller，sidecar run 使用 sidecar caller 但保持原有 sidecar reminder 关闭策略。`builtin.reportResult` 已按 caller identity 控制缺失 `report_result` reminder：用户直聊默认不提醒，非用户 caller 且目标 profile 暴露 `report_result` 时继续通过 continuation/next-turn 注入 reminder。`simulator.leader` 已新增 `bash`、移除 `report_result`，弱化 OutputSchema 为普通文本合同，并通过 `reference/agent/workspace-tool-use.md` 共享 workspace tool-use 规则；它现在作为 `simulation/` runtime owner 维护 subjects/entities/runs、调度 actor simulator，并用普通 Markdown handoff 返回。
- Agent profile 最新补充：`leader.default`、`leader.assets`、`rp.leader` 和 `simulator.leader` 的创建 input 已统一为空对象。当前 Project 归属由 session `projectPath` / `WorkspaceFocusReminder` 表达，`rp.leader` 与 `simulator.leader` 根据当前 Project 推导 `manual/` 和 `simulation/` 路径；前端默认 profile 新建入口继续传 `input: {}` 即可创建这些 leader session。
- Agent profile 最新补充：新增共享职责地图 `reference/agent/profile-routing.md`，并由 `leader.default`、`leader.assets`、`rp.leader`、`simulator.leader` 在 `HistorySet` 导入。入口 leader 现在能看到常用 profile 的简短职责、适合/不适合任务和错位转场建议；当当前任务明显不属于自身职责时，应说明原因并建议用户新建或切换到对应 agent。
- Agent session 最新补充：历史 session 读取已兼容缺失或不可运行 profile。`listSessions`、snapshot、live state 和 relations 会返回 `summary.profileAvailability` / `profileIssueMessage`，profile 缺失时 pending approval 仅降级识别全局审批工具；真正会继续运行或复制运行依赖的入口（prompt/continue/steer/followup/resolution、`new`、`compact`、`tree + invoke`）会拒绝并提示恢复 profile 或新建/切换其他 profile。前端在当前 session 不可运行时进入只读历史模式，禁用发送、Plan Mode、模型/思考参数、compact、编辑保存和重跑等会影响后续运行的动作，同时保留查看历史、复制、切换分支、归档和关联 agent 面板。
- Agent runtime 最新补充：`SidecarProfilePass` V1 已进入 Harness 核心机制，profile 可声明 `prepareRun` / `settleRun` 旁路；sidecar 沿用当前 session/profile/session tree，在父 run 当前节点 fork 旁路 leaf 执行，sidecar transcript 持久化到该旁路分支但不成为主 active path，并通过 `merge()` 注入 runtimeMessages、persistedMessages、runtimeState 或 writePlans。`runtimeMessages` 只进父 run 模型上下文；`persistedMessages` 写入父 session active path，第一版只允许 user message、origin 为 `harness`，`prepareRun` 合并后若 provider-visible context 超出模型窗口会直接失败。provider-visible tools 和 schema 保持 profile 最大集合以保护工具/schema 缓存，sidecar `toolKeys` 作为执行权限子集且只能引用 root `tools`；`report_result.data` 只用于主路结构化结果且保持 optional，旁路结构化结果必须通过 `report_sidecar_result.data` 返回；`report_sidecar_result.data` 的模型可见 schema 来自全部 `sidecarDataSchema` 的 profile-stable union，执行期再按 active sidecar 精确校验。`simulator.actor` 已接入 `actor.context-load` / `actor.memory-save`，创建 input 只接收 `{ subjectPath }`；`actor.context-load` 返回纯文本 `report_sidecar_result.data` 并在主 run 前持久化注入 `<actor-sidecar-context>`，`actor.memory-save` 在主 run 后维护 subject-facing `events.jsonl`、`memory.jsonl` 与 `mind.md`。Subject RAG 第一版使用 `{project}/.nbook/subject-rag.sqlite` 作为 SQLite + sqlite-vec 可重建缓存，事实源仍是 subject JSONL 文件；`subject_rag_search` 读取顶层 `embedding` 服务配置，Node/Nitro runtime 使用 `node:sqlite` 加载 sqlite-vec，Bun smoke 继续可用，写入工具只标 dirty，search 前同步重建 dirty source；Agent 工具层必须显式传单一 source `["events"]` 或 `["memory"]`，不默认双搜也不允许一次搜两层；Global 配置 provider/model/API Key/baseURL/dimensions，Global 启用但模型/维度/timeout 留空会写默认 `text-embedding-3-small` / `1536` / `30000ms`，Project 只覆盖 model/dimensions，未配置时明确报错，不做关键词 fallback，embedding 超时会明确失败而不是让工具永久执行中。`subject_memory_update` 会把 subject-facing facts 数组交给真实 `memory.curator` profile 生成 JSON Patch，工具层应用、校验并写回 `memory.jsonl`。IDE 左侧已新增 Project 级 `RAG` 可视化入口，可浏览当前 Project subjects 的 events/memory、执行真实 subject-scoped RAG 搜索、重建索引，并对 JSONL 事实源做基础 CRUD。
- Agent profile 最新补充：profile 工具合同已硬切到 root `tools` binding，生产代码不再接受 `allowedToolKeys` / `mainRunAllowedToolKeys`。`tools` 决定 profile 最大工具集合和模型可见 schema，主 run 用 `mainRunToolKeys` 收窄执行权限，sidecar 用 `toolKeys` 收窄执行权限且只能引用 root tools；runtime `prepareTurn.turnSnapshotPatch.toolKeys` 也只裁剪执行权限，不改变 provider-visible tools/schema。`report_result` 现在只从 profile root binding 和主路 `OutputSchema` 生成 profile-stable provider-visible schema；`report_sidecar_result` 从全部 sidecar `sidecarDataSchema` 生成 profile-stable provider-visible union schema。active sidecar 必须通过 `report_sidecar_result.data` 返回结构化结果，校验失败在 tool execution 阶段产生模型可见 error toolResult 并允许同一 run 修正。`create_agent.input` 保持任意 JSON object，不做 profile-aware union，也不再把 JSON string 静默 normalize；模型应先调用 `get_agent_profile` 查看目标 InputSchema，再传真实对象。
- Agent profile 最新补充：结果工具拆分的缓存稳定版已补齐残余边界。缺失结果 reminder 现在按当前 turn `executionToolKeys` 判断是否能执行期望结果工具，不再被 provider-visible root tools 误触发；sidecar 中错用 `report_result` 连续失败时，Runtime Error 摘要按期望的 `report_sidecar_result` 收口并保留真实工具错误；`final_message_as_result` 只允许明确 string `sidecarDataSchema`，union/object 等复杂旁路结果必须使用 `parse_final_message_json` 或 `report_sidecar_result`。
- Agent profile 最新补充：工具定义层已收敛到 `AgentToolDefinition` / `defineAgentTool()`。内置工具通过 definition `.runtime()` 汇总注册；profile root `tools` 可引用轻量内置 binding、直接内联 profile 自带 definition，或用 `tools.registered(key)` 引用已注册插件工具。Harness 解析工具时先查当前 profile 私有 definition，再回退全局 registry；自带工具作用域限定在当前 profile run，自带审批工具复用现有 suspend + resolution toolResult 语义。工具聚合入口已整理为 `server/agent/tools/index.ts`，控制工具与 agent 协作工具分文件维护；`createBuiltinTools()` 为无参入口，agent 协作工具只支持 session context 内的 `executeWithContext`。
- Agent runtime 最新补充：TSX Profile DSL 新增显式 `Import` string fragment，profile 可在 `System` 或 `HistorySet > Message` 中加载共享文本上下文；V1 只允许 `AGENTS.md`、`reference/**`、`docs/**`，支持 Markdown heading 截取、UTF-8 `maxBytes` 截断和 fenced block 输出，缺失文件默认返回空字符串，`required=true` 时保持严格。`leader.default` 已通过 `Import` 加载 `reference/agent/leader-default.md`、`reference/content/markdown-dialect.md` 和 `reference/agent/project-workspace-guide.md`，把工具、Task Management、多 Agent、SQL、Plan Mode、Skills、Markdown 扩展和 Project Workspace 文件语义从长 system prompt 拆到稳定参考；Profile Workbench 已能解析、展示和拖入 `Import` 节点；Nitro build 后会复制 `AGENTS.md`、`reference/`、`docs/` 到 `.output/server/`，保证 compiled runtime 可解析导入。
- Agent profile 最新补充：新目录规范后的 prompt engineering V1 已落地。`simulator.leader` 成为写作模式和 RP 模式共用的世界模拟主管，负责 simulation 裁决、subject 调度、state commit 建议、writer-safe brief 和 director handoff；`director` 成为剧情导演，负责 Thread / Scene / Plot 设计和 Plot System 落库，不写正文也不维护 simulation state；`simulator.actor` 作为唯一 subject simulator profile，旧 actor legacy profile 已删除。Plot 工具新增 `create_story_plots`，支持同一 Scene 批量追加行动级 Plot；Plot Agent Spec 已沉淀到 `reference/plot/agent-spec.md`，写作 workflow skills 和 agent reference 已同步使用 `simulator.leader` / `director` / `simulation/` 口径。
- Agent profile 最新补充：RP 正文归属已收紧为“所有世界内用户可见正文都由 `rp.writer` 写”。`rp.leader` 仍负责开局引导、化身确认、IC/OOC 审查、调用 `simulator.leader` 和编写 Writer Brief，但不直接撰写开场白、初始化 prose 或常规 Tick prose；开场白 / 初始化正文固定通过 `rp.writer` 写入 `simulation/runs/ticks/000000-initial-state/prose.md`，`rp.leader` 最终只组装正文链接和元场景引导。
- User-assets 同步已改为黑名单式 managed sync：`assets/workspace/.nbook` 下除本地状态、session、compiled artifact、profile metadata 和专用 profile/variable 源外的大部分系统资源都会进入 `.profile-sync-state.json` 跟踪，包含 `agent/skills/**`、`templates/**`、`agent/profile-templates/**`、`agent/bin/**`、`agent/scripts/**`、`agent/config/**` 和 writing presets；未手改用户覆盖会随系统更新自动更新，已手改且系统更新时保留用户文件并通过 sync warning / Diff Workbench 查看差异。系统删除传播第一版不自动删除用户旧文件。
- Config 系统：配置已拆为 Boot Config、Global Config、Project Config 和 Browser State。Boot Config 使用根 `config.yaml`；Global Config 使用 `workspace/.nbook/config.json`；Project Config 使用 `workspace/{project}/.nbook/config.json`；正式 API 是 `/api/config/*`，旧 `/api/settings/*` 与 `/api/workspace-settings` 已删除。
- 外部小说素材：`novel-import-tomato-reference` 第一版支持免费小说 epub 与 Tomato Novel Downloader 本地结果导入到当前 Project Workspace 的 `reference/tomato/`，用于后续拆书和结构分析；SillyTavern 导入使用 canonical skill `novel-import-silly-tavern-card`，CLI 位于同名 skill 目录。CLI 支持本地 `.json` / `.raw.json` / best-effort `.png` inspect，默认把稳定文本从 `reference/silly-tavern/` 导入到 `lorebook/character`、`lorebook/location`、`lorebook/faction`、`lorebook/species`、`lorebook/world/rule`、`lorebook/system`、`lorebook/item`、`lorebook/event` 或 `lorebook/note`；混合职责条目不自动拆分，低置信稳定设定进入 pending note，动态 worldbook 条目归档到 `reference/silly-tavern/{slug}/dynamic-worldbook/`，卡片主体开场素材归档到 `reference/silly-tavern/{slug}/card-body/`，`--rp` 额外归档动态机制到 `reference/silly-tavern/{slug}/simulation-migration/`，不创建 `simulation/subjects`、`simulation/entities` 或 `simulation/runs`。
- 文档治理：把稳定参考、调研资料、任务 walkthrough 分开维护。
- 全站鉴权：默认启用的 cookie session 登录、管理员用户管理页、主界面右上角头像菜单、登录失败限流、复杂密码生成、Global Config `auth.enabled` 开关和初始管理员创建脚本已经接入。

## Module Status

| Module | Status | Notes |
| --- | --- | --- |
| Workspace | Active | 已转向 portable Project Workspace，术语以 [reference/workspace/TERMS.md](reference/workspace/TERMS.md) 为准：Workspace Root 默认 `workspace/`，Project Workspace 默认 `workspace/{project}/`，根 `project.yaml` 是项目唯一真相源。内容节点模板和默认 Project 目录模板已切到 Bundled Workspace Template `assets/workspace/.nbook/templates/...`，用户覆盖层是 `workspace/.nbook/templates/...`；`workspace project create` 默认使用 `project-directory-templates` 创建 Project Workspace、写入 `project.yaml`、初始化 Project SQLite，并随默认模板生成 `simulation/`；支持 `--target <dir>` 指定实际 Project Workspace 目录，目标已存在且显式传 `--template project-directory-templates` 时可补齐缺失模板文件；manuscript 模板使用 `manuscript/001-volume/001-chapter/` 的 volume / chapter 层级；Project Workspace 与 user-assets 文件树 snapshot 由统一 index manager 持有 watcher、dirty/rebuild、revision 和 subscriber，SSE 只订阅 index 更新；文件树和干净编辑器会响应真实文件变化，网页/磁盘同时修改时走保存冲突对话框。 |
| Markdown Studio | Active | 主编辑器只保留富文本和源码两种模式；图片、inline code、workspace/domain reference 和表格在富文本侧使用真实 ProseMirror node / mark，并序列化回 Markdown；`StructuredTextEditor` 不再维护独立 schema，只作为表单工具栏包装层。 |
| Config | Active | 新配置真值源为 `/api/config/*`：Boot Config `config.yaml` 只放启动/部署字段，Global Config `workspace/.nbook/config.json` 保存 auth/models/agent/UI/editor 长期偏好，Project Config `workspace/{project}/.nbook/config.json` 只覆盖 registry 允许的字段；Provider API Key GET 脱敏、PUT 支持保留/清空/覆盖。设置 Dialog 已升级为配置中心，可显式切换 Global Config / Project Config / Browser State，并能选择任意 Project Config 编辑默认模型、默认 Profile、Agent Profile 共同默认模型参数和单个 Profile 模型覆盖。 |
| Auth | Active | 默认开启的全站 cookie session 鉴权已接入；Global Config `auth.enabled` 可关闭整站守卫；包含登录页、管理员用户管理页、主界面账户菜单、登录失败限流、复杂密码生成、用户状态/角色/密码重置和初始管理员脚本。 |
| Reference | Active | 文件化内容节点 refs 不再承载 visibility；Agent prompt 已收敛为 inline ref 表达自然提及、structured refs 表达稳定系统关系；Plot refs 已迁到内容节点路径，不再依赖数据库 Lorebook。 |
| Plot / Story | Active | 剧情系统已迁入 Project SQLite：每个 Project Workspace 的 `.nbook/project.sqlite` 保存 Story / StoryPhase / StoryThread / StoryScene / StoryPlot / StorySceneRef，不再依赖 App SQLite 的 `Novel` / `novelId`。refs 指向设定/角色/地点时使用 `lorebook/.../` 内容节点路径，剧情内部对象仍使用 thread/scene/plot URI；数据库不再维护 `Volume` / `Chapter` 表，Scene 挂章使用 `chapterPath` 指向 `manuscript/.../` content-node 目录。Plot 入口会把 Agent 常见的 `project-slug/manuscript/...` / `workspace/project-slug/manuscript/...` 归一为内部 `manuscript/...`，但持久化值仍保持 Project Workspace 相对路径。 |
| Agent v2 | Archived | 旧实现已移动到 `server/agent-v2` 与 `assets/agent-v2`，只作为迁移参考，不进入 active typecheck、测试或运行时。旧 `AgentThread` / `AgentMessage` Prisma 模型已删除。 |
| Agent | Active | Pi-based harness 已成为 `server/agent` 主实现：TypeBox profile、active TSX Profile DSL、`ProfileTurnPlan`、动态 `.nbook` profile/skill catalog、JSONL session、slash command、approval resume、linked agents、compaction、Pi 风格基础文件工具、agent 工具、task 工具、Plot 工具、SQL 工具和真实 provider smoke 均已跑通。18 runtime pipeline/hooks 第一批已接入 `SessionWritePlan` / `SessionWriteExecutor`、`ToolSessionWriteSink`、扁平 session 文件路径、follow-up queue 状态、waiting/resume lifecycle、partial assistant metadata、内部 `RunFrame` / `TurnSnapshot` / `TurnOutcome` / `RunLoopResult` 和最小 `defineAgentRuntime({ hooks })` runner；缺失 report_result 的 reminder retry 已迁到同一个 RunFrame 的 continuation / next-turn 边界，`report_result` 模型可见字段已从 `walkthrough` 严格改为 `result`，`ingestTurn` 支持 runtime-only transcript，且 runtime-only 下的 report_result reminder 不落 session，`prepareRun` / `prepareNextTurn` hook 已能注入不落 session 的 runtimeMessages，`defineAgentRuntime()` 已能展开内置 runtime bundle，默认 session runtime 已有 built-in hook，profile 顶层 `compaction` 配置是 compaction 唯一启用入口，`builtin.profilePrompt` 已显式启用 profile prepare system prompt，且自定义 runtime 不组合时不会注入 provider 或 snapshot；`builtin.transcriptPersistence` 已显式返回默认 persist，且自定义 runtime 不组合 transcript persistence 时不会隐式落 assistant/toolResult；`builtin.reportResult` 已显式启用缺失 report_result 的 reminder retry，自定义 runtime 不组合时不会仅因 `allowedToolKeys` 包含 `report_result` 自动提醒；没有 `compaction` 配置时手动 `/compact` 报错，自动路径只在上下文超过模型窗口时返回明确错误；`builtin.sessionContext` 已先控制 `prepare().modelContextMessages` 注入 provider context，其余 built-in hook 仍作为后续行为迁移锚点；hook runtimeState 按 hook namespace 隔离并支持对象浅合并，hook context 已暴露 typed `ctx.input` 和只读 `ctx.session.read()` / `ctx.session.agentDialogueContent()` facade，Run Kernel 类型边界已抽到 `server/agent/harness/run-kernel-types.ts`，Run Kernel stage error phase 包装已抽到 `server/agent/harness/run-kernel-error.ts`，Turn Transaction outcome 应用已抽到 `server/agent/harness/turn-transaction.ts`，turn continuation / shouldStop 纯判定已抽到 `server/agent/harness/turn-continuation.ts`，partial failure helper / failed ingest draft 已抽到 `server/agent/harness/turn-failure.ts`，RunFrame 状态写回已抽到 `server/agent/harness/run-frame-state.ts`，prepare-next-turn reducer 已抽到 `server/agent/harness/prepare-next-turn.ts`，tool-side savePoint writes 会和本轮 transcript 同 batch flush，自动 compaction 已迁到 turn save point 后、下一轮前并会重建 RunFrame context，`settleRun` hook 已能读取正常 run 的 `reportResult` 并写最终 projection。旧 `profile.ingest()` active 合同已删除，默认普通 profile 行为暂仍由旧 loop提供，完整独立 Run Kernel 模块和内置 hook bundle 仍在 18 后续。正式 HTTP 入口是 `/api/agent/sessions/**`；前端 Agent 抽屉已迁到 session snapshot + event hub + command/tree/invocation contract，保留聊天、停止、审批恢复、模型选择、Plan Mode、`/compact`、linked agents、edit/retry/rollback/fallback 的核心入口；`/clear` 现在通过 tree 清空同一 session 的 active leaf。Session 选择弹窗已升级为带 profile/status/relation/limit 筛选的近期 session 管理入口，默认只显示 leader profile；linked agent 面板现在同时显示当前 session 绑定出去的 agent 和绑定当前 session 的上游 agent。Harness 已支持运行中显式 `steer` / `followup`：`steer` 在 safe point all drain 并继续当前 ReAct loop，`followUp` 在 loop 结束后 one-at-a-time 开启 fresh loop；前端 Composer 已接入“引导/队列”按钮语义、Ctrl/Meta 快捷入口、pending queue chips 和成功通知。Harness 运行错误会写入 `invocation_lifecycle.errorInfo`，前端投影为模型不可见的 Run Error 卡，并用 invoke HTTP 返回做兜底通知。变量系统第一版已接入：invoke 传 `clientState`，`ctx.input` 回到 profile 静态输入，Profile 通过 `ctx.vars` / `<Variable>` / `<VariableSchema>` 读取 `client/global/project/session` 变量，profile 内 `variableDefinitions` 可随 profile artifact 注册 `session.*` 变量，`variable_schema` / `variable_read` / `variable_patch` 已注册为 Agent 工具并按注册 schema 校验读写；`ctx.workspace` / `ctx.input.studio` 旧入口已从 active 代码删除，`leader.default` 的 Project Workspace 和 Plot Focus runtime 提醒已改为普通内联 `Reminder`。`client.*` 前端写回 ack 管线和 Workspace Root / Project 变量 definition `.compiled` 仍是后续 TODO。active user profile 覆盖已通过 `.system-profile-metadata.json` / `.profile-sync-state.json` 同步到最新版，`leader.default` 实际运行时可见 `get_agent_profile`；writer 已硬切为 `chapterPaths` 单章节合同，retrieval 输入已精简为 prompt-only，输出 `{ entries, note? }` 给 Leader 判断，Leader 调 writer 时只传 `entries[].path` 数组；user-assets 同步现在默认管理 bundled `.nbook` 大多数资源，包含 skills、templates、profile-templates、bin/scripts/config 和 writing presets，黑名单排除本地状态、sessions、compiled artifact 与专用 metadata，未手改自动跟随、已手改返回 diff warning。 |
| Skills | Active | 新 Agent 后端只扫描 `assets/workspace/.nbook/agent/skills` 与 `workspace/.nbook/agent/skills`；v2 的写作流程、初始化、番茄导入、SillyTavern 角色卡导入、RP模式、skill-creator 和 TSX profile editing skills 已整体迁到 v3 系统 skill root，`RP目录初始化` 已删除，`assets/agent-v2/skills` 仅作为归档参考。独立 `skill` 工具已禁用；模型通过 `SkillCatalog` 看到 key/name/description/when_to_use/location，需要使用时用 `read` 打开对应 `SKILL.md`，reference/scripts/templates/examples 按入口说明按需读取。`stop-slop` 已作为系统 skill 加入 `assets/workspace/.nbook/agent/skills/stop-slop/`，用于消除 AI 写作套路；`Import` DSL 现在允许读取系统 skill 文档，普通 `writer` profile 首轮 `HistorySet` 默认导入 `stop-slop/SKILL.md`。RP / simulation profiles 当前状态：`rp.leader` 已作为 builtin profile 实现，是 RP 用户交流、开局引导、化身创建、体验边界和陪伴式统筹层，读取 `manual/` 与 `agent-context/rp.leader/`，按“处境 -> 行动 -> 世界回应 -> 新选择点”组织 Tick，并在需要世界裁决、人物/环境反应或 state commit 时调用 `simulator.leader`；`RP模式` skill 的普通入口也已改为优先创建或切换到 `rp.leader`。`simulator.leader` 仍是写作模式和 RP 共用的世界模拟主管，读取 `AGENTS.md`、`agent-context/simulator.leader/context.md`、最近 Tick、相关 subject/entity 状态、lorebook、reference 和 Plot，统一调度 `simulator.actor` 并输出 writer-safe brief；旧 `leader.rp` 已删除且不作为新合同名。`simulator.actor` 主 run 直接 Import 自己的 `soul.md`（第一人称扮演手册、无 frontmatter、无秘密）作为身份，并按 invoke 传入的 `kind`（player/npc）注入行为规则：player 化身不抢话、不自创关键行动，只把上级 directive 第一人称自然化复述；npc 可按 soul.md 性格自主反应。主 run 不读取 `subject.md`（全知秘密档，只给 simulator.leader），只消费 actor.context-load sidecar 持久化注入的 actor-safe context 和 actor-facing message；context-load 是纯 RAG 检索器（不读任何文件原文），memory-save sidecar 维护 `events.jsonl`、`memory.jsonl` 与 `mind.md`（不读写 `subject.md`/`soul.md`/`state.md`），`state.md` 由 simulator leader 裁决；冷启动初始记忆由 simulator.leader 直接落进 `events.jsonl` / `memory.jsonl`，`memory-seed.md` 中转文件已移除。`rp.writer` profile input 为空，只消费上级注入的 writer brief；它保留“小猫之神”写作预设并默认导入 `stop-slop` skill，不自主遍历 `simulation/`、`lorebook/`、`manual/`、`agent-context/` 或 `reference/`，若 brief 明确指定路径才读写文件，不再要求 `report_result.data.prose`；默认 Project 模板已将 profile guidance 收敛到 `agent-context/`，新增 `agent-context/rp.leader/` 作为 RP 引导上下文和长期记忆占位，`simulation/` 只保留 `subjects/`、`entities/` 和 `runs/` 示例 Tick。系统 skill 还包含 `profile-system-guide`，用于让 `leader.assets` 用通俗方式协助用户理解和修改 harness / TSX profile / skill / user-assets 覆盖层，并提供文档索引、profile compile CLI、恢复系统版本和新建模板指导；该 skill 已新增 reference 文档，详细说明 session truth、invoke 生命周期、ProfileTurnPlan 分区、TSX DSL、user-assets 覆盖和 compile/preview/run 区别。TSX Profile Workbench 第一版已接入用户 profile 源码写入、新建模板、文件 diagnostics、prepare preview、创建 session 入口，并可解析/局部写回稳定的 `<ProfilePrompt>` TSX DSL tree；用户 profile 编辑已切为自动轻量 DSL 解析 + 手动后台 worker 编译，保存不触发自动编辑期 runtime profile loader，创建 Session 需要最近一次编译通过且源码未改动；普通 runtime catalog 已硬切为 `.compiled` 运行真相源，系统 compiled artifact 随 system assets 预编译发布，用户 compiled artifact 只能由 Workbench 或 `profile compile` 手动生成；profile 和变量 definition compiled artifact 已改为稳定文件名，hash 只保存在 manifest 中，编译和 user-assets 同步都会清理未引用旧产物；旧 `scripts/compile-profile.ts` / `scripts/check-profile.ts` / `scripts/profile-compile-cli.ts` 已被 `profile status/check/compile/preview` CLI 替换；旧 `profile-templates` / `user-profile-templates` API 仍作为 501 tombstone，不再作为新合同。 |
| Docs | Active | 文档治理继续按 `reference/` 稳定参考、`docs/tutorials/` 用户教程、`docs/tasks/` 任务 walkthrough、`docs/research/` 调研资料分层；仓库根 `spec/` 已硬切改名为 `reference/`，`reference/README.md` 作为 NeuroBook Reference Bookshelf。Agent reference 现以 `reference/agent/README.md` 为入口，包含 profile、Import、Run Kernel hooks、Sidecar 和 harness 黑盒合同；content reference 现以 `reference/content/README.md` 为入口，并把旧 lorebook information-control 长文拆为 `directory-protocol.md` 与 `information-control.md`，新增 `manual.md` 作为 Project 说明书目录稳定规范。站点新增“从第一本书到第一次 RP”基础教程，覆盖 Project Workspace、Skill 初始化、前三章写作、SillyTavern 角色卡导入和 `simulation/` 世界模拟；站内 `/agent/`、`/profile/`、`/profile-tsx/` 路由已恢复为当前合同导读页，详细实现合同继续链接到 `reference/`；旧 `docs/modules/agent/tools-reference.md` 已移除，避免 active 文档继续暴露 v2 工具名。 |

## TODO

- 跑通剧情系统的核心写作链路。
- 跑通 Lore / mention / reference 的端到端体验。
- 补齐 Writer / Planner / Critic / Editor 的多 Agent 工作流。
- 简化 story 工具和 content node schema 的重复定义。
- 继续把旧章节面板和预览页从 DB Chapter 心智收敛到 manuscript content-node。
- 收尾 Project API 命名：前端 store 和部分兼容 DTO 仍沿用 `Novel` / `currentNovelId` 命名承载 `projectPath`，后续需要在不扩大行为面的前提下重命名为 Project 术语。
- 继续清理非 Plot 链路中的旧 `lorebook://` 引用入口。
- 继续稳定 Markdown Studio/表单包装层的标题、inline code、引用、图片和表格编辑体验。
- Agent Composer 已按 Task 40 收敛为单 Composer、纯文本输入和前端禁用 waiting_user steer/followup；已补根级引用 chip 渲染、`$ skill` 首次加载刷新和原地大文本模式，后续补一次真实浏览器交互验收。
- 设计泛用自然语言编辑工具：输入目标文件、自然语言操作说明和可选上下文消息数量，由轻量模型辅助修改文件；后续可用于 Agent 记忆系统、RP 状态维护和常规文件编辑减负。
- 建立 token statistics、写作历史 statistics 和缓存命中统计。
- 扩展番茄小说导入：补基础数据、评论、全站搜索、正文下载含段评和图片等能力。
- 将 workspace 保存冲突视图与后续 Git 版本控制能力整合。
- 独立发版脚本需要保持两段式 profile 产物刷新：源码构建期继续运行 `profile:metadata` 生成 `.system-profile-metadata.json`，`product:stage` 还要在 Product Root 内重新 `profile compile --all --system`，并断言 Profile Workbench worker 的 `tsx/esm/api` 可从 `.output/server/node_modules` 解析，同时断言系统 `.compiled/*.mjs` 不含 CI/本机绝对 Product require 或 `.building.mjs` 临时引用，确保 artifact 可迁移且 dependency hash / 依赖解析都指向产品内 vendor。用户 assets 同步会基于 `.profile-sync-state.json` 自动更新未手改的系统 profile 覆盖，手改或缺 state 时保留用户文件并提示遮蔽。发布入口只保留 `bun run release -- <stable|prerelease|canary|alpha|beta|rc>`；正式版要求 SemVer `X.Y.Z`、干净工作区和 `--yes --push`，也可用 `--next patch|minor|major` 显式自动增长；先行版本按 SemVer channel/tag 校验并继续创建 GitHub prerelease，可用 `--next patch|minor|major` 基于当前发布线选择基础版本增长，当前发布线取 `package.json.version` 和当前 HEAD 最近 SemVer tag 中较新的版本，其中 alpha/beta/rc 默认扫描已有 tag 自动递增序号。
- 设置页后续继续增强 raw/effective 差异展示；当前配置中心已支持 Global / Project / Browser State 目标切换、Project selector、Project 默认模型、默认 Profile 和 Agent Profile 模型覆盖。
- 清理动态 profile 渐进迁移 fallback：生产注册路径已切到 assets profile + builtin contract，系统 assets profile 也已改成 `defineAgentProfile`；后续删除旧源码 builtin class，或把仍被复用的 prompt helper 迁成稳定公共 helper。
- 重新设计 TSX profile 的 TypeBox Schema Builder：旧 Zod 低代码 schema 编辑不再作为新合同保留；新的 Profile Workbench 第一版只做 schema 只读展示和 TypeBox 骨架生成，后续再基于 v3 profile 经验补可稳定定位的局部辅助编辑。
- 为 skill catalog 增加类似 tool 的白名单或启用控制，避免所有可发现 skill 默认进入模型可见 catalog。
- 小说写作流程和 emulation 第一版已落地到系统 skill、默认 Project 模板和 leader/RP prompt：写作 workflow skill 已统一为 `novel-workflow-01-*` 到 `novel-workflow-10-*` 的 canonical key，技法入口使用 `novel-technique-commercial-rhythm`，导入入口使用 `novel-import-*`。当前运行目录仍是 `simulation/`，写作中的 `emulation` 暂作为世界运行态推进概念。`simulation/runs` 默认模板改为 `current.md`、`index.md`、`ticks/000000-initial-state/report.md` 和 `prose.md`，`input.md` / actor packets / commit log / tool log 等机械产物留给后续 workflow/runtime 自动生成。
- 后续如果支持加载第三方 profile，新增 profile 审查 skill，用于检查陌生 `.profile.tsx` 的危险代码、工具权限、schema contract 和提示词行为；当前动态 profile 不做 sandbox，按用户可信本地代码处理。
- 后续如有需要，再把系统默认 profile 暴露到系统设置；当前默认 Agent Profile 写入 Global / Project Config，system default 仍为小说 `leader.default`、用户 assets `leader.assets`。
- 让 workspace 默认 Profile 设置页复用 profile catalog 的 loadStatus / issue 信息，避免 contract-only 或 missing profile 在设置列表中被当作普通可运行项展示；当前运行路径已阻止 contract-only profile 执行。
- 收紧 Import 白名单的 `workspace/` 放行：当前为支持 `simulator.actor` 主路 Import 自己的 `soul.md`，`isAllowedImportPath` 整体放开了 `workspace/**` 前缀（仍保留 `..` 越界检查），相当于任何 profile 都能 Import 任意 Project 运行态文件。后续需要按 profile / subject 收敛为细粒度授权，避免越权读取其他 Project 或其他 subject 的运行态文件。
- 外部 Project（workspace 容器外的绝对路径）下 `simulator.actor` 主路 Import `soul.md` 会失败，因为 Import 按 repo-root 解析 `workspace/${subjectPath}/soul.md`；当前 RP 小说都在 `workspace/` 下可接受，后续支持外部 Project 时需要换更通用的 soul 注入方式。
- Agent 前端迁移后补一次浏览器交互验收：重点验证多窗口事件同步、followUp queue、approval resume、Plan Mode、model command、compact、edit/retry/rollback/fallback 和流式工具卡片。
- Agent SSE 前端合同第一轮已落地：`/api/agent/sessions/:id/events` 已有 stream manager、自动重连、snapshot single-flight、连接状态、run phase、`contentIndex` assistant block 合并和手动重连/刷新历史入口；snapshot 已区分 `eventCursor` 恢复点与 `latestSeq` 事件流尾部，刷新运行中 session 可 replay 尚未落盘的 runtime transcript；后续保留真实浏览器验收与工具执行输出流式后端贯通。
- 后续为 Agent session 增加 provider 级 `systemPrompt` metadata/hash 记录，避免 TSX profile 源码变化后旧 session 的系统提示不可追踪；当前前端不把 systemPrompt 展示成聊天消息。
- Agent session event hub 后续支持跨进程/多实例广播；第一版是单进程内存 replay，不保证实时 fan-out。重启/dev reload 恢复已收敛为 `(eventEpoch, per-session seq)` cursor + connected handshake + snapshot reset；同进程运行中刷新通过 transcript replay anchor pin 住 EventHub replay buffer，snapshot `eventCursor` 会回到本轮未落盘 transcript 前；waiting snapshot 可从 session active path hydrate pending approval 对应的 waiting activeInvocation，`continue(resolution)` 可在新 harness 上复用原 logical `invocationId`。当前已补齐 session admission queue 和 `SessionWriteExecutor` per-session write queue，避免并发 resolution 双提交和同 session JSONL leaf 写入交错。
- TSX Profile Workbench 后续补完整辅助编辑：当前第一版已能从系统模板新建、保存、轻量解析、手动后台编译、诊断、预览、创建自定义 profile session，并解析/局部写回稳定 `ProfilePrompt` DSL tree；TypeBox Schema Builder、工具权限 checklist 局部替换、复杂 `Watch.render`、跨 helper 的完整 AST round-trip，以及是否为多 profile catalog 编译引入 worker pool 仍待重做。
- Agent 变量系统已接入 profile 编辑期类型补全底层：从 builtin/client、Workspace Root/global、Project Workspace/project 和 profile/session definitions 派生 generated `.d.ts`，让 `ctx.vars.get()`、`<Variable>`、`<VariableSchema>` 和 `watchPath` 获得 path 补全和返回值类型提示；类型产物只作为 authoring aid，不改变 runtime registry/schema 校验真相源。后续补 Workbench 变量选择器和更细的 schema-aware patch builder。
- Pi Agent Harness 迁移后续补 `invoke_agent` 非阻塞调用；v3 agent 工具已收敛为 `create_agent`、`invoke_agent`、`get_agent(id?: number)`、`get_agent_profile`、`get_session`、`detach_agent`；`invoke_agent` 工具返回 sessionId、usage、finalMessage、report_result 摘要，不返回完整 history；`request_user_input`、`enter_plan_mode`、`exit_plan_mode` 的审批/回答恢复统一走 `continue + resolution`，由 harness 补齐 toolResult 后继续。独立 `skill` 工具已禁用，skill 正文通过 `read` 读取 SkillCatalog location。
- Pi Agent Harness 迁移后续评估文件/变量回溯：变量可通过 session custom state entry reduce，文件回溯需要专门的 `file_snapshot` / `file_patch` entry 或接入 Git/worktree snapshot，第一版不承诺文件内容回滚。
- 观察 `arch` source 模式快速同步脚本的稳定性，并决定是否要把远端部署目标做成可配置 preset。
- 修复 workspace-files API tests 的 Vitest mock API 兼容问题；当前 `server/workspace-files` 通过，`server/api/workspace-files` 的部分测试仍受 `vi.resetModules` / `vi.doUnmock` 不存在影响。
- 重构审批系统，把工具审批一般化为可由工具定义声明的协议。当前审批是 harness 特判：`approvalToolKeys`（`request_user_input` / `enter_plan_mode` / `exit_plan_mode`）命中即 suspend，approved 后不执行工具 execute，只用固定 `tool_approval` / `user_input` resolution 落 toolResult，前端渲染（approve/reject、user_input 表单、plan 预览）硬编码在 `pendingApprovalDto` + 前端组件。工具层重构后 `defineAgentTool` 只支持语义 A 的 `approvalRequired`（仅 suspend 确认，approved 落文本 toolResult，不回调 execute）。后续需要做：语义 B（approved 后真正执行工具 `executeWithContext`，在 resolution 链路新增执行分支）、自定义 resolution shape、工具自带审批校验逻辑、动态审批 UI 渲染协议。

## Risks

- 文档和代码容易漂移，需要每次重大任务结束时更新仓库状态和任务 walkthrough。
- Agent prompt 背景信息过多时，模型可能过度迎合噪声，后续要控制信息注入。
- Markdown Studio 的 ProseMirror node / mark 与 Markdown 序列化如果不同步，容易产生源码模式和富文本模式行为漂移。
- Agent 前端已迁到新 session contract，但尚未做浏览器交互验收；多窗口同步、流式工具卡和审批恢复仍需要在真实开发服务器里走一遍。

## Recent Tasks

| Task | Status | Walkthrough |
| --- | --- | --- |
| Agent RP Mode Planning | Planning | [docs/tasks/01-agent-roleplay-mode/README.md](docs/tasks/01-agent-roleplay-mode/README.md) |
| RP Manual Directory And RP Leader Naming | Implemented | [docs/tasks/45-rp-manual-directory-and-rp-leader-naming/README.md](docs/tasks/45-rp-manual-directory-and-rp-leader-naming/README.md) |
| Pi Agent Harness Migration | In Progress | [docs/tasks/02-pi-agent-harness-migration/README.md](docs/tasks/02-pi-agent-harness-migration/README.md) |
| Config System | Done | [docs/tasks/03-config-system/README.md](docs/tasks/03-config-system/README.md) |
| TSX Profile Workbench | Active | [docs/tasks/04-tsx-profile-workbench/README.md](docs/tasks/04-tsx-profile-workbench/README.md) |
| Leader Profile v2 Adaptation | Done | [docs/tasks/05-leader-profile-v2-adaptation/README.md](docs/tasks/05-leader-profile-v2-adaptation/README.md) |
| Leader Default Prompt Parity | Updated | [docs/tasks/06-leader-default-prompt-parity/README.md](docs/tasks/06-leader-default-prompt-parity/README.md) |
| Project Identity Migration | Planning | [docs/tasks/09-project-identity-migration/README.md](docs/tasks/09-project-identity-migration/README.md) |
| Portable Project Workspace | In Progress | [docs/tasks/11-portable-project-workspace/README.md](docs/tasks/11-portable-project-workspace/README.md) |
| Profile Variable Types | Implemented | [docs/tasks/12-profile-variable-types/README.md](docs/tasks/12-profile-variable-types/README.md) |
| Agent SSE Front-End Contract | Implemented | [docs/tasks/14-agent-sse-front-end-contract/README.md](docs/tasks/14-agent-sse-front-end-contract/README.md) |
| Agent Session Management | Implemented | [docs/tasks/15-agent-session-management/README.md](docs/tasks/15-agent-session-management/README.md) |
| Agent Steer / FollowUp Composer | Implemented | [docs/tasks/16-agent-steer-followup-composer/README.md](docs/tasks/16-agent-steer-followup-composer/README.md) |
| Agent Composer Plain Text Input | Implemented | [docs/tasks/40-agent-composer-plain-text-input/README.md](docs/tasks/40-agent-composer-plain-text-input/README.md) |
| Session Title/Summary Enhancement | Implemented | [docs/tasks/17-session-title-summary-enhancement/README.md](docs/tasks/17-session-title-summary-enhancement/README.md) |
| Agent Runtime Pipeline Hooks | Implemented | [docs/tasks/18-agent-runtime-pipeline-hooks/README.md](docs/tasks/18-agent-runtime-pipeline-hooks/README.md) |
| Agent Sidecar Profile Pass | Implemented | [docs/tasks/23-agent-sidecar-profile-pass/README.md](docs/tasks/23-agent-sidecar-profile-pass/README.md) |
| Agent Profile Tool Bindings | Implemented | [docs/tasks/47-agent-profile-tool-bindings/README.md](docs/tasks/47-agent-profile-tool-bindings/README.md) |
| Agent Tool Definition Layer | Implemented | [docs/tasks/48-agent-tool-definition-layer/README.md](docs/tasks/48-agent-tool-definition-layer/README.md) |
| Agent SSE Reload Recovery | Implemented | [docs/tasks/24-agent-sse-reload-recovery/README.md](docs/tasks/24-agent-sse-reload-recovery/README.md) |
| Agent Harness Black-Box Tests | Implemented | [docs/tasks/25-agent-harness-black-box-tests/README.md](docs/tasks/25-agent-harness-black-box-tests/README.md) |
| Windows Portable Packaging | Updated | [docs/tasks/26-windows-portable-packaging/README.md](docs/tasks/26-windows-portable-packaging/README.md) |
| Agent Mode Layout | Implemented | [docs/tasks/27-agent-mode-layout/README.md](docs/tasks/27-agent-mode-layout/README.md) |
| Markdown Studio Welcome Agent Onboarding | Implemented | [docs/tasks/50-markdown-studio-welcome-agent-onboarding/README.md](docs/tasks/50-markdown-studio-welcome-agent-onboarding/README.md) |
| Agent Profile Routing | Implemented | [docs/tasks/52-agent-profile-routing/README.md](docs/tasks/52-agent-profile-routing/README.md) |
| NeuroBook Directory Protocol | Planning | [task](docs/tasks/28-lorebook-information-control-protocol/README.md), [directory](reference/content/directory-protocol.md), [information-control](reference/content/information-control.md) |
| Reference Bookshelf Reorg | Active | [docs/tasks/30-reference-bookshelf-reorg/README.md](docs/tasks/30-reference-bookshelf-reorg/README.md) |
| Agent Profile Import Node | Implemented | [docs/tasks/29-agent-profile-import-node/README.md](docs/tasks/29-agent-profile-import-node/README.md) |
| User Onboarding Tutorials | Implemented | [docs/tasks/33-user-onboarding-tutorials/README.md](docs/tasks/33-user-onboarding-tutorials/README.md) |
| Agent Prompt Engineering: Simulation And Director | Implemented | [docs/tasks/36-agent-prompt-engineering-simulation-director/README.md](docs/tasks/36-agent-prompt-engineering-simulation-director/README.md) |
| System Assets Preflight | Planning | [docs/tasks/37-system-assets-preflight/README.md](docs/tasks/37-system-assets-preflight/README.md) |
| Profile Context Memory | Implemented | [docs/tasks/38-lorebook-context-memory/README.md](docs/tasks/38-lorebook-context-memory/README.md) |
| Simulation Rollback Mechanism | Planning | [docs/tasks/42-simulation-rollback-mechanism/README.md](docs/tasks/42-simulation-rollback-mechanism/README.md) |
| Subject RAG Memory | Implemented | [docs/tasks/43-subject-rag-memory/README.md](docs/tasks/43-subject-rag-memory/README.md) |
| RAG Visualization | Implemented | [docs/tasks/44-rag-visualization/README.md](docs/tasks/44-rag-visualization/README.md) |
| Full Site Auth | Done | [docs/tasks/archived/fullsite-auth/README.md](docs/tasks/archived/fullsite-auth/README.md) |
| Docker Compose Deployment | Updated | [docs/tasks/archived/docker-compose-deployment/README.md](docs/tasks/archived/docker-compose-deployment/README.md) |
| Provider Reasoning Replay | Done | [docs/tasks/archived/provider-reasoning-replay/README.md](docs/tasks/archived/provider-reasoning-replay/README.md) |
| TSX Profile Template Editor | Preview Debug Workbench | [docs/tasks/archived/tsx-profile-template-editor/README.md](docs/tasks/archived/tsx-profile-template-editor/README.md) |
| Agent TSX Prompt Context | Done | [docs/tasks/archived/agent-tsx-prompt-context/README.md](docs/tasks/archived/agent-tsx-prompt-context/README.md) |
| Agent Plan Mode | Done | [docs/tasks/archived/agent-plan-mode/README.md](docs/tasks/archived/agent-plan-mode/README.md) |
| 内容节点 retrieval / inject | Done | [docs/tasks/archived/content-node-retrieval/README.md](docs/tasks/archived/content-node-retrieval/README.md) |
| 内容节点状态与信息差 | Done | [docs/tasks/archived/content-node-state/README.md](docs/tasks/archived/content-node-state/README.md) |
| 番茄小说导入 Skill | Done | [docs/tasks/archived/tomato-novel-skill/README.md](docs/tasks/archived/tomato-novel-skill/README.md) |
| 文档体系重整 | Updated | [docs/tasks/archived/documentation-reorg/README.md](docs/tasks/archived/documentation-reorg/README.md) |
| Markdown Studio Notion-like 富文本 | Done | [docs/tasks/archived/markdown-studio-notion-rich-text/README.md](docs/tasks/archived/markdown-studio-notion-rich-text/README.md) |
| Workspace 文件实时同步与保存冲突 | Done | [docs/tasks/archived/workspace-file-sync/README.md](docs/tasks/archived/workspace-file-sync/README.md) |
| 用户 Assets 工作区 | Done | [docs/tasks/archived/user-assets-workspace/README.md](docs/tasks/archived/user-assets-workspace/README.md) |

